# Maximum Strong Independent Sets in Hypergraphs: Reductions, Bounds, and Greedy Certificates

Yingquan (Cody) Wu<sup>∗</sup> MBZUAI Institute of Foundation Models Sunnyvale, CA, USA icodywu@gmail.com

Jason Cong University of California, Los Angeles Los Angeles, CA, USA cong@cs.ucla.edu

Preprint. Under review at the Journal of the ACM.

## Abstract

We study the maximum strong independent set problem in a finite hypergraph: find the largest vertex set that intersects every hyperedge in at most one vertex. This objective arises whenever each observed block is a local incompatibility constraint but transitive closure across overlapping blocks is not justified. A motivating example is multi-band LSH-MinHash deduplication, where each collision bucket gives local evidence, while connected-component contraction can impose spurious global equivalences. The paper develops an incidence-structural toolkit for this problem. We prove exact reductions for dominance, incidence twins, and weight-1 blocks; derive closed-form and low-weight upper bounds; introduce puncturing and covering certificates that sharpen those bounds; and analyze a layered greedy clustering algorithm driven by block weights and residual incidence. The algorithmic analysis includes feasibility, maximality, conditional optimality, a layered witness-matching upper bound, and incidence-local complexity bounds. The results give correctness, termination, fixed-point, and optimality certificates for broad incidence families, together with examples showing when diferent certificates separate or coincide.

Keywords: maximum independent set, strong independent set, hypergraphs, set packing, structural bounds, greedy algorithms.

ACM CCS: Theory of computation—Approximation algorithms analysis; Problems, reductions and completeness. Mathematics of computing—Hypergraphs.

## 1 Introduction

A strong independent set in a hypergraph is a vertex set that contains at most one vertex from each hyperedge. The corresponding maximum strong independent set problem is a natural abstraction for selection under local block constraints: a block asserts that its vertices are mutually incompatible for simultaneous selection, but overlaps between diferent blocks do not justify transitive contraction. This distinction is central in applications where blocks are generated by noisy or local evidence rather than by an equivalence relation.

One motivating example is large-scale LSH-MinHash deduplication [1, 2, 4, 3]. A multi-band LSH pipeline emits collision buckets band by band. Each bucket is a local incompatibility block, but connected-component contraction over overlapping buckets can over-merge unrelated vertices through chains of local collisions. The correct combinatorial object is therefore the hypergraph whose vertices are items and whose hyperedges are emitted buckets; feasible retention is exactly strong independence. A companion application study reports ClueWeb22 (1.62B documents) and HPLT (3.98B documents) ablations where, across all full-corpus rows, the puncturing certificate is strictly stronger than the converged covering certificate, and the layered greedy solver reaches at least 99.43% of the puncturing bound (within 0.5% in 16 of 20 rows, and within 0.6% in all rows) [7]. The results below use this example only as motivation. The theory is stated for an arbitrary finite hypergraph H = (V, B).

The same incidence-block objective also appears in fixed-path routing, weighted set packing and combinatorial-auction winner determination, and perfect minimal hashing [5, 6, 13]. In all cases, feasibility is governed by materialized multiway conflicts rather than by transitive equivalence classes.

This paper develops a structural and algorithmic framework for maximum strong independent set in such incidence hypergraphs. The central new contribution is an incidence-certificate layer for bounding the optimum: a closed-form block-weight upper bound, its weight-1 refinement, layered support-superset sharpening, iterative puncturing certificates, and covering/reweighting certificates. These bounds are explicit functions of the materialized vertex-block incidence lists; they do not rely on solving a linear program (LP), semidefinite program (SDP), theta-body relaxation, or exact search subproblem. Around this certificate layer, we prove exact reductions based on dominance, incidence-twin compression, canonical handling of weight-1 blocks, and pivot dominance closure. We then give algorithms that actively improve and track the certificates: iterative greedy puncturing searches residual block subfamilies for stronger bounds, greedy covering constructs independent upper-bound witnesses, and greedy layered clustering uses the same block-weight order to build feasible root sets. The proofs give validity, termination, fixed-point, feasibility, maximality, conditional optimality, layered witness-matching, and incidence-local complexity guarantees.

The guiding principle is that the useful structure is not generic graph adjacency alone but the full vertex-block incidence relation. In particular, low block weight and incidence support determine both the strongest upper-bound certificates and the order in which the bound-improving and clustering algorithms should act. This perspective separates the structural objective from any particular source of blocks and makes the framework applicable to any setting in which local incompatibility constraints are materialized as a hyperedge family.

Maximum strong independent set inherits classical hardness phenomena from Karp’s reductions [8], and sits within a broad approximation, local-search, and relaxation literature for hypergraph packing problems [14, 15, 16, 17, 18]. Bounded-degree and uniform hypergraph independent-set algorithms give important general baselines [11, 10, 12], while recent work studies data reductions for strong maximum independent set in hypergraphs [13]. Our focus is complementary: the objective is classical, but the block-weight bounds, residual certificate-search algorithms, and certificate-guided clustering schedule are new incidence-structural tools.

Table 1 summarizes the distinction. Existing set-packing and hypergraph-MIS machinery typically optimizes a feasible packing by local exchanges, proves guarantees under rank/degree restrictions, relaxes the independence constraints into an auxiliary convex program, or reduces the instance before exact search. The contribution here is not another instantiation of those templates. We introduce an incidence-certificate layer whose quantities are computed directly from the given vertex–block incidence lists: the closed-form block-weight sum, canonical weight-1 preclusion, layered support-superset sharpening, iterative puncturing, covering reweighting, and greedy witness-matching certificates. These certificates upper-bound the optimum, explain when the bound is tight, and drive the block-native greedy clustering schedule without constructing a pairwise conflict graph, solving an LP/SDP/theta relaxation, or invoking a generic local-search oracle.

Table 1: Comparison with existing approaches for hypergraph strong independent set and setpacking-type formulations.
<table><tr><td>Approach</td><td>Main object</td><td>Typical guarantee</td><td>Relation to this work</td></tr><tr><td>Classical set packing and local search [14, 15, 16, 17]</td><td>Pack disjoint sets or improve a feasible packing hardness results, often through local exchanges.</td><td>Approximation and parameterized by set size or exchange neighborhood.</td><td>Defines a neighboring optimization landscape, but does not provide the block-weight incidence certificates used here.</td></tr><tr><td>Bounded- degree or uniform hypergraph MIS [11, 10, 12]</td><td>Find large weak or strong independent sets under degree, rank, or uniformity assumptions.</td><td>;Approximation algorithms and limitations for generic hypergraph families.</td><td>Provides generic baselines; our algorithms instead use materialized block incidences, minimum block weights, and layer-local updates.</td></tr><tr><td>SDP/theta- style relaxations [12, 18]</td><td>Relax independence constraints into semidefinite or theta-body tools. programs.</td><td>bounds or approximation closed-form and residual</td><td>Optimization-based upper Complementary to our explicit certificates, which require no relaxation solver.</td></tr><tr><td>Exact preprocessing and data reduction [13]</td><td>Reduce strong-MIS instances while preserving reduction effectiveness exact solvability.</td><td>Kernelization/data- before exact or branch-based search.</td><td>Related in spirit to dominance and incidence-twin compression, but our reductions are tied to computable upper-bound certificates and greedy clustering.</td></tr><tr><td>Incidence- certificate framework (this work)</td><td>Use block weights, incidence supports, dominance, twins, puncturing, and covering to bound and cluster.</td><td>Closed-form, weight-1, puncturing, covering, witness-matching, and certificates.</td><td>Targets materialized block families directly and explains when a block-weight greedy incidence-local complexity clustering schedule tracks the strongest certificates.</td></tr></table>

## 2 Problem Formulation and Preliminaries

Let $\boldsymbol { B } = \{ B _ { 1 } , \dots , B _ { M } \}$ be a finite block family, and let

$$
V : = \bigcup _ { B \in B } B
$$

be the active universe. Any vertex that never appears in any block is unconstrained by B and is omitted from this optimization. Define the hypergraph

$$
H = ( V , B ) ,\tag{1}
$$

where elements of V are vertices and blocks are hyperedges. Throughout the theoretical sections, we use “block” and “hyperedge” interchangeably. Accordingly, when we refer to a maximum independent set objective below, we mean the strong hypergraph version: no hyperedge may contain two selected vertices.

We use the standing convention that the block family is inclusion-reduced: for distinct indices $i \neq j$

$$
B _ { i } \not \subseteq B _ { j } .\tag{2}
$$

Equivalently, if one forms the pairwise graph induced by block membership, each block B is treated as a clique on the vertex set B: every pair in B is an incompatibility pair under the strong-independence objective. This is a local selection constraint, not a transitive equivalence relation outside the stated block family.

Definition 1 (Strong independent set and maximum strong independence number). A set $R \subseteq V$ is a strong independent set of $H = ( V , B )$ if

$$
| R \cap B | \leq 1 \qquad \forall B \in B .\tag{3}
$$

The maximum strong independence number is

$$
\alpha ( \mathcal B ) : = \operatorname* { m a x } \{ | R | : R \subseteq V , \ | R \cap B | \leq 1 \ \forall B \in \mathcal B \} .\tag{4}
$$

To characterize the upper bounds and to derive our block-native greedy clustering algorithm, it is useful to record the incidence profile of each vertex. Intuitively, a vertex that appears in many blocks is globally more entangled: retaining it consumes feasibility across many local clique constraints at once.

Definition 2 (block-index incidence set and vertex degree). For each vertex $v \in V$ , define its block-index incidence set by

$$
I ( v ) : = \{ i \in \{ 1 , \dots , M \} : v \in B _ { i } \} ,\tag{5}
$$

and its vertex degree by

$$
d ( v ) : = | I ( v ) | = | \{ B \in B : v \in B \} | .\tag{6}
$$

Example 1(a) (Running example: block family and vertex degrees). We use the following block family as a running example in Sections 3–6:

$$
\begin{array} { r l } & { B _ { 1 } = \{ a , u \} , } \\ & { B _ { 2 } = \{ b , u , f , p \} , \qquad B _ { 3 } = \{ b , f , q \} , } \\ & { B _ { 4 } = \{ e , g , p \} , \qquad B _ { 5 } = \{ e , g , q \} , } \\ & { B _ { 6 } = \{ c , x , p \} , \qquad B _ { 7 } = \{ c , y , q \} , \qquad B _ { 8 } = \{ c , x , y \} , } \\ & { B _ { 9 } = \{ r , f , x , p \} , \qquad B _ { 1 0 } = \{ r , g , y , q \} , } \\ & { B _ { 1 1 } = \{ r , x , q \} , \qquad B _ { 1 2 } = \{ r , y , p \} . } \end{array}
$$

Its block-overlap graph, where blocks are adjacent when they intersect, is connected, for example through

$$
\begin{array} { r } { B _ { 1 } \cap B _ { 2 } \neq \emptyset , \quad B _ { 2 } \cap B _ { 4 } \neq \emptyset , \quad B _ { 4 } \cap B _ { 6 } \neq \emptyset , \quad B _ { 6 } \cap B _ { 9 } \neq \emptyset , \quad B _ { 9 } \cap B _ { 1 0 } \neq \emptyset , \quad B _ { 1 0 } \cap B _ { 1 1 } \neq \emptyset , } \end{array}
$$

so connected-component contraction would merge the whole instance into one connected component and retain only one representative. The vertex degrees are

$$
\begin{array} { l c l } { { d ( a ) = 1 , \qquad d ( b ) = d ( e ) = d ( u ) = 2 , } } \\ { { d ( c ) = d ( f ) = d ( g ) = 3 , } } \\ { { d ( r ) = d ( x ) = d ( y ) = 4 , \qquad d ( p ) = d ( q ) = 5 . } } \end{array}
$$

The vertices $u , f , g$ are deliberate support-superset distractors:

$$
\begin{array} { l } { I ( a ) = \{ 1 \} \subsetneq I ( u ) = \{ 1 , 2 \} , } \\ { I ( b ) = \{ 2 , 3 \} \subsetneq I ( f ) = \{ 2 , 3 , 9 \} , } \\ { I ( e ) = \{ 4 , 5 \} \subsetneq I ( g ) = \{ 4 , 5 , 1 0 \} . } \end{array}
$$

Later dominance and low-weight clustering rules will remove these distractors, revealing the block structure that drives the sharper certificate and the greedy clustering schedule.

## 3 Characterizations of Maximum Strong Independent Sets

This section records incidence reductions and canonical optimality conditions for maximum strong independent sets. The reductions either preserve $\alpha ( \boldsymbol { B } )$ exactly or identify vertices that can be fixed or suppressed without excluding all optima. These structural rules are the basis for the upper-bound certificates and greedy schedules in later sections.

A recurring structural statistic is the minimum incidence degree inside a block.

Definition 3 (block weight). For each block $B \in B .$ , define

$$
w ( B ) : = \operatorname* { m i n } _ { v \in B } d ( v ) .\tag{7}
$$

Example 1(b) (Running example: block weights). For the running example in Example 1(a), the block weights are

$$
\begin{array} { l } { { w ( B _ { 1 } ) = 1 , \qquad w ( B _ { 2 } ) = \cdots = w ( B _ { 5 } ) = 2 , } } \\ { { w ( B _ { 6 } ) = \cdots \cdot \cdot = w ( B _ { 1 0 } ) = 3 , \qquad w ( B _ { 1 1 } ) = w ( B _ { 1 2 } ) = 4 . } } \end{array}
$$

The apparent weight-3 status of $B _ { 9 }$ and $B _ { 1 0 }$ is caused only by the degree-3 support-superset distractors $f$ and $g .$ . The dominance rule below will justify deleting the distractors $u , f , g$ using $a , b , e$ as witnesses, respectively; in that reduced view the weights become

$$
\begin{array} { l } { { w _ { - } ( B _ { 1 } ) = 1 , \qquad w _ { - } ( B _ { 2 } ) = \cdots = w _ { - } ( B _ { 5 } ) = 2 , } } \\ { { w _ { - } ( B _ { 6 } ) = w _ { - } ( B _ { 7 } ) = w _ { - } ( B _ { 8 } ) = 3 , \qquad w _ { - } ( B _ { 9 } ) = \cdots = w _ { - } ( B _ { 1 2 } ) = 4 . } } \end{array}
$$

Thus the dominance-reduced family naturally decomposes into the intended weight layers 1, 2, 3, 4, while the unreduced family disguises two true weight-4 blocks as weight-3 blocks.

Definition 4 (Incidence-twin quotient). Define an equivalence relation on the active vertex universe by

$$
u \sim v \iff I ( u ) = I ( v ) .\tag{8}
$$

Since $V = \textstyle \bigcup _ { B \in B } B$ , every equivalence class has nonempty support. Let

$$
{ \overline { { V } } } : = V / { \sim }
$$

be the set of incidence-twin classes, and write [v] for the class containing v. For each block $B _ { i }$ define its quotient block by

$$
{ \overline { { B } } } _ { i } : = \{ [ v ] : v \in B _ { i } \} .\tag{9}
$$

The incidence-twin quotient family is

$$
\overline { { B } } : = \{ \overline { { B } } _ { i } : i = 1 , \ldots , M \} ,\tag{10}
$$

viewed as an indexed block family, with duplicate quotient blocks removable by the inclusion rule above if desired.

For the next theorem, write $\boldsymbol { B } = \{ B _ { 1 } , \dots , B _ { M } \}$ and use the incidence sets $I ( \cdot )$ from Definition 2. We also use a deletion shorthand: for a vertex subset $S \subseteq V$ , write

$$
\mathcal { B } - S : = \{ B \setminus S : B \in \mathcal { B } , B \setminus S \neq \emptyset \} ,
$$

with empty blocks discarded; for a single vertex $b ,$ write $\boldsymbol { B } - \{ b \}$ . In the running example, the containments

$$
I ( a ) \subsetneq I ( u ) , \qquad I ( b ) \subsetneq I ( f ) , \qquad I ( e ) \subsetneq I ( g )
$$

are the concrete cases to keep in mind: $u , f , g$ are more constrained support-superset vertices, so an optimum never needs to keep them instead of their smaller-support witnesses.

Theorem 1 (Dominance and incidence-twin compression). The following two vertex-level reductions preserve the optimal retention value.

(i) (Dominance removal) If two vertices $a , b \in V$ satisfy

$$
I ( a ) \subseteq I ( b ) ,\tag{11}
$$

then removing b does not change the optimal retention value:

$$
\alpha ( B ) = \alpha ( B - \{ b \} ) .\tag{12}
$$

(ii) (Incidence-twin compression) For the quotient family in Definition $^ { 4 , }$

$$
\alpha ( { \overline { { B } } } ) = \alpha ( B ) .\tag{13}
$$

Proof. For (i), any feasible retained set in $\boldsymbol { B } - \{ b \}$ is feasible in $B ,$ so $\alpha ( B - \{ b \} ) \leq \alpha ( B )$ . Let $O ^ { \star }$ be an optimal feasible set for B. If $) \notin O ^ { \star }$ , then $O ^ { \star }$ is feasible for $\boldsymbol { B } - \{ \boldsymbol { b } \}$ and we are done. Assume $b \in O ^ { \star }$ and define

$$
O ^ { \prime } : = ( O ^ { \star } \setminus \{ b \} ) \cup \{ a \} .\tag{14}
$$

For any index $i \in I ( a )$ , we have $i \in I ( b )$ by $I ( a ) \subseteq I ( b )$ . Since $b \in O ^ { \star }$ and $O ^ { \star }$ is feasible, no other element of $O ^ { \star }$ can lie in $B _ { i } ;$ replacing b by a keeps occupancy at most one in such blocks. All other blocks are unafected. Hence $O ^ { \prime }$ is feasible for B and $| O ^ { \prime } | = | O ^ { \star } |$ , with $b \notin O ^ { \prime }$ . Therefore there exists an optimal solution not using b, implying $\alpha ( B - \{ b \} ) \ge \alpha ( B )$ . Combining both inequalities gives equality.

For (ii), let $\pi : V \to { \overline { { V } } }$ be the projection $\pi ( v ) = [ v ]$ . First let $R \subseteq V$ be feasible for B. No two distinct vertices in R can lie in the same incidence-twin class: if $u \sim v$ and $u \ne v ,$ then $I ( u ) = I ( v ) \neq \emptyset .$ , so u and v co-occur in every block indexed by this common support, contradicting feasibility. Hence $| \pi ( R ) | = | R |$ . Moreover, if $\pi ( R )$ contained two classes in some quotient block ${ \overline { { B } } } _ { i } .$ then R would contain two vertices in the original block $B _ { i }$ , again contradicting feasibility. Thus $\pi ( R )$ is feasible for ${ \overline { { B } } } ,$ , so $\alpha ( { \overline { { B } } } ) \geq \alpha ( B )$

Conversely, let ${ \overline { { R } } } \subseteq { \overline { { V } } }$ be feasible for ${ \overline { { B } } } ,$ and choose one representative vertex $\rho ( C ) \in C$ from each class $C \in { \overline { { R } } } .$ Set

$$
R : = \{ \rho ( C ) : C \in { \overline { { R } } } \} .
$$

If R contained two vertices in some original block $B _ { i }$ , then $\overline { { R } }$ would contain the two corresponding classes in ${ \overline { { B } } } _ { i }$ , violating feasibility of ${ \overline { { R } } } .$ . Therefore R is feasible for $\boldsymbol { B }$ and $| R | = | \overline { { R } } | , \mathrm { s o } \alpha ( \mathcal { B } ) \geq \alpha ( \overline { { \mathcal { B } } } )$ Combining the two inequalities proves equality. □

Remark 1 (Cost of exact vertex-dominance closure). Theorem $1 ( \mathrm { i } )$ is an exact reduction rule, but applying it exhaustively requires detecting support containment among vertex incidence sets:

$$
I ( a ) \subseteq I ( b ) .
$$

Equivalently, one must compute the support antichain of the vertex family. If there are $N : = | V |$ active vertices, $M : = | \boldsymbol { B } |$ blocks, total incidence size

$$
I _ { \mathrm { t o t } } : = \sum _ { v \in V } d ( v ) ,
$$

and maximum vertex degree $\Delta : = \operatorname* { m a x } _ { v } d ( v )$ , then a direct sorted-list comparison over all vertex pairs costs

$$
O ( N ^ { 2 } \Delta )
$$

in the worst case. A bitset implementation instead costs

$$
O ( N ^ { 2 } M / w _ { \mathrm { w o r d } } )
$$

bit operations and

$$
O ( N M / w _ { \mathrm { w o r d } } )
$$

words of storage. Inverted-index variants avoid comparing all pairs explicitly, but still require intersecting block posting lists for many possible dominators,

$$
\bigcap _ { i \in I ( a ) } B _ { i } ,
$$

and can be output- or intersection-size dominated on dense block families. Thus full exact superset suppression is a valid maximum strong independent set preprocessing rule, but it is too expensive to use as a routine global pass at large scale.

By contrast, Theorem 1(ii) is much cheaper: incidence-twin compression only groups equal supports, which can be implemented by hashing or sorting support lists and then verifying collisions. It is therefore used as shared preprocessing. The stricter containment rule from part (i) is used later only in a bounded, layer-revealed form: Algorithm 1 applies partial dominance suppression to sharpen the closed-form certificate, rather than attempting full dominance closure of the maximum strong independent set instance.

Part (ii) can also be read as repeated application of part (i): within each class $C ,$ choose one representative $r _ { C }$ and delete every other $v \in C .$ , since $I ( r _ { C } ) = I ( v )$ . The explicit quotient notation is still useful algorithmically because it records the deleted vertex ids for later materialization.

Remark 2 (Algorithmic benefit of incidence-twin compression). Incidence-twin compression weakly reduces the active vertex count and total incidence size:

$$
| \overline { { V } } | \leq | V | , \qquad \sum _ { i } | \overline { { B } } _ { i } | \leq \sum _ { i } | B _ { i } | .\tag{15}
$$

If an incidence-twin class has size $k > 1$ and common support size $d > 0$ , the total incidence size drops by at least $( k - 1 ) d .$ In addition to lowering memory and runtime, the quotient prevents identical-support multiplicities from inflating quantities such as $n _ { \mathrm { m i n } } ( B )$ that drive puncturing, covering, and greedy layered clustering priorities. It is therefore a preprocessing step for the algorithms and certificates below.

The next characterization concerns blocks containing a vertex that appears nowhere else. When incidence-twin compression has been applied, degrees, block weights, and multiplicity counts are understood on the quotient family, with bars omitted for readability. Let

$$
{ \mathcal { B } } _ { 1 } : = \{ B \in { \mathcal { B } } : w ( B ) = 1 \} ,\tag{16}
$$

and for each $B \in B _ { 1 }$ , choose one vertex $z _ { B } \in B$ with $d ( z _ { B } ) = 1$

Theorem 2 (Canonical optimal form for weight-1 blocks). There exists an optimal feasible set $O ^ { \star }$ such that

$$
z _ { B } \in O ^ { \star } \qquad \forall B \in B _ { 1 } .\tag{17}
$$

Equivalently, every weight-1 block can be rooted by a weight-1 vertex in an optimal solution.

Proof. Define $Z : = \{ z _ { B } : B \in B _ { 1 } \}$ . Since $d ( z _ { B } ) = 1$ , each $z _ { B }$ belongs only to its own block B. Hence no two elements of Z can collide in any block, so Z is feasible.

Among all optimal feasible sets, choose O maximizing $| O \cap Z |$ . Suppose, for contradiction, that there exists $B \in B _ { 1 }$ with $z _ { B } \notin O$

If $O \cap B = \emptyset$ , then $O \cup \{ z _ { B } \}$ is feasible (because $z _ { B }$ appears only in $B )$ , which contradicts optimality of O.

Therefore $O \cap B = \{ x _ { B } \}$ for some $x _ { B } \neq z _ { B }$ . Consider

$$
O ^ { \prime } : = ( O \setminus \{ x _ { B } \} ) \cup \{ z _ { B } \} .\tag{18}
$$

This set is feasible: replacing $x _ { B }$ by $z _ { B }$ afects only block $B ,$ where the occupancy remains one, and in every other block feasibility can only improve because $x _ { B }$ is removed. Also, $| O ^ { \prime } | = | O |$ , so $O ^ { \prime }$ is optimal. But now $| O ^ { \prime } \cap Z | = | O \cap Z | + 1$ , contradicting the choice of $O$

Hence $z _ { B } \in O$ for every $B \in B _ { 1 }$ . Taking $O ^ { \star } : = O$ proves the claim.

In the running example, $B _ { 1 } = \{ a , u \}$ and $d ( a ) = 1$ . If an optimal solution used u to cover the local conflict in $B _ { 1 }$ , replacing u by a cannot create any new conflict because a appears nowhere else. Thus one may canonically root $B _ { 1 }$ at a before considering the higher-weight layers.

The preceding results characterize vertex dominance/twin compression and canonical weight-1 roots. The next rule gives a pivot-level canonicalization test built directly from Theorem 1: after deleting vertices dominated by a chosen pivot, a simple block condition guarantees an optimal solution containing that pivot.

For a pivot vertex $a \in V$ , define the vertices dominated by a, the incident-block neighborhood of $^ { a , }$ and the residual blockers of a by

$$
D ( a ) : = \{ v \in V \setminus \{ a \} : I ( a ) \subseteq I ( v ) \} , \qquad \Gamma ( a ) : = \bigcup _ { i \in I ( a ) } B _ { i } , \qquad R ( a ) : = \Gamma ( a ) \setminus ( \{ a \} \cup D ( a ) ) .\tag{19}
$$

Thus $D ( a )$ is exactly the set of vertices removable by Theorem $1 ( \mathrm { i } )$ using a as dominator, while $R ( a )$ is the set of remaining possible blockers of a after that dominance removal.

Theorem 3 (Pivot dominance closure and canonical inclusion). For any pivot $a \in V$ , the following hold.

(i) (Closure under dominance removal)

$$
\alpha ( B ) = \alpha ( B - D ( a ) ) .\tag{20}
$$

(ii) (Suficient condition for an optimal set containing a) If there exists a block $B ^ { * } \in B$ such that

$$
R ( a ) \subseteq B ^ { * } ,\tag{21}
$$

then there exists an optimal feasible set $O ^ { \star }$ with $a \in O ^ { \star }$

Proof. For (i), remove vertices in $D ( a )$ one-by-one. For each removed vertex $v \in D ( a )$ , one has $I ( a ) \subseteq I ( v )$ , so Theorem 1(i) applies with pivot $^ { a , }$ and the optimum value is preserved at each step. Since $D ( a )$ is finite, the final reduced instance satisfies $\alpha ( B ) = \alpha ( B - D ( a ) )$

For (ii), let $B ^ { \prime }$ denote the reduced instance after removing $D ( a ) . \mathrm { \ B y \ ( i ) } , \alpha ( B ^ { \prime } ) = \alpha ( B )$ . Take an optimal feasible set O for $B ^ { \prime }$ . If $a \in O$ , we are done.

Assume a $\notin O$ . Since O is feasible and $B ^ { * }$ is a block, $| O \cap B ^ { * } | \le 1$

If $O \cap B ^ { * } = \emptyset$ , then $O \cap R ( a ) = \emptyset$ because $R ( a ) \subseteq B ^ { * }$ . Adding a keeps feasibility: any potential conflict with a must come from a block containing $^ { a , }$ hence from $\Gamma ( a )$ , but $D ( a )$ was removed and $O$ has no element in $R ( a )$ . Thus $O \cup \{ a \}$ is feasible, contradicting optimality.

So $O \cap B ^ { * } = \{ x \}$ for some x. If $x \in B ^ { * } \setminus \Gamma ( a )$ , then x is in no block containing $^ { a , }$ so the same argument shows $O \cup \{ a \}$ is feasible, again contradicting optimality. Therefore necessarily

$$
x \in R ( a ) \subseteq B ^ { * } .
$$

Define

$$
O ^ { \prime } : = ( O \setminus \{ x \} ) \cup \{ a \} .\tag{22}
$$

Any block containing a can intersect $O$ only at x: its members lie in $\Gamma ( a )$ ; elements of $D ( a )$ are absent in $B ^ { \prime } ;$ and $| O \cap R ( a ) | = 1$ because $R ( a ) \subseteq B ^ { * }$ and $O \cap B ^ { * } = \{ x \}$ . Hence $O ^ { \prime }$ is feasible and $| O ^ { \prime } | = | O | = \alpha ( B ^ { \prime } )$ . So $B ^ { \prime }$ has an optimal set containing $^ { a , }$ and by value equivalence this is also optimal for the original instance. □

For the equality case below, write

$$
\boldsymbol { B } ^ { \prime } : = \boldsymbol { B } - \boldsymbol { D } ( \boldsymbol { a } )
$$

for the pivot-reduced instance.

Corollary 1 (Equality case of pivot canonicalization). If Theorem $\mathcal { 3 } ( i i )$ holds with

$$
B ^ { * } = R ( a ) ,
$$

then every optimal feasible set O for $B ^ { \prime }$ with a $\notin O$ satisfies

$$
| O \cap R ( a ) | = 1 .
$$

In particular, swapping that unique element with a yields an optimal feasible set containing a.

Proof. By feasibility and $B ^ { * } = R ( a )$ , one has $| O \cap R ( a ) | \leq 1$ . If the intersection were empty, then $O \cup \{ a \}$ would be feasible in $B ^ { \prime }$ (same argument as in the theorem proof), contradicting optimality. Hence the intersection size is exactly one, and the swap conclusion follows from Theorem $3 ( \mathrm { i i } )$ . □

The equality case also has a useful downstream consequence for the closed-form block-weight bound introduced in the next section. In the equality case $B ^ { * } = R ( a )$ , one may canonically keep $^ { a , }$ delete $D ( a )$ , and then apply Theorem 5 to the reduced instance $\boldsymbol { B } - \boldsymbol { D } ( \boldsymbol { a } )$ . This yields a weakly smaller computable upper bound than applying Theorem 5 directly to the original family, although the improvement is only local and typically marginal.

Example 2 (Dominance closure and equality-case swapping). Let

$$
{ \cal B } = \{ \{ a , d , u , v \} , \{ a , d \} , \{ u , v \} , \{ u , x \} , \{ v , y \} , \{ x , y \} \} .
$$

All vertices have incidence degree at least two. For pivot $^ { a , }$

$$
I ( a ) = \{ 1 , 2 \} , \qquad I ( d ) = \{ 1 , 2 \} ,
$$

so $d \in D ( a )$ and dominance removal deletes d without changing the optimum. In the reduced instance, $R ( a ) = \{ u , v \}$ , and this set is exactly the block $B ^ { * } = \{ u , v \}$ . Theorem 3(ii) therefore guarantees an optimal feasible set containing a.

The equality-case corollary describes the only obstruction to already adding a. Any optimum excluding a must contain exactly one vertex from $\{ u , v \}$ . For example, $\{ u , y \}$ is feasible and optimal in the reduced instance; swapping the unique element $u \in R ( a )$ with a gives the equally large optimum $\{ a , y \}$

This dominance theorem also explains why we do not state separate private-shadow certificates. If a blocker u of a pivot a has a shadow z outside $\Gamma ( a )$ with

$$
I ( z ) \subseteq I ( u ) \setminus I ( a ) ,
$$

then $I ( z ) \subseteq I ( u )$ , so u is already removable by Theorem 1(i). Thus such shadow arguments are best viewed as dominance preprocessing followed by the pivot canonicalization above, not as independent structural rules.

The final characterization separates structural optimality from algorithmic attainability. It identifies an incidence-block pattern whose optimum is certified exactly, regardless of whether a particular greedy chronology finds the displayed optimal set.

We first fix the reduced-instance notation used in the certificate. Perform vertex-level dominance suppression by choosing a set $D \subseteq V$ such that every $v \in D$ has a surviving witness $r ( v ) \in V \backslash D$ with

$$
I ( r ( v ) ) \subseteq I ( v ) .
$$

Define the dominance-reduced block family by

$$
\begin{array} { r } { B ^ { - } : = \{ B \setminus D : B \in B , B \setminus D \neq \emptyset \} , } \end{array}
$$

index it as

$$
\begin{array} { r } { B ^ { - } = \{ B _ { i } ^ { - } : i \in \mathcal { I } ^ { - } \} , } \end{array}
$$

and let $I _ { - } ( v ) , d _ { - } ( v )$ , and $w _ { - } ( \cdot )$ denote incidence sets, degrees, and block weights computed inside $B ^ { - }$ . Suppose the block-index set $\mathcal { Z } ^ { - }$ is partitioned into nonempty blocks

$$
\mathcal { I } _ { 1 } , \ldots , \mathcal { I } _ { m } ,
$$

and for each $t \in \{ 1 , \ldots , m \}$ there exists a surviving vertex $z _ { t } \in V \setminus D$ such that

$$
I _ { - } ( z _ { t } ) = \mathcal { T } _ { t } .
$$

Set

$$
Z : = \{ z _ { 1 } , \ldots , z _ { m } \} .
$$

The following certificate says that, after support-superset suppression, these block representatives already form an optimum.

Theorem 4 (Dominance-reduced incidence-block optimality certificate). Assume that every reduced block in the same block has block-size weight:

$$
w _ { - } ( B _ { i } ^ { - } ) = | { \mathcal { I } } _ { t } | \qquad \forall t \in \{ 1 , \dots , m \} , ~ \forall i \in { \mathcal { I } } _ { t } .
$$

Then $Z$ is a maximum feasible set for the reduced family and for the original family. In particular,

$$
\alpha ( B ) = m .\tag{23}
$$

Proof. For every $v \in D$ , the witness $r ( v )$ survives the suppression pass and satisfies $I ( r ( v ) ) \subseteq I ( v )$ Deleting the vertices of D one by one is therefore value-preserving by Theorem 1(i), and empty blocks impose no feasibility constraint. Hence

$$
\alpha ( B ^ { - } ) = \alpha ( B ) .
$$

In $B ^ { - }$ , each $z _ { t }$ has support $\mathcal { I } _ { t }$ . Hence $Z$ is feasible: if $i \in \mathcal { I } _ { t }$ , then $z _ { s } \in B _ { i } ^ { - }$ if $i \in I _ { - } ( z _ { s } ) = \mathcal { T } _ { s }$ ， which happens only for $s = t$ because the blocks form a partition.

Now let O be any feasible set for $B ^ { - }$ . Assign one unit of charge to each $v \in O$ and distribute it evenly over the $d _ { - } ( v )$ reduced blocks containing v. Each block receives charge from at most one retained vertex. If $i \in \mathcal { I } _ { t }$ and $O \cap B _ { i } ^ { - } = \{ v \}$ , then

$$
d _ { - } ( v ) \geq w _ { - } ( B _ { i } ^ { - } ) = | { \mathcal T } _ { t } | ,
$$

so the reduced block receives at most $1 / | \mathcal { T } _ { t } |$ charge. Therefore the total charge received by reduced blocks in block $\mathcal { T } _ { t }$ is at most

$$
\sum _ { i \in \mathcal { I } _ { t } } \frac { 1 } { \vert \mathcal { T } _ { t } \vert } = 1 .
$$

Summing over the $m$ blocks gives $| O | \le m$ . Since Z is feasible in $B ^ { - }$ and $| Z | = m$ , it is maximum in $B ^ { - }$ <sup>−</sup>. The dominance deletion gives $\alpha ( B ) = \alpha ( B ^ { - } ) = m$ . Because $Z \cap D = \emptyset$ , the same set $Z$ is feasible for the original family B as well. □

Example 1(c) (Running example: dominance-reduced incidence blocks). Return to the running example in Example $1 ( \mathrm { a } )$ . First perform the support-superset suppression

$$
D = \{ u , f , g \} ,
$$

because

$$
\begin{array} { l } { I ( a ) = \{ 1 \} \subsetneq I ( u ) = \{ 1 , 2 \} , } \\ { I ( b ) = \{ 2 , 3 \} \subsetneq I ( f ) = \{ 2 , 3 , 9 \} , } \\ { I ( e ) = \{ 4 , 5 \} \subsetneq I ( g ) = \{ 4 , 5 , 1 0 \} . } \end{array}
$$

The reduced family $B ^ { - }$ keeps the same block labels. In $B ^ { - }$ , take the same block partition later used by the greedy trace:

$$
\mathcal { I } _ { 1 } = \{ 1 \} , \qquad \mathcal { I } _ { 2 } = \{ 2 , 3 \} , \qquad \mathcal { I } _ { 3 } = \{ 4 , 5 \} , \qquad \mathcal { I } _ { 4 } = \{ 6 , 7 , 8 \} , \qquad \mathcal { I } _ { 5 } = \{ 9 , 1 0 , 1 1 , 1 2 \} ,
$$

with representatives

$$
z _ { 1 } = a , \qquad z _ { 2 } = b , \qquad z _ { 3 } = e , \qquad z _ { 4 } = c , \qquad z _ { 5 } = r .
$$

The reduced family satisfies the block-size weight condition:

$$
\begin{array} { l } { { w _ { - } ( B _ { 1 } ) = 1 , \qquad w _ { - } ( B _ { 2 } ) = \cdots = w _ { - } ( B _ { 5 } ) = 2 , } } \\ { { w _ { - } ( B _ { 6 } ) = w _ { - } ( B _ { 7 } ) = w _ { - } ( B _ { 8 } ) = 3 , \qquad w _ { - } ( B _ { 9 } ) = \cdots = w _ { - } ( B _ { 1 2 } ) = 4 . } } \end{array}
$$

Thus Theorem 4 certifies

$$
Z = \{ a , b , e , c , r \}
$$

as a maximum feasible set and gives

$$
\alpha ( B ) = 5 .
$$

The unreduced closed-form sum is larger, $U ( B ) = 3 1 / 6$ , because f and $g$ artificially lower the weights of $B _ { 9 }$ and $B _ { 1 0 }$ from 4 to 3. The dominance-reduced certificate removes exactly this distortion. Section 5 shows that the greedy layered clustering algorithm realizes the same suppression intrinsically: the low-weight roots $a , b ,$ e absorb $u , f , g$ before the higher-weight layers are processed.

Except for incidence-twin compression and the weight-1 canonical form, which are used below as preprocessing and first certificate steps, these exact characterization rules are optional tools: they can shrink or partially canonicalize an instance without changing the optimum, but the scalable certificates and algorithms below do not rely on the more expensive dominance-closure preprocessing being applied.

## 4 Bounds and Certificates for Maximum Strong Independent Sets

## 4.1 Closed-Form and Low-Weight Certificates

Exact characterizations are useful when their hypotheses hold; at billion-vertex scale we also need certificates that are cheap to compute on arbitrary block families. With block weight already defined, the first certificate is the closed-form block-weight sum.

Definition 5 (Closed-form upper-bound value). For any block family $\mathcal { F }$ with intrinsic degree function $d \mathscr { F } ^ { ( \cdot ) }$ and induced block weights

$$
w \mathcal { F } ( B ) : = \operatorname* { m i n } _ { v \in B } d \mathcal { F } ( v ) ,\tag{24}
$$

define

$$
U ( \mathcal { F } ) : = \sum _ { B \in \mathcal { F } } \frac { 1 } { w _ { \mathcal { F } } ( B ) } .\tag{25}
$$

In particular, for the ambient family B this is exactly

$$
U ( B ) = \sum _ { B \in B } { \frac { 1 } { w ( B ) } } .\tag{26}
$$

Theorem 5 (Closed-form upper bound). For every feasible retained set $R \subseteq V$

$$
| R | \leq U ( B ) .\tag{27}
$$

Proof. Assign one unit of charge to each retained vertex $v \in R ,$ and distribute it evenly among the $d ( v )$ blocks containing v. Each incident block receives charge $1 / d ( v )$ . Since R is feasible, each block receives charge from at most one retained vertex. If $v \in R \cap B$ , then by definition $w ( B ) \leq d ( v )$ hence $1 / d ( v ) \leq 1 / w ( B )$ . Summing over all blocks, the total received charge is at most $U ( B )$ . On the other hand, each retained vertex contributes total charge 1, so the total charge equals $| R |$ □

The intuitive interpretation is straightforward. A vertex with large $d ( v )$ is globally entangled: choosing it consumes capacity across many blocks. A block containing a degree-1 or degree-2 vertex is therefore structurally sharp and should be resolved early.

Example 1(d) (Running example: closed-form bound). Applying Theorem 5 directly to the unreduced running example gives

$$
U ( \mathcal { B } ) = 1 + 4 \cdot \frac { 1 } { 2 } + 5 \cdot \frac { 1 } { 3 } + 2 \cdot \frac { 1 } { 4 } = \frac { 3 1 } { 6 } .
$$

The dominance-reduced certificate is sharper. The representatives

$$
z _ { 1 } = a , \quad z _ { 2 } = b , \quad z _ { 3 } = e , \quad z _ { 4 } = c , \quad z _ { 5 } = r
$$

have supports

$$
\{ 1 \} , \quad \{ 2 , 3 \} , \quad \{ 4 , 5 \} , \quad \{ 6 , 7 , 8 \} , \quad \{ 9 , 1 0 , 1 1 , 1 2 \} ,
$$

and their dominated support-superset vertices include

$$
u , \ f , \ g \quad { \mathrm { w i t h } } \quad I ( a ) \subsetneq I ( u ) , \ I ( b ) \subsetneq I ( f ) , \ I ( e ) \subsetneq I ( g ) .
$$

After deleting $u , f , g$ , Theorem 4 applies to the reduced family and gives

$$
U ( \mathcal { B } ^ { - } ) = 1 + 4 \cdot \frac { 1 } { 2 } + 3 \cdot \frac { 1 } { 3 } + 4 \cdot \frac { 1 } { 4 } = 5 .
$$

Thus the optimal value is already certified as

$$
\alpha ( B ) = 5 .
$$

Section 5 will return to this same instance and show that the greedy layered clustering algorithm performs the same first-order superset suppression and attains this value exactly.

The canonical optimal form for weight-1 blocks (Theorem 2) yields the following computable preclusion bound. Let

$$
\mathscr { B } _ { 1 } : = \{ B \in \mathscr { B } : w ( B ) = 1 \} , \qquad \mathscr { B } _ { > 1 } : = \mathscr { B } \setminus \mathscr { B } _ { 1 } , \qquad V _ { 1 } : = \bigcup _ { B \in \mathscr { B } _ { 1 } } B .\tag{28}
$$

For each $B \in B _ { > 1 }$ define the residual block

$$
B ^ { \circ } : = B \setminus V _ { 1 } ,\tag{29}
$$

and keep only nonempty residual blocks

$$
\begin{array} { r } { B ^ { \circ } : = \{ B ^ { \circ } : B \in B _ { > 1 } , \ B ^ { \circ } \neq \varnothing \} . } \end{array}\tag{30}
$$

On $B ^ { \circ }$ , residual vertex degrees equal the original degrees of the surviving vertices, so the residual block weights are

$$
w ^ { \circ } ( B ^ { \circ } ) : = \operatorname* { m i n } _ { v \in B ^ { \circ } } d ( v ) ,\tag{31}
$$

and

$$
U ( \mathcal { B } ^ { \circ } ) = \sum _ { B ^ { \circ } \in B ^ { \circ } } \frac { 1 } { w ^ { \circ } ( B ^ { \circ } ) } .\tag{32}
$$

Corollary 2 (Weight-1 preclusion bound). Every feasible retained set R satisfies

$$
| R | \leq | B _ { 1 } | + U ( B ^ { \circ } ) .\tag{33}
$$

Moreover,

$$
\begin{array} { r } { | B _ { 1 } | + U ( B ^ { \circ } ) \le U ( B ) , } \end{array}\tag{34}
$$

so this is never weaker than Theorem 5.

Proof. Split R as $R = ( R \cap V _ { 1 } ) \cup ( R \setminus V _ { 1 } )$ . For the first part, each $x \in R \cap V _ { 1 }$ belongs to at least one block in $\boldsymbol { B } _ { 1 }$ , hence

$$
| R \cap V _ { 1 } | \leq \sum _ { B \in B _ { 1 } } | R \cap B | \leq | B _ { 1 } | .\tag{35}
$$

Now consider $R _ { 0 } : = R \setminus V _ { 1 } \subseteq V \setminus V _ { 1 }$ . For every $B \in B _ { > 1 }$

$$
| R _ { 0 } \cap B ^ { \circ } | = | R _ { 0 } \cap B | \le 1 ,\tag{36}
$$

so $R _ { 0 }$ is feasible for residual blocks $B ^ { \circ }$ . If $v \in V \setminus V _ { 1 }$ , then v is in no block of $\boldsymbol { B } _ { 1 }$ , hence its degree over $B ^ { \circ }$ is exactly $d ( v )$ , and the block-weight theorem applies with weights $w ^ { \circ }$ :

$$
| R _ { 0 } | \le U ( B ^ { \circ } ) .\tag{37}
$$

Adding both parts gives the claimed refined bound.

For the second inequality, $\textstyle \sum _ { B \in B _ { 1 } } 1 / w ( B ) = | B _ { 1 } |$ . Also for each $B ^ { \circ }$ induced from $B \in B _ { > 1 }$ , one has

$$
w ^ { \circ } ( B ^ { \circ } ) = \operatorname* { m i n } _ { v \in B ^ { \circ } } d ( v ) \geq \operatorname* { m i n } _ { v \in B } d ( v ) = w ( B ) ,\tag{38}
$$

thus $1 / w ^ { \circ } ( B ^ { \circ } ) \leq 1 / w ( B )$ , and summing yields the result.

□

The next corollary gives a complementary achievability certificate for residual weight-2 structure. Unlike the weight-1 case, weight 2 alone does not force a canonical root in each block. However, the degree-2 vertices define an ordinary graph-matching problem over block incidences in the sense of Edmonds’ blossom algorithm [9]. The size of such a matching is always a feasible retained count, and it certifies optimality exactly when it saturates the integer closed-form bound.

For this certificate, let C be a residual block family on active universe

$$
V _ { \cal C } : = \bigcup _ { B \in { \cal C } } B ,
$$

with intrinsic degrees $d c ( \cdot )$ and intrinsic block weights $w c ( \cdot )$ . Construct a graph $G _ { 2 } ( \mathcal { C } )$ whose vertices are the blocks in C and whose edges are the degree-2 vertices: for each vertex v with $d \boldsymbol { c } ( \boldsymbol { v } ) = 2$ and incident blocks $B _ { i } , B _ { j } \in \mathcal { C }$ , add an edge $( B _ { i } , B _ { j } )$ labeled by v. Assume

$$
w c ( B ) = 2 \qquad \forall B \in { \mathcal { C } } ,
$$

and let M be any matching in $G _ { 2 } ( \mathcal { C } )$

Corollary 3 (Saturated weight-2 matching certificate). The vertices labeling the matched edges form a feasible retained set $f o r { \mathcal C }$ , and

$$
| M | \leq \alpha ( { \mathcal { C } } ) \leq \left\lfloor { \frac { | { \mathcal { C } } | } { 2 } } \right\rfloor = \lfloor U ( { \mathcal { C } } ) \rfloor .\tag{39}
$$

Consequently, $i f \left| M \right| = \left\lfloor { c } \right\rceil / 2 \mathrm { \Delta }$ , then the matched vertices form an optimal retained set $f o r { \mathcal C }$

Proof. Since every block in C has intrinsic weight 2, Theorem 5 applied to C gives

$$
\alpha ( \mathcal { C } ) \leq U ( \mathcal { C } ) = \sum _ { B \in \mathcal { C } } \frac { 1 } { 2 } = \frac { | \mathcal { C } | } { 2 } .
$$

The left side is an integer, so

$$
\alpha ( { \mathcal { C } } ) \leq \left\lfloor { \frac { | { \mathcal { C } } | } { 2 } } \right\rfloor .
$$

Now retain the vertex labeling each matched edge of M. Because M is a matching, no two selected degree-2 vertices share an incident block. Hence every block in C contains at most one selected vertex, so the selected vertices are feasible. This gives $| M | \leq \alpha ( \mathcal { C } )$ . If $| M | = \lfloor | { \mathcal { C } } | / 2 \rfloor$ , the feasible matching construction attains the integer upper bound and is therefore optimal. □

As a minimal saturated example, take four blocks arranged in a cycle by degree-2 vertices:

$$
C _ { 1 } = \{ x _ { 1 2 } , x _ { 4 1 } \} , \quad C _ { 2 } = \{ x _ { 1 2 } , x _ { 2 3 } \} , \quad C _ { 3 } = \{ x _ { 2 3 } , x _ { 3 4 } \} , \quad C _ { 4 } = \{ x _ { 3 4 } , x _ { 4 1 } \} .
$$

Every block has intrinsic weight 2, so $U ( { \mathcal { C } } ) = 2$ . The matching $\{ ( C _ { 1 } , C _ { 2 } ) , ( C _ { 3 } , C _ { 4 } ) \}$ , labeled by $x _ { 1 2 }$ and $x _ { 3 4 }$ , saturates the bound and certifies that $\{ x _ { 1 2 } , x _ { 3 4 } \}$ is optimal.

This certificate is deliberately only suficient. It can hold in the presence of higher-degree vertices, but it certifies optimality only when the degree-2 layer already saturates the integer block-weight bound. It is therefore not a weight-2 analogue of Corollary 2: there is no unconditional weight-2 preclusion pass, no automatic additive term to peel of, and no guaranteed bound improvement from the mere fact that a block has weight 2. Rather, Corollary 3 is an achievability check showing that, in the special saturated-matching case, the closed-form block-weight bound is tight.

The closed-form and weight-1 certificates above are evaluated on a fixed active block family. The next subsection treats them as reusable primitives inside a residual search: support-superset sharpening and iterative puncturing repeatedly modify the active family, recompute intrinsic weights, and keep the smallest resulting valid upper bound $U _ { \mathrm { p u n c t } }$

## 4.2 Layered Superset Sharpening and Puncturing

The cost discussion after Theorem 1 explains why we do not run full exact superset suppression as a global preprocessing pass. The next algorithm uses the same dominance principle more selectively: it looks only at supports revealed by current block-weight vertices, deletes the higher-degree supportsuperset vertices certified by those witnesses, and uses the resulting weight increases to sharpen the closed-form certificate. We abbreviate this procedure as LSS in the empirical tables.

Algorithm 1 (Layered Superset Sharpening). Given an active block family $\mathcal { F } = \{ F _ { 1 } , \ldots , F _ { m } \}$ whose closed-form certificate will be evaluated after the surrounding routine has exhausted its weight-1 preclusion step, perform the following pass.

1. First perform incidence-twin compression on the remaining active family, then compute vertex degrees

$$
d _ { \mathcal { F } } ( v ) = | \{ i : v \in F _ { i } \} |
$$

and block weights $\begin{array} { r } { w _ { \mathcal { F } } ( F _ { i } ) = \operatorname* { m i n } _ { v \in F _ { i } } d _ { \mathcal { F } } ( v ) } \end{array}$

2. Process block-weight layers from smallest to largest. In layer $w ,$ , build a fresh local incidence map

$$
J _ { w } ( v ) : = \{ i : v \in F _ { i } , \ w _ { \mathscr { F } } ( F _ { i } ) = w \} .
$$

Maintain the accumulated support

$$
P ( v ) : = \bigcup _ { \lambda \leq w } J _ { \lambda } ( v )
$$

over layers already scanned.

3. Whenever a vertex a satisfies

$$
| P ( a ) | = d \mathcal { F } ( a ) = w ,
$$

its full live support $I _ { \mathcal { F } } ( a ) = P ( a )$ has been revealed exactly at its own block-weight layer. Such a vertex is a block-weight witness for that support.

4. Intersect the blocks in this revealed support,

$$
C ( a ) : = \bigcap _ { i \in P ( a ) } F _ { i } ,
$$

and delete every active $b \in C ( a )$ with $d _ { \mathcal { F } } ( b ) > w$ from the residual certificate family. Exact identity/equal-support cases are handled by the incidence-twin compression step at the start of the pass.

5. After a support $P ( a )$ has been processed, remove a from the accumulated map to save memory. After a nonempty deletion pass, rebuild incidence twins, recompute degrees and block weights, and repeat until no block-weight witness deletes a vertex.

For the next result and its cost discussion, let

$$
I _ { \mathcal { F } } : = \sum _ { F \in \mathcal { F } } | F |
$$

denote the active incidence size for one sharpening pass, and let $T _ { \cap }$ denote the total cost of the block intersections performed for newly revealed supports. Let $\mathcal { F } ^ { \sharp }$ denote the terminal active family after Algorithm 1 stops. For a single nonempty deletion pass, write ${ \mathcal { F } } ^ { \prime }$ for the family obtained after deleting the certified support-superset vertices and discarding any empty blocks.

Proposition 1 (Correctness and fixed point of layered superset sharpening). Every support-superset deletion made by Algorithm 1 preserves $\alpha ( \mathcal { F } )$ . Moreover, $i f { \mathcal { F } } ^ { \prime }$ is obtained from $\mathcal { F }$ by one nonempty deletion pass, then

$$
\sum _ { F ^ { \prime } \in \mathcal { F } ^ { \prime } } \frac { 1 } { w _ { \mathcal { F } ^ { \prime } } ( F ^ { \prime } ) } \le \sum _ { F \in \mathcal { F } } \frac { 1 } { w _ { \mathcal { F } } ( F ) } .
$$

In $\mathcal { F } ^ { \sharp }$ , no active block F, block-weight witness $a \in F .$ , and active vertex b satisfy

$$
d _ { \mathcal { F } ^ { \sharp } } ( a ) = w _ { \mathcal { F } ^ { \sharp } } ( F ) , \qquad I _ { \mathcal { F } ^ { \sharp } } ( a ) \subsetneq I _ { \mathcal { F } ^ { \sharp } } ( b ) .
$$

Proof. Consider a deletion made when the support of a block-weight witness a is revealed in layer w. The algorithm has

$$
d _ { \mathcal { F } } ( a ) = | P ( a ) | = w , \qquad P ( a ) = I _ { \mathcal { F } } ( a ) ,
$$

and it deletes only vertices b in

$$
C ( a ) = \bigcap _ { i \in P ( a ) } F _ { i } .
$$

Therefore b belongs to every block containing $^ { a , }$ so $I _ { \mathcal { F } } ( a ) \subseteq I _ { \mathcal { F } } ( b )$ . Since the algorithm deletes only b with $d _ { \mathcal { F } } ( b ) > w = d _ { \mathcal { F } } ( a )$ , the containment is strict, and Theorem 1(i) permits deleting b without changing $\alpha ( \mathcal { F } )$ . Repeating the argument over all deletions and rebuild passes proves preservation of the optimum by induction. Equal-support vertices are already handled by the incidence-twin quotient before each pass.

Removing vertices from blocks cannot decrease the degree of any surviving vertex and can only increase, or leave unchanged, the minimum surviving degree in each nonempty block. Hence each surviving block term is weakly decreased when recomputed in ${ \mathcal { F } } ^ { \prime }$ ; empty blocks contribute nothing. Thus the displayed closed-form sum weakly decreases.

It remains to prove the stated fixed-point property. Let $\mathcal { F } ^ { \sharp }$ be the active family after the final rebuild, incidence-twin compression, and sharpening pass. Suppose for contradiction that an active block $F$ , a block-weight witness $a \in F$ , and an active vertex b satisfy

$$
a \in F , \qquad d _ { \mathcal { F } ^ { \sharp } } ( a ) = w _ { \mathcal { F } ^ { \sharp } } ( F ) , \qquad I _ { \mathcal { F } ^ { \sharp } } ( a ) \subsetneq I _ { \mathcal { F } ^ { \sharp } } ( b ) .
$$

When the final pass reaches layer $w _ { \mathcal { F } ^ { \sharp } } ( F ) = d _ { \mathcal { F } ^ { \sharp } } ( a )$ , the full support of a is revealed, and b belongs to every block in that support. Hence $b \in C ( a )$ and $d _ { \mathcal { F } ^ { \sharp } } ( b ) > d _ { \mathcal { F } ^ { \sharp } } ( a )$ , so the pass would delete b. This contradicts termination.

Equivalently, no higher-degree support superset remains for the block-weight supports inspected by the pass. This is a certificate-sharpening property, not a full dominance closure over all vertices: support containments whose smaller support is not a current block-weight witness may remain because they do not afect the first-order closed-form certificate in the same way.

The implementation cost is also local. A pass first scans all active incidences to compute degrees and block weights, then scans each block once in its layer to update the accumulated supports $P ( \cdot )$ ; this contributes $O ( I _ { \mathcal { F } } )$ . For every newly revealed support, the algorithm intersects its incident blocks, contributing total work $T _ { \cap }$ across the pass. Applying a nonempty deletion set requires one additional linear scan of the active block lists. Thus one pass costs $O ( I _ { \mathcal { F } } + T _ { \cap } )$ , with peak auxiliary memory given by the accumulated not-yet-fully-revealed support frontier plus temporary intersection sets.

By contrast, a brute-force dominance oracle would materialize full supports for all vertices and test $I ( a ) \subseteq I ( b )$ over many vertex pairs, costing $\Omega ( | V | ^ { 2 } )$ support comparisons in the worst case, even before accounting for support lengths. Algorithm 1 avoids this global pair table: it only performs intersections for supports as they become fully revealed by the layer order.

On the running example, the preliminary weight-1 preclusion removes the block $B _ { 1 }$ and clusters away u with the root a. Algorithm 1 then starts on the residual family. In layer 2, the full supports of the block-weight witnesses b and e are revealed:

$$
I ( b ) = \{ 2 , 3 \} , \qquad I ( e ) = \{ 4 , 5 \} .
$$

The intersections $B _ { 2 } \cap B _ { 3 }$ and ${ B _ { 4 } } \cap { B } _ { 5 }$ contain the higher-degree vertices $f$ and $^ { g , }$ respectively. Hence the pass deletes f and $^ { g , }$ raising the residual weights of $B _ { 9 }$ and $B _ { 1 0 }$ from their apparent weight 3 to their reduced weight 4. This is the intended role of the algorithm: it sharpens the certificate where block-weight witnesses expose a support-superset distractor, without attempting a global dominance closure.

Beyond the canonical weight-1 layer, the closed-form bound can be further sharpened by puncturing the block family: delete selected blocks, recompute the incidence weights on the residual family, and keep the smallest valid certificate encountered. Puncturing does not require every surviving block to keep its original weight. The validity comes from monotonicity of the feasible region under deletion, while the tightening comes from searching for a residual family whose recomputed certificate is smaller.

For the certificate below, consider a residual instance obtained from B by deleting blocks and by applying weight-1 preclusion to fix $q$ roots and remove their clustered vertices from all remaining blocks. Let C be the remaining nonempty block family, and assume every still-active vertex is incident to at least one block of C. Its intrinsic degrees, block weights, and closed-form value are

$$
d _ { \mathcal { C } } ( v ) : = | \{ B \in \mathcal { C } : v \in B \} | , \qquad w _ { \mathcal { C } } ( B ) : = \operatorname* { m i n } _ { v \in B } d _ { \mathcal { C } } ( v ) ,\tag{40}
$$

and

$$
U ( \mathcal { C } ) : = \sum _ { B \in \mathcal { C } } \frac { 1 } { w _ { \mathcal { C } } ( B ) } .\tag{41}
$$

If the residual instance is additionally processed by repeated weight-1 preclusion, write $\mathcal { C } ^ { \circ }$ for the final nonempty residual family.

Proposition 2 (Punctured-family upper-bound certificate).

$$
\alpha ( \mathcal { B } ) \leq q + \alpha ( \mathcal { C } ) \leq q + U ( \mathcal { C } ) .\tag{42}
$$

In particular,

$$
\alpha ( B ) \leq q + U ( { \mathcal { C } } ^ { \circ } ) .\tag{43}
$$

Proof. Each weight-1 preclusion step charges at most one retained vertex to the precluded block, so all precluded parts contribute at most $q$ retained vertices. After these charged vertices are removed, deleting blocks only relaxes the remaining feasibility constraints. Hence the uncharged part of any feasible retained set for the original family is feasible for the residual family C on its active universe, giving

$$
\alpha ( B ) \leq q + \alpha ( \mathcal { C } ) .
$$

Because every still-active vertex is incident to at least one block of ${ \mathcal { C } } ,$ , Theorem 5 applies to the residual instance with degrees and weights computed inside ${ \mathcal { C } } ,$ yielding $\alpha ( { \mathcal { C } } ) \leq U ( { \mathcal { C } } )$ . The final statement is the same argument applied after repeated weight-1 preclusion. □

Proposition 2 turns bound sharpening into a certificate-search problem. The next algorithm is the practical iterative search used in our implementation. It processes current block-weight layers from high to low and, within each layer, dynamically chooses the block with the smallest current minimum-degree count. It accepts punctures that do not increase the current certificate, including zero-gain punctures that expose new weight-1 blocks; those blocks are precluded at the next rebuild and can unlock a strictly smaller certificate. The returned value is the iterative puncturing bound, denoted $U _ { \mathrm { p u n c t } }$ , namely the smallest valid certificate recorded over all outer rebuild-and-puncture rounds.

Algorithm 2 (Iterative Greedy Layered Puncturing Algorithm). Initialize the live family $c \gets B$ the fixed weight-1 contribution $q  0$ , and the best iterative certificate $U _ { \mathrm { p u n c t } }  U ( B )$ . For a current residual family C, write

$$
n _ { \operatorname* { m i n } , \mathcal { C } } ( B ) : = | \{ v \in B : d _ { \mathcal { C } } ( v ) = w _ { \mathcal { C } } ( B ) \} |
$$

for the number of minimum-degree vertices in block B.

Repeat the following outer rounds until no block is punctured in a full round.

1. Rebuild, preclude weight-1 blocks, and sharpen the certificate. Recompute $d c ( \cdot )$ and $w c ( \cdot )$ , compress incidence twins, and while there is a block $B \in { \mathcal { C } }$ with $w c ( B ) = 1$ , choose a degree-1 root in B, add one to q, remove the vertices of B from all live blocks, delete empty blocks, and rebuild the afected degrees and weights. After the current weight-1 wave is exhausted, apply Algorithm 1 to the residual certificate family. If sharpening deletes vertices, rebuild incidence twins, degrees, and block weights before recording the certificate. Weight-1 blocks are not newly created by this vertex-deletion pass; they are exposed by block punctures and handled at the next rebuild.

2. Record the current certificate. Update

$$
U _ { \mathrm { p u n c t } }  \operatorname* { m i n } \{ U _ { \mathrm { p u n c t } } , \ q + \sum _ { B \in \mathcal { C } } \frac { 1 } { w c ( B ) } \} .\tag{44}
$$

If ${ \mathcal { C } } = \emptyset , { \mathrm { s t o p . } }$

3. Process layers from high to low. Maintain a global vertex-to-live-block index and current block weights for the residual family. For current weights w in decreasing order, initialize the active layer

$$
\mathcal { L } _ { w } : = \{ B \in \mathcal { C } : w _ { \mathcal { C } } ( B ) = w \}
$$

with scores $n _ { \mathrm { m i n } , \mathcal { C } } ( B )$ . Repeatedly choose a block $B \in \mathcal { L } _ { w }$ with minimum current $n _ { \mathrm { m i n } , \mathcal { C } } ( B )$ breaking ties arbitrarily. Use the global index to identify every same- or higher-weight block whose weight may change after puncturing B, and compute the exact local certificate change

$$
\Delta ( B ) : = \left( q + \sum _ { C \in { \mathcal { C } } } { \frac { 1 } { w c ( C ) } } \right) - \left( q + \sum _ { C \in { \mathcal { C } } \backslash \{ B \} } { \frac { 1 } { w _ { { \mathcal { C } } \backslash \{ B \} } ( C ) } } \right) ,\tag{45}
$$

updating only blocks incident to vertices of B.

4. Puncture non-worsening or weight-1-enabling blocks. Delete B if $\Delta ( B ) \geq 0$ , or if deleting B creates a new weight-1 block that will be precluded in the next rebuild. After a deletion, update the afected vertex degrees, block weights, and active-layer scores through the global index; if a block’s weight decreases below w, move it to the corresponding later layer. At the end of each layer, discard index entries for vertices that cannot afect any future processed layer.

Output the iterative puncturing bound $U _ { \mathrm { p u n c t } }$

Remark 3 (Layered puncturing intuition). A puncture may lower the weight of some surviving blocks, so it is not justified by preserving the old closed-form certificate. Instead, it is justified by recomputing the certificate on the residual family and retaining the best value seen. The nonworsening condition handles immediately useful punctures, while the weight-1-enabling condition permits zero-gain moves that expose canonical roots after the next rebuild. Empirically, these zero-gain moves are crucial: they often create the next wave of weight-1 preclusions and therefore produce a much tighter final certificate.

Corollary 4 (Validity and termination of the iterative puncturing bound). The iterative puncturing bound $U _ { \mathrm { p u n c t } }$ returned by Algorithm 2 is a valid upper bound:

$$
\alpha ( B ) \leq U _ { \mathrm { p u n c t } } .\tag{46}
$$

Moreover, the algorithm terminates after finitely many punctures and preclusion steps.

Proof. Every certificate recorded by the algorithm has the form $q + U ( \mathcal { C } ^ { \circ } )$ after puncturing, repeated incidence-twin compression, weight-1 preclusion, and layered superset sharpening on a residual family. Proposition 2, together with Theorem 1 and Proposition 1, shows that each such quantity upper-bounds $\alpha ( \boldsymbol { B } )$ . The output is the minimum over recorded valid certificates and is therefore valid. Termination follows because every accepted puncture deletes at least one live block, every weight-1 preclusion removes at least one nonempty block and its clustered vertices from the residual family, every nontrivial incidence-twin or superset-sharpening pass deletes at least one live vertex from the residual certificate family, and the initial incidence family is finite. □

Example 3 (Two-round puncturing cascade). Let the vertices be $x _ { 1 } , x _ { 2 } , x _ { 3 } , x _ { 4 } , y _ { 1 } , y _ { 2 } , y _ { 3 } , y _ { 4 }$ , and define

$$
\begin{array} { l l l l } { { A _ { 1 } = \{ x _ { 1 } , x _ { 2 } \} , } } & { { A _ { 2 } = \{ x _ { 3 } , x _ { 4 } \} , } } & { { A _ { 3 } = \{ x _ { 1 } , x _ { 3 } \} , } } & { { A _ { 4 } = \{ x _ { 2 } , x _ { 4 } \} , } } \\ { { L _ { 1 } = \{ x _ { 1 } , y _ { 1 } , y _ { 2 } \} , } } & { { L _ { 2 } = \{ x _ { 3 } , y _ { 3 } , y _ { 4 } \} , } } & { { L _ { 3 } = \{ x _ { 1 } , x _ { 3 } , y _ { 1 } , y _ { 3 } \} , } } & { { L _ { 4 } = \{ x _ { 2 } , x _ { 4 } , y _ { 2 } , y _ { 4 } \} . } } \end{array}
$$

The degrees are

$$
d ( x _ { 1 } ) = d ( x _ { 3 } ) = 4 , \qquad d ( x _ { 2 } ) = d ( x _ { 4 } ) = 3 , \qquad d ( y _ { i } ) = 2 \quad ( i = 1 , \ldots , 4 ) ,
$$

so

$$
w ( A _ { 3 } ) = 4 , \qquad w ( A _ { 1 } ) = w ( A _ { 2 } ) = w ( A _ { 4 } ) = 3 , \qquad w ( L _ { i } ) = 2 \quad ( i = 1 , \ldots , 4 ) ,
$$

and the initial closed-form certificate is

$$
U ( B ) = \frac { 1 } { 4 } + \frac { 3 } { 3 } + \frac { 4 } { 2 } = \frac { 1 3 } { 4 } .
$$

One admissible high-to-low puncturing trace first removes $A _ { 3 }$ , then $A _ { 1 }$ and $A _ { 2 }$ , each of which decreases the current recomputed certificate. The dynamic increasing- $\cdot n _ { \mathrm { m i n } }$ order is visible along the way: $A _ { 3 }$ is the only block in layer $w = 4 ;$ after puncturing $A _ { 3 }$ , the layer $w = 3$ contains $A _ { 1 } , A _ { 2 } , A _ { 4 }$ with tied or updated minimum-degree counts, and after one of $A _ { 1 } , A _ { 2 }$ is punctured the other becomes the only remaining block in that highest layer.

The residual family is then

$$
A _ { 4 } , L _ { 1 } , L _ { 2 } , L _ { 3 } , L _ { 4 } ,
$$

with current certificate $5 / 2$ . At this point all live blocks have weight 2, but their current minimumdegree counts are

$$
n _ { \mathrm { m i n } } ( A _ { 4 } ) = 2 , \qquad n _ { \mathrm { m i n } } ( L _ { 1 } ) = n _ { \mathrm { m i n } } ( L _ { 2 } ) = 3 , \qquad n _ { \mathrm { m i n } } ( L _ { 3 } ) = n _ { \mathrm { m i n } } ( L _ { 4 } ) = 4 .
$$

Thus the increasing- ${ \cdot n } _ { \mathrm { m i n } }$ rule selects $A _ { 4 }$ before the larger L-blocks. Puncturing $A _ { 4 }$ is a zero-gain move, but it creates weight-1 structure in the latent L-blocks. Algorithm 2 handles such newly exposed weight-1 blocks at the next outer rebuild, so the current layer can still continue its puncturing pass.

Continuing the same dynamically updated order, $L _ { 1 }$ and $L _ { 2 }$ are the smallest remaining weight-2 blocks by current $n _ { \mathrm { { m i n } } } .$ so they are punctured before the larger blocks $L _ { 3 } , L _ { 4 }$ . This leaves only

$$
L _ { 3 } = \{ x _ { 1 } , x _ { 3 } , y _ { 1 } , y _ { 3 } \} , \qquad L _ { 4 } = \{ x _ { 2 } , x _ { 4 } , y _ { 2 } , y _ { 4 } \} ,
$$

both of residual weight 1. The next rebuild therefore precludes $L _ { 3 }$ and $L _ { 4 } ,$ , fixes two canonical units, leaves the residual family empty, and records

$$
U _ { \mathrm { p u n c t } } = 2 .
$$

The example isolates why Algorithm 2 is an outer-iteration procedure rather than a one-shot deletion rule: some useful punctures do not improve the certificate at the moment they are made, but the dynamic increasing-n order finds small exposure blocks first, these punctures expose the next wave of weight-1 blocks, and the certificate tightens only after the subsequent rebuild.

## 4.3 Covering Certificates

The block-weight bound, its weight-1 preclusion form, and the puncturing certificate provide instance-level upper bounds. They also identify low-w(B) blocks as the most constraining parts of the instance, motivating both the covering certificate below and the greedy clustering schedule in the next section.

Example 4 (A loose weight-2 block-weight bound). Let $x _ { 1 } , \ldots , x _ { 2 n }$ be vertices with $n \geq 3$ , and define pair blocks

$$
P _ { t } : = \{ x _ { 2 t - 1 } , x _ { 2 t } \} \quad ( t = 1 , \ldots , n ) ,
$$

together with the two parity blocks

$$
O : = \{ x _ { 1 } , x _ { 3 } , . . . , x _ { 2 n - 1 } \} , \qquad E : = \{ x _ { 2 } , x _ { 4 } , . . . , x _ { 2 n } \} .
$$

Then every vertex has degree 2, and every block has minimum weight $w = 2$ . Hence Theorem 5 gives

$$
\alpha ( B ) \leq { \frac { n + 2 } { 2 } } .
$$

However, $\alpha ( B ) = 2$ . Indeed, one may retain two vertices, for example $\{ x _ { 1 } , x _ { 4 } \}$ . On the other hand, the parity blocks force any feasible set to contain at most one odd-indexed vertex and at most one even-indexed vertex. This family shows that the closed-form bound can remain loose even when all blocks have uniform weight 2 and no larger $\ ` \mathrm { a l l } \mathrm { - } x \ '$ block is present. Example 5 later traces this same family through puncturing, covering, and greedy clustering.

Theorem 5 provides the foundational ordering principle for the two greedy procedures that follow. It shows that a block’s contribution toward the maximum strong independent set size is governed primarily by the inverse of its minimum weight, so lower-weight blocks are the most constraining part of the instance and should be handled first.

The Greedy Layered Covering Algorithm below and the Greedy Layered Clustering Algorithm in Section 5 are complementary realizations of this same principle: both prioritize increasing block weight, and both refine ties by favoring blocks containing more vertices of that current minimum weight. The weight-2 example above then explains why a second, covering-based viewpoint is useful in addition to the closed-form charging bound of Theorem 5: concentrated overlap can force many nominal low-weight candidates into a small number of incompatibility blocks.

Algorithm 3 (Greedy Layered Covering Algorithm and iteration). Initialize a cumulative covered set $\mathcal { Z } _ { 0 } : = \emptyset$ , a residual block family

$$
{ \mathcal { F } } ^ { ( 0 ) } : = \{ B \in { \mathcal { B } } : B \neq \emptyset \} ,
$$

where residual blocks retain their original block indices, and a counter $N _ { \mathrm { c o v } } : = 0$ . Degrees are recomputed intrinsically on the current residual family:

$$
d _ { t } ( v ) : = | \{ B \in \mathcal { F } ^ { ( t ) } : v \in B \} | .
$$

The Greedy Layered Covering Algorithm repeatedly performs the following:

1. If $\mathcal { F } ^ { ( t ) } = \emptyset$ , stop and return the selected covering family

$$
\begin{array} { r } { \boldsymbol { B } ^ { \star } : = \{ B _ { 0 } ^ { \star } , \ldots , B _ { N _ { \mathrm { c o v } } - 1 } ^ { \star } \} } \end{array}
$$

and the final counter $N _ { \mathrm { c o v } } = | B ^ { \star } |$

## 2. Compute current block weights

$$
w _ { t } ( B ) : = \operatorname* { m i n } _ { v \in B } d _ { t } ( v ) ,
$$

the current minimum layer

$$
\lambda _ { t } : = \operatorname* { m i n } _ { B \in \mathcal { F } ^ { ( t ) } } w _ { t } ( B ) , \qquad \mathcal { M } _ { t } : = \{ B \in \mathcal { F } ^ { ( t ) } : w _ { t } ( B ) = \lambda _ { t } \} ,
$$

and, for each current degree-λ vertex, the layer-local vertex-to-block index

$$
\mathcal { T } _ { \lambda _ { t } } ( v ) : = \{ i : \ : B _ { i } \in \mathcal { M } _ { t } , \ : v \in B _ { i } , \ : d _ { t } ( v ) = \lambda _ { t } \} ,
$$

together with, for each $B \in \mathcal { M } _ { t }$ , the number of current minimum-weight vertices

$$
n _ { t } ( B ) : = | \{ v \in B : d _ { t } ( v ) = \lambda _ { t } \} | .
$$

## 3. Select a covering block

$$
B _ { t } ^ { \star } \in \arg \operatorname* { m a x } _ { B \in \mathcal { M } _ { t } } n _ { t } ( B ) ,
$$

breaking ties arbitrarily.

4. Update

$$
\mathcal { Z } _ { t + 1 } : = \mathcal { Z } _ { t } \cup B _ { t } ^ { \star } , \qquad N _ { \mathrm { { c o v } } }  N _ { \mathrm { { c o v } } } + 1 ,
$$

and update the residual family by removing the cumulative covered set from every original block:

$$
\mathcal { F } ^ { ( t + 1 ) } : = \{ B \setminus \mathcal { Z } _ { t + 1 } : B \in \mathcal { B } , \ B \setminus \mathcal { Z } _ { t + 1 } \neq \emptyset \} .\tag{47}
$$

The iterated covering version used in the numerical tables sets $\boldsymbol { B } ^ { ( 0 ) } : = \boldsymbol { B }$ and repeats this covering pass on the current family. At iteration $s + 1$ , it runs the pass on $B ^ { ( s ) }$ , records the returned family as $B ^ { ( s + 1 ) }$ , records

$$
N _ { \mathrm { c o v } } ^ { ( s + 1 ) } : = | \mathcal { B } ^ { ( s + 1 ) } | , \qquad U _ { \mathrm { c o v e r } } ^ { ( s + 1 ) } : = U ( \mathcal { B } ^ { ( s + 1 ) } ) ,
$$

and stops when $B ^ { ( s + 1 ) } = B ^ { ( s ) }$ . Thus each reported $U _ { \mathrm { c o v e r } }$ is the final restricted block-weight certificate at convergence, while the associated iteration count is the first $s + 1$ for which the covering family is unchanged.

Algorithm 3 deliberately uses the opposite layer priority from Algorithm 2. Puncturing starts from the highest residual block weights and, inside a layer, chooses the smallest current $n _ { \mathrm { m i n } , \mathcal { C } } ( B )$ because such blocks are the safest candidates to remove without worsening the certificate. Covering starts from the lowest residual block weight and chooses the largest current $n _ { t } ( B )$ because such blocks cover the most active minimum-degree vertices in the bottleneck layer.

Remark 4 (Two-pass implementation within a fixed covering layer). For implementation, Step 3 of Algorithm 3 need not recompute $n _ { t } ( B )$ over all residual blocks after every covering choice. Once a fixed layer $w = \lambda _ { t }$ is reached, one may perform two passes, using $d \boldsymbol { \mathscr { F } }$ for the current residual-family degree. In the first pass, scan all current weight-w blocks, compute and store their counts

$$
\begin{array} { r } { n ( B ) : = | \{ v \in B : d _ { \mathcal { F } } ( v ) = w \} | , } \end{array}
$$

and build the layer-local degree-w vertex-to-block index $\mathcal { T } _ { w } ( \cdot )$

In the second pass, repeatedly choose an unprocessed block with maximum current $n ( B )$ , add all of its vertices to the covered set, and for each of its degree-w vertices $v ,$ decrement by one the stored count $n ( \cdot )$ of every still-unprocessed associated block in $\mathcal { T } _ { w } ( v )$ . If the count of an unprocessed block drops to zero, then all of its degree-w vertices have been covered, so its residual weight has increased beyond $w ;$ it may therefore be removed from the current layer and appended to a higher-layer work list, for example the next layer queue.

This two-pass realization uses temporary storage for $\mathcal { T } _ { w } ( \cdot )$ , but it updates only the blocks actually touched by the newly covered degree-w vertices and therefore avoids rescanning the entire residual layer after each covering selection.

Theorem 6 (Greedy covering upper bound). Algorithm $\mathcal { B }$ returns an upper bound on the maximum feasible retention value:

$$
\alpha ( B ) \leq N _ { \mathrm { c o v } } .\tag{48}
$$

Proof. Let the algorithm select blocks

$$
B _ { 0 } ^ { \star } , B _ { 1 } ^ { \star } , \ldots , B _ { N _ { \mathrm { c o v } } - 1 } ^ { \star } .
$$

At termination, the residual family is empty. Following the residual updates shows that every original block $B \in B$ satisfies

$$
B \setminus \mathcal { Z } _ { N _ { \mathrm { c o v } } } = \emptyset ,
$$

hence

$$
B \subseteq \mathcal { Z } _ { N _ { \mathrm { c o v } } } = \bigcup _ { t = 0 } ^ { N _ { \mathrm { c o v } } - 1 } B _ { t } ^ { \star } .
$$

Therefore every retained root belongs to at least one selected covering block. Assign each retained root to the earliest selected block containing it. This assignment is injective, because each selected

block may contain at most one retained root by feasibility. Hence the number of retained roots is at most the number of selected covering blocks:

$$
\alpha ( B ) \leq N _ { \mathrm { c o v } } .
$$

The next lemma separates two roles of a restricted block family that preserves the active vertex universe: dropping blocks relaxes feasibility, while under a stronger pairwise-cover condition the optimum is preserved exactly. Throughout the lemma, let $B ^ { \star } \subseteq B$ satisfy

$$
{ \underset { B \in B ^ { \star } } { \bigcup } } B = \bigcup _ { B \in B } B .
$$

Lemma 1 (Subfamily monotonicity and equality under pairwise covering). Under this shareduniverse assumption:

(i) One always has

$$
\alpha ( B ) \leq \alpha ( B ^ { \star } ) .
$$

(ii) $H ,$ in addition, for every omitted block $B \in B \setminus B ^ { \star }$ and every distinct $x , y \in B$ , there exists $C \in B ^ { \star }$ such that

$$
\{ x , y \} \subseteq C ,
$$

then

$$
\alpha ( B ) = \alpha ( B ^ { \star } ) .
$$

Proof. For part (i), the two families have the same active universe, and every B-feasible set is automatically $B ^ { \star }$ -feasible because $B ^ { \star }$ is a subfamily. Hence $\alpha ( B ) \leq \alpha ( B ^ { \star } )$

For part (ii), let R be any $B ^ { \star } .$ -feasible set. If some omitted block $B \in B \backslash B ^ { \star }$ contained two retained vertices $x , y \in R$ , then by assumption there would exist $C \in B ^ { \star }$ with $\{ x , y \} \subseteq C .$ , contradicting feasibility of $R$ for $B ^ { \star }$ . Hence every $B ^ { \star }$ -feasible set is also B-feasible, so $\alpha ( B ^ { \star } ) \leq \alpha ( B )$ . Together with part (i), this gives equality. □

Remark 5 (Illustration of subfamily monotonicity). Take

$$
\mathcal { B } = \Big \{ B _ { 0 } = \{ a , b , c \} , ~ B _ { 1 } = \{ a , b , d \} , ~ B _ { 2 } = \{ a , c , e \} , ~ B _ { 3 } = \{ b , c , f \} \Big \} ,
$$

and let

$$
B ^ { \star } = \{ B _ { 1 } , B _ { 2 } , B _ { 3 } \} .
$$

The omitted block $B _ { 0 }$ is not itself in $B ^ { \star }$ , but every pair in $B _ { 0 }$ is covered inside $B ^ { \star }$ :

$$
\{ a , b \} \subseteq B _ { 1 } , \qquad \{ a , c \} \subseteq B _ { 2 } , \qquad \{ b , c \} \subseteq B _ { 3 } .
$$

Hence Lemma 1(ii) applies and gives

$$
\alpha ( B ) = \alpha ( B ^ { \star } ) .
$$

In this example the common optimum is 3, achieved for instance by the feasible set $\{ d , e , f \}$ . The point is that all pairwise conflicts inside $B _ { 0 }$ are already enforced by the selected subfamily $B ^ { \star }$

By contrast, plain covering alone does not force equality. Consider

$$
\begin{array} { r } { \widetilde { \mathcal { B } } = \left\{ \widetilde { B } _ { 0 } = \{ a , b , c \} , \widetilde { B } _ { 1 } = \{ a , b , d \} , \widetilde { B } _ { 2 } = \{ a , c , d \} \right\} , \qquad \widetilde { \mathcal { B } } ^ { \star } = \{ \widetilde { B } _ { 1 } , \widetilde { B } _ { 2 } \} . } \end{array}
$$

The family $\tilde { B } ^ { \star }$ is a covering subfamily because

$$
\tilde { B } _ { 0 } \subseteq \tilde { B } _ { 1 } \cup \tilde { B } _ { 2 } .
$$

However the pair $\{ b , c \}$ is not contained in any block of $\tilde { B } ^ { \star }$ , so the pairwise-cover condition of Lemma 1(ii) fails. Indeed,

$$
\alpha ( \widetilde { B } ) = 1 \qquad \mathrm { w h e r e a s } \qquad \alpha ( \widetilde { B } ^ { \star } ) = 2 ,
$$

because $\{ b , c \}$ is feasible for $\tilde { B } ^ { \star }$ but not for ${ \tilde { B } } .$ This gives a strict instance of part (i):

$$
\alpha ( \widetilde { B } ) < \alpha ( \widetilde { B } ^ { \star } ) .
$$

The restricted covering theorem below uses the monotonicity part of Lemma 1, together with the fact that a covering family preserves the active vertex universe: once such a family $B ^ { \star }$ has been identified, Theorem 5 can be reapplied on that restricted family to obtain a sharper upper bound. Here $B ^ { \star } \subseteq B$ is a covering subfamily, meaning that every block $B \in B$ satisfies

$$
B \subseteq \bigcup _ { C \in B ^ { \star } } C .
$$

Equivalently, on the active block universe, every vertex that appears in the original block family still appears in the restricted family.

Theorem 7 (Restricted block-weight refinement over any covering family). $I f B ^ { \star } \subseteq B$ is a covering subfamily, then

$$
\alpha ( B ) \leq U ( B ^ { \star } ) \leq | B ^ { \star } | .\tag{49}
$$

In particular, taking $\boldsymbol { B ^ { \star } } = \left\{ B _ { 0 } ^ { \star } , \ldots , B _ { N _ { \mathrm { c o v } } - 1 } ^ { \star } \right\}$ from Theorem 6 gives

$$
\alpha ( B ) \leq U ( B ^ { \star } ) \leq N _ { \mathrm { c o v } } .
$$

Proof. By part (i) of Lemma 1,

$$
\alpha ( B ) \leq \alpha ( B ^ { \star } ) .
$$

Applying Theorem 5 to the restricted family $B ^ { \star }$ gives

$$
\alpha ( B ^ { \star } ) \leq U ( B ^ { \star } ) .
$$

Here the covering condition is what makes the restricted-family certificate legitimate for the same active vertex universe: no originally active vertex has disappeared from all retained blocks. Combining the two inequalities yields

$$
\alpha ( B ) \leq U ( B ^ { \star } ) .
$$

Finally, every block weight contributing to $U ( B ^ { \star } )$ is at least 1,

$$
U ( B ^ { \star } ) \leq | B ^ { \star } | .
$$

The outer iteration in Algorithm 3 is justified by the same covering-and-reweighting principle: once one covering family has been extracted, the pass may be applied again to that restricted family, yielding a nested sequence of valid upper bounds whose limit family has a simple fixed-point structure. Initialize

$$
\begin{array} { r } { B ^ { ( 0 ) } : = B , } \end{array}
$$

and for each $t \geq 0$ , let $B ^ { ( t + 1 ) } \subseteq B ^ { ( t ) }$ be the covering family produced by running the Greedy Layered Covering Algorithm on $B ^ { ( t ) }$ . If $B ^ { ( t + 1 ) } = B ^ { ( t ) }$ , the iteration has converged and stops; otherwise $B ^ { ( t + 1 ) } \subset B ^ { ( t ) }$ . Let $w _ { t } ^ { \star } ( B )$ denote the restricted block weight computed inside $B ^ { ( t ) }$

Theorem 8 (Iterated covering refinement and fixed-point structure). For the covering sequence above, every constructed $t \geq 1$ satisfies

$$
\alpha ( \boldsymbol { B } ) \leq U ( \boldsymbol { B } ^ { ( t ) } ) \leq | \boldsymbol { B } ^ { ( t ) } | ,\tag{50}
$$

and the sequence $| B ^ { ( t ) } |$ is nonincreasing and stabilizes after finitely many steps. Therefore there exists some $t _ { \mathrm { c o v } } \geq 0$ such that

$$
B ^ { ( t _ { \mathrm { c o v } } + 1 ) } = B ^ { ( t _ { \mathrm { c o v } } ) } ,
$$

and every block in the converged family has restricted weight 1:

$$
w _ { t _ { \mathrm { c o v } } } ^ { \star } ( B ) = 1 \qquad \forall B \in \mathcal { B } ^ { ( t _ { \mathrm { c o v } } ) } ,
$$

and consequently

$$
U ( B ^ { ( t _ { \mathrm { c o v } } ) } ) = | B ^ { ( t _ { \mathrm { c o v } } ) } | .
$$

Proof. We first prove the validity and termination claims. By construction, each $B ^ { ( t + 1 ) }$ is a covering subfamily of $B ^ { ( t ) }$ . Applying Theorem 7 with $B = B ^ { ( t - 1 ) }$ and $B ^ { \star } = B ^ { ( t ) }$ yields

$$
\alpha ( \boldsymbol { \mathcal { B } } ^ { ( t - 1 ) } ) \leq U ( \boldsymbol { \mathcal { B } } ^ { ( t ) } ) \leq | \boldsymbol { \mathcal { B } } ^ { ( t ) } | .
$$

Since $B ^ { ( t ) } \subseteq B ^ { ( t - 1 ) }$ for every t, Lemma 1(i) gives

$$
\alpha ( \boldsymbol { \mathcal { B } } ) = \alpha ( \boldsymbol { \mathcal { B } } ^ { ( 0 ) } ) \le \alpha ( \boldsymbol { B } ^ { ( t - 1 ) } ) ,
$$

and therefore

$$
\alpha ( \boldsymbol { B } ) \leq U ( \boldsymbol { B } ^ { ( t ) } ) \leq | \boldsymbol { B } ^ { ( t ) } | .
$$

Finally, because $B ^ { ( t + 1 ) } \subseteq B ^ { ( t ) }$ and the family sizes are nonnegative integers, the sequence $| B ^ { ( t ) } |$ is nonincreasing and must stabilize after finitely many iterations.

It remains to identify the structure of a converged family. Now let $t _ { \mathrm { c o v } }$ be a converged index. Run the Greedy Layered Covering Algorithm on the fixed family $B ^ { ( t _ { \mathrm { c o v } } ) }$ . Since the covering family returned by that run is again $B ^ { ( t _ { \mathrm { c o v } } ) }$ , every block of $B ^ { ( t _ { \mathrm { c o v } } ) }$ is eventually selected during the run.

Assume for contradiction that some block of $B ^ { ( t _ { \mathrm { c o v } } ) }$ has restricted weight greater than 1, and let

$$
\widehat { B } _ { > 1 } : = \{ B \in B ^ { ( t _ { \mathrm { c o v } } ) } : w _ { t _ { \mathrm { c o v } } } ^ { \star } ( B ) > 1 \} .
$$

Choose $B _ { i } \in  { \widehat { B } } _ { > 1 }$ to be the last block actually selected among all blocks of $\widehat { B } _ { > 1 }$ during this fixed-point rerun.

Take any vertex $v \in B _ { i }$ . Since $w _ { t _ { \mathrm { c o v } } } ^ { \star } ( B _ { i } ) > 1$ , every vertex of $B _ { i }$ has restricted degree at least 2 inside $B ^ { ( t _ { \mathrm { c o v } } ) }$ , so there exists another block $C _ { v } \neq B _ { i }$ in $B ^ { ( t _ { \mathrm { c o v } } ) }$ containing v.

If $w _ { t _ { \mathrm { c o v } } } ^ { \star } ( C _ { v } ) = 1$ , then $C _ { v }$ is selected earlier than $B _ { i }$ because all weight-1 blocks are processed before any block of intrinsic weight greater than 1. If $w _ { t _ { \mathrm { c o v } } } ^ { \star } ( C _ { v } ) > 1$ , then $C _ { v } \in \widehat { B } _ { > 1 }$ , and by the choice of $B _ { i }$ as the last selected block in $\widehat { B } _ { > 1 }$ , the block $C _ { v }$ is again selected earlier than $B _ { i }$

In either case, the earlier selection of $C _ { v }$ covers the vertex v before $B _ { i }$ would be selected. Since this argument applies to every vertex $v \in B _ { i }$ , all elements of $B _ { i }$ are already covered before its supposed selection time. Therefore the residual version of $B _ { i }$ is empty at that moment, so $B _ { i }$ must be discarded rather than selected. This contradicts the fact that every block of the fixed-point family $B ^ { ( t _ { \mathrm { c o v } } ) }$ is selected again in the rerun.

Therefore no converged block can have restricted weight greater than 1, proving that $w _ { t _ { \mathrm { c o v } } } ^ { \star } ( B ) = 1$ for every $B \in B ^ { ( t _ { \mathrm { c o v } } ) }$ . The displayed identity follows immediately. □

Apply the iterated covering procedure until convergence, and denote its terminal covering family by

$$
\widehat { B } : = B ^ { ( t _ { \mathrm { c o v } } ) } .
$$

Since Theorem 8 gives unit block weight in ${ \widehat { B } } ,$ choose for each terminal block $B \in { \widehat { B } }$ one vertex $z _ { B } \in B$ with

$$
d _ { \widehat { B } } ( z _ { B } ) = 1 .
$$

Set

$$
Z : = \{ z _ { B } : B \in { \widehat { B } } \} .
$$

The next corollary records the canonical structure of this terminal restricted family.

Corollary 5 (Unit-weight covering family and canonical optimum). For every finite block family $B { \mathrm { , } }$ the terminal family $\widehat { B }$ is a covering subfamily of $\boldsymbol { B }$ satisfying

$$
w _ { \widehat { B } } ( B ) = 1 \qquad \forall B \in \widehat { B } .
$$

Moreover, $Z$ is an optimal retained set for $\widehat { B }$ satisfying

$$
| Z | = | { \widehat { B } } | = \alpha ( { \widehat { B } } ) = U ( { \widehat { B } } ) .
$$

Proof. Theorem 8 shows that $\widehat { B }$ is a covering subfamily of B and that every block in $\widehat { B }$ has intrinsic weight 1. Therefore Theorem 2, applied to the restricted family ${ \widehat { B } } ,$ gives an optimal feasible set containing one restricted-degree-1 vertex $z _ { B }$ from every block $B \in { \widehat { B } }$ . Since each such vertex belongs to exactly one block of ${ \widehat { B } } .$ the selected set $Z$ is feasible and satisfies $| Z | = | \widehat { B } |$ . The same fixed-point theorem also gives $U ( { \widehat { B } } ) = | { \widehat { B } } |$ , and Theorem 5 applied to $\widehat { B }$ yields $\alpha ( { \widehat { B } } ) \leq U ( { \widehat { B } } )$ . Hence

$$
| Z | = | \widehat { B } | = U ( \widehat { B } ) \geq \alpha ( \widehat { B } ) .
$$

Since $Z$ is itself feasible for ${ \widehat { B } } ,$ one also has $| Z | \leq \alpha ( { \widehat { B } } )$ , proving the claimed equalities. □

For later comparison, it is convenient to name the main structural bounds introduced in this section:

$$
U _ { \mathrm { c f } } : = U ( \mathcal { B } ) , \qquad U _ { \mathrm { w 1 } } : = | \mathcal { B } _ { 1 } | + U ( \mathcal { B } ^ { \circ } ) , \qquad U _ { \mathrm { p u n c t } } : = \mathrm { P u n c t } ( \mathcal { B } ) ,\tag{51}
$$

for the closed-form, weight-1 preclusion, and iterative puncturing bounds, respectively, where Punct(B) denotes the certificate returned by Algorithm 2. For an iterated covering sequence $B ^ { ( t ) }$ we also write

$$
N _ { \mathrm { c o v } } ^ { ( t ) } : = | \boldsymbol { \mathcal { B } } ^ { ( t ) } | , \qquad U _ { \mathrm { c o v e r } } ^ { ( t ) } : = U ( \boldsymbol { B } ^ { ( t ) } ) .\tag{52}
$$

In particular, the first covering round satisfies $N _ { \mathrm { c o v } } ^ { ( 1 ) } = | B ^ { \star } |$ and $U _ { \mathrm { c o v e r } } ^ { ( 1 ) } = U ( B ^ { \star } )$ . When the iterated sequence is run to convergence in the empirical tables, $U _ { \mathrm { c o v e r } }$ denotes the final value of $U _ { \mathrm { c o v e r } } ^ { ( t ) }$

The puncturing and iterated-covering refinements are complementary rather than inclusive. The iterative puncturing bound $U _ { \mathrm { p u n c t } }$ is obtained by searching over residual block subfamilies across outer rounds, recomputing intrinsic weights after punctures, and repeatedly precluding newly exposed weight-1 blocks; the iterated covering bounds $U _ { \mathrm { c o v e r } } ^ { ( t ) }$ instead come from repeatedly selecting covering families and recomputing intrinsic weights on those restricted families. Because these mechanisms are diferent, neither quantity dominates the other in general. In practice, one should therefore compute both and use

$$
\operatorname* { m i n } \{ U _ { \mathrm { p u n c t } } , U _ { \mathrm { c o v e r } } ^ { ( 1 ) } , U _ { \mathrm { c o v e r } } ^ { ( 2 ) } , . . . \}
$$

as the strongest available structural comparison target.

Example 1(e) (Running example under the Greedy Layered Covering Algorithm). The running example from Examples $1 ( \mathrm { a } ) { - } 1 ( \mathrm { d } )$ also behaves cleanly under the Greedy Layered Covering Algorithm. One valid covering run selects

$$
B ^ { \star } = \{ B _ { 1 } , B _ { 2 } , B _ { 4 } , B _ { 8 } , B _ { 1 1 } \} ,
$$

so Theorem 6 gives

$$
\alpha ( B ) \leq N _ { \mathrm { c o v } } = 5 .
$$

Moreover, in the restricted family $B ^ { \star }$ , each selected block has restricted weight 1: for example, $B _ { 1 }$ contains the restricted-degree-1 vertex $a , B _ { 2 }$ contains $b ,$ and $B _ { 4 } , B _ { 8 } , B _ { 1 1 }$ contain $e , c , r ,$ respectively. Hence

$$
\begin{array} { r } { U ( \mathcal { B } ^ { \star } ) = 1 + 1 + 1 + 1 + 1 = 5 , } \end{array}
$$

so Theorem $^ { 7 }$ reaches the same exact value. In this instance the dominance-reduced closed-form bound, the covering-family restricted bound, and the closed-form bound for the selected covering set all coincide with the true optimum.

This algorithm is complementary to Theorem 5: Theorem 5 gives a closed-form charging bound, while the Greedy Layered Covering Algorithm searches directly for a small covering family of incompatibility blocks in the active minimum-weight layer. Theorem 7 then shows that any such covering family, regardless of how it is obtained, yields a restricted block-weight upper bound.

The practical purpose of the greedy construction is to capture concentration efects like the weight-2 example above, where many nominal low-weight candidates collapse into a single forced block. The secondary key $n _ { t } ( B )$ is not needed for validity, but it is the natural greedy choice because it attempts to cover as many current degree-λ<sub>t</sub> candidates as possible per unit increase of the bound. The tradeof is that, unlike Theorem 5, this procedure has no closed form and requires repeated residual updates, so its direct computation is typically quadratic-scale in the residual incidences.

Remark 6 (Closed-form, covering-count, and restricted-covering bounds are complementary). Consider the block family

$$
\left\{ a _ { 1 } , a _ { 2 } , a _ { 3 } , a _ { 4 } , a _ { 6 } \right\} , \left\{ a _ { 1 } , a _ { 2 } , a _ { 5 } , a _ { 6 } , a _ { 7 } \right\} , \left\{ a _ { 3 } , a _ { 4 } , a _ { 5 } , a _ { 7 } , a _ { 8 } \right\} , \left\{ a _ { 2 } , a _ { 4 } , a _ { 7 } \right\} , \left\{ a _ { 1 } , a _ { 3 } , a _ { 8 } \right\} , \left\{ a _ { 5 } , a _ { 6 } , a _ { 8 } \right\} .
$$

Here each vertex has degree 3, so all blocks have weight 3 and Theorem 5 gives $6 / 3 = 2$ . By contrast, the Greedy Layered Covering Algorithm of Theorem 6 may select the three covering blocks

$$
B ^ { \star } = \{ B _ { 0 } , B _ { 1 } , B _ { 2 } \} ,
$$

yielding the coarser count bound $N _ { \mathrm { c o v } } = 3$ , even though one minimal covering family is already $\{ B _ { 1 } , B _ { 2 } \}$ . However, Theorem $7$ recovers the sharper value from the selected family itself: within $B ^ { \star }$ one has

$$
w ^ { \star } ( B _ { 0 } ) = 2 , \qquad w ^ { \star } ( B _ { 1 } ) = 2 , \qquad w ^ { \star } ( B _ { 2 } ) = 1 ,
$$

so

$$
U ( B ^ { \star } ) = \frac { 1 } { 2 } + \frac { 1 } { 2 } + 1 = 2 .
$$

This matches the exact optimum. Indeed, the unique optimal retained set is

$$
\{ a _ { 2 } , a _ { 8 } \} ,
$$

because $a _ { 2 }$ and $a _ { 8 }$ are the only pair of vertices that never co-occur in a block, whereas every other vertex conflicts with all remaining candidates. Thus this example shows all three viewpoints are genuinely diferent: the closed-form theorem can already be sharp on balanced instances, the greedy covering count from Theorem 6 may overcount through a non-minimal covering choice, and Theorem 7 can tighten that covering-based bound back down by reweighting the selected covering family.

Theorem 8 makes the picture sharper in two ways. First, for the weight-2 family above, a covering family such as $\{ O , E , P _ { 1 } \}$ gives the restricted-weight bound $1 + 1 + 1 = 3$ : this is still not exact, but it collapses the gap from asymptotically growing with n to a constant-size gap. Second, for the present example, a second covering iteration reduces the family to $\{ B _ { 1 } , B _ { 2 } \}$ , yielding the tight bound

$$
1 + 1 = 2 .
$$

This favorable outcome also depends on the secondary greedy order inside each weight layer: the decreasing- $\cdot n _ { \mathrm { m i n } } ( B )$ rule is what drives the covering routine toward the stronger families used here, whereas a weaker within-layer order need not produce the same refinement.

## 5 Greedy Layered block-Weight Clustering

The previous section develops structural bounds for the maximum strong independent set objective on a hyperedge family. We now turn those bounds into an explicit block-native solver. In particular, Theorems 5 and 6 provide the first-order and second-order scheduling principles that drive the block-weight-layered greedy clustering routine below.

Generic bounded-degree hypergraph algorithms typically treat a hypergraph as a graph-style combinatorial object in a more general setting [10]. In contrast, the approach here is explicitly incidence-native: it assumes the block family itself is materialized and uses block weights, minimumdegree witnesses, and block-side residual updates as the primary state.

Theorem 5 provides the first-order principle: blocks of smaller minimum weight are more constraining and therefore should be processed first. Theorem 6 provides a second-order principle: within a fixed block-weight layer, blocks containing more minimum-weight vertices exert stronger covering pressure and should be prioritized next. This suggests a block-native greedy algorithm organized by block-weight layers with a lexicographic within-layer priority, rather than by an auxiliary hypergraph optimization routine.

Let $\boldsymbol { B } = \{ B _ { 1 } , \dots , B _ { M } \}$ denote the incidence-twin-compressed block family. The algorithm below is stated on an abstract indexed family, but its operations are deliberately block-local: residual deletion and layer updates are expressed as scans of block records and vertex-to-block incidence lists.

Incidence-twin compression quotients vertices with identical block-index support and stores their original vertex lists for final materialization if needed. All initial degrees, block weights, and $n _ { \mathrm { m i n } }$ values are computed on this quotient family; this avoids treating mutually exclusive identical-support vertices as separate minimum-weight candidates.

Following Corollary 2, the clustering routine first performs a weight-1 preclusion pass. For each $B \in \mathcal { B } _ { 1 } : = \{ B \in B : w ( B ) = 1 \}$ , it selects a degree-1 vertex class z<sub>B</sub> as canonical root, attaches currently unassigned members of B to that root, and marks attached classes as removed from subsequent processing. It then removes attached classes from all remaining blocks and keeps the resulting nonempty residual family $B ^ { \mathrm { r e s } }$ . Degrees are recomputed on $B ^ { \mathrm { r e s } }$ and kept fixed during the later layered pass; once a class is attached, it is removed from subsequent layer processing rather than assigned a new residual degree.

In implementation, we materialize $B ^ { \mathrm { r e s } }$ as a mutable list-of-lists representation

$$
\mathbf { B } = [ B _ { 1 } , \ldots , B _ { M _ { \mathrm { r e s } } } ] ,
$$

where each $B _ { i }$ is updated in place during processing. Layer priorities use

$$
\widehat { w } ( B ) : = \operatorname* { m i n } _ { v \in B } d ( v ) , \qquad n _ { \operatorname* { m i n } } ( B ) : = \big | \{ v \in B : d ( v ) = \widehat { w } ( B ) \} \big | ,
$$

and for each integer weight w we maintain a block-index set

$$
{ \mathcal { S } } _ { w } : = \{ i : { \widehat { w } } ( B _ { i } ) = w \} .
$$

Within a fixed block-weight layer $\widehat { w } ( B ) = w$ , Theorem 6 suggests prioritizing larger $n _ { \mathrm { m i n } } ( B )$ : such blocks cover more current degree-w candidates and therefore exert stronger covering pressure. Representative state is maintained by a root set R and a vertex-to-root map $\phi : V \to V$ (vertex 7→ current root), initialized from the weight-1 preclusion pass and then updated by the layered greedy stage. The blocks are released progressively $( B _ { i }  \emptyset )$ until all residual blocks are consumed.

Definition 6 (Active root). A vertex u is an active root if it is currently chosen as the representative of its cluster.

Definition 7 (vertex-to-root map). A vertex-to-root map is a function $\phi : V \to V$ such that $\phi ( v )$ is the current representative of the cluster containing v, and $\phi ( v ) = v \ \mathrm { i f f } \ v$ is a root.

Definition 8 (Cluster). For a root u, define

$$
{ \mathcal { C } } _ { u } : = \{ v \in V : \phi ( v ) = u \} .\tag{53}
$$

Definition 9 (Root selection key). After a block B has been chosen by the block-level priority, let

$$
w ( B ) : = \operatorname* { m i n } _ { v \in B } d ( v ) , \qquad M ( B ) : = \{ v \in B : d ( v ) = w ( B ) \} .\tag{54}
$$

Thus

$$
n _ { \mathrm { m i n } } ( B ) = | M ( B ) | .
$$

For a candidate $v \in M ( B )$ , define its equal-weight collision neighborhood

$$
\mathcal N _ { w ( B ) } ( v ) : = \left\{ u \in \bigcup _ { B ^ { \prime } \ni v } B ^ { \prime } : d ( u ) = w ( B ) \right\} ,\tag{55}
$$

and its equal-weight collision score

$$
c _ { w ( B ) } ( v ) : = \left| \mathcal { N } _ { w ( B ) } ( v ) \right| .\tag{56}
$$

Since each $v \in M ( B )$ lies in exactly $w ( B )$ blocks, $c _ { w ( B ) } ( v )$ counts the number of distinct degree-w(B) vertices coupled to v through those blocks. Thus, after the block B has been selected, the root is chosen from $M ( B )$ by

$$
v ^ { \star } = \arg \operatorname* { m i n } _ { v \in M ( B ) } \bigl ( c _ { w ( B ) } ( v ) , \operatorname { i d } ( v ) \bigr ) .\tag{57}
$$

This can also be extended to include source-priority terms without changing feasibility.

Fix a block B and two candidate roots $u , v \in M ( B )$ , so that

$$
d ( u ) = d ( v ) = w ( B ) .
$$

Proposition 3 (Equal-weight collision minimization). The number of current degree-w(B) candidates consumed immediately by the root choice is monotone in the collision score:

$$
c _ { w ( B ) } ( u ) \leq c _ { w ( B ) } ( v ) \quad \Longrightarrow \quad | \mathcal { N } _ { w ( B ) } ( u ) | \leq | \mathcal { N } _ { w ( B ) } ( v ) | .
$$

Consequently, after a block has been selected, choosing from $M ( B )$ the candidate with the smallest $c _ { w \left( B \right) } \left( \cdot \right)$ minimizes the number of degree-w(B) candidates clustered away at that step, and therefore preserves the largest remaining pool of degree-w(B) candidates for future roots in the same layer.

Proof. By definition, $\mathcal { N } _ { w ( B ) } ( v )$ is exactly the set of current degree-w(B) vertices coupled to v through the blocks incident to v. Once v is chosen as root, these coupled degree-w(B) vertices are clustered away from future root consideration in the current layer. Hence the number of degree-w(B) candidates consumed immediately by choosing v is exactly

$$
c _ { w ( B ) } ( v ) = | \mathcal { N } _ { w ( B ) } ( v ) | .
$$

Therefore minimizing the collision score minimizes the number of current degree-w(B) candidates removed at that step, leaving the largest remaining pool available for subsequent root creation in the same layer. □

Note that $c _ { w ( B ) } ( v )$ counts distinct degree-w(B) competitors, so in general

$$
c _ { w ( B ) } ( v ) \leq \sum _ { B ^ { \prime } \ni v } n _ { \operatorname* { m i n } } ( B ^ { \prime } ) ,
$$

with equality only when those minimum-weight candidate sets are disjoint outside of v itself.

Algorithm 4 (Greedy Layered Clustering Algorithm). The clustering stage proceeds as follows.

1. Load the indexed blocks, compress incidence-twin vertex classes, and denote the resulting quotient family by $\boldsymbol { B } = \{ B _ { 1 } , \dots , B _ { M } \}$ . Count quotient-vertex frequencies $d ( v )$

2. Weight-1 preclusion: identify ${ \mathcal { B } } _ { 1 } : = \{ B \in { \mathcal { B } } : w ( B ) = 1 \}$ . For each $B \in B _ { 1 }$ , pick a degree-1 root $z _ { B }$ , attach currently unassigned members of B to $z _ { B }$ , and update $R , \phi .$

3. Remove already clustered vertices from remaining blocks, set $B ^ { \mathrm { r e s } }$ to the resulting nonempty residual block family, materialize it as $\mathbf { B } = [ B _ { 1 } , \ldots , B _ { M _ { \mathrm { r e s } } } ]$ , recompute residual frequencies $d ( v )$ on $B ^ { \mathrm { r e s } }$ , and for each residual block index i compute $\begin{array} { r } { \widehat { w } ( B _ { i } ) = \operatorname* { m i n } _ { v \in B _ { i } } d ( v ) } \end{array}$ and insert i into the corresponding block-index set $\mathcal { S } _ { \widehat { w } \left( B _ { i } \right) }$

4. For each block-weight layer w from the current minimum upward:

(a) First pass over $s _ { w } .$ for each $i \in S _ { w }$ , remove already clustered vertices from $B _ { i }$ . If $B _ { i } = \varnothing$ discard it. If $B _ { i }$ contains no degree-w vertex, recompute $\widehat { w } ( B _ { i } ) > w$ and move i to the corresponding larger-weight set ${ \mathcal { S } } _ { \widehat { w } \left( B _ { i } \right) }$ . Otherwise compute $n _ { \operatorname* { m i n } } ( B _ { i } ) = | \{ v \in B _ { i } : d ( v ) = w \} |$ and keep i in the active layer.

(b) From the surviving current-layer blocks, build the degree-w vertex-to-block index

$$
\begin{array} { r } { \mathcal { T } _ { w } ( v ) : = \{ i \in \mathcal { S } _ { w } : v \in B _ { i } , \ d ( v ) = w \} , } \end{array}
$$

for each active degree-w vertex.

(c) While active blocks remain in the current layer and the index $\mathcal { T } _ { w } ( \cdot )$ is nonempty, choose an active block $B _ { i }$ with maximum current $n _ { \mathrm { m i n } } ( B _ { i } )$ , breaking ties arbitrarily.

(d) For the chosen block, define $M ( B _ { i } ) : = \{ v \in B _ { i } : d ( v ) = w \}$ . If $M ( B _ { i } ) = \alpha _ { \cdot }$ , recompute $\widehat { w } ( B _ { i } ) > w$ , move i to the corresponding larger-weight set $\mathcal { S } _ { \widehat { w } \left( B _ { i } \right) }$ , and continue. Otherwise compute the current collision scores $c _ { w } ( v )$ for $v \in M ( B _ { i } )$ and choose

$$
\boldsymbol { u } ^ { \star } = \arg \operatorname* { m i n } _ { \boldsymbol { v } \in M ( B _ { i } ) } \big ( c _ { w } ( \boldsymbol { v } ) , \mathrm { i d } ( \boldsymbol { v } ) \big ) .
$$

(e) Let $\mathcal { T } _ { w } ( u ^ { \star } )$ be the associated degree-w block indices of the new root. Cluster all currently unassigned vertices in

$$
\bigcup _ { j \in \mathcal { T } _ { w } ( u ^ { \star } ) } B _ { j }
$$

to $u ^ { \star }$ and set those blocks to $\varnothing .$ For each degree-w vertex v in that union, use the alreadybuilt index $\mathcal { T } _ { w } ( v )$ to decrement by one the current counts $n _ { \mathrm { { m i n } } } ( \cdot )$ of all still-active associated blocks, then delete v from $\mathcal { T } _ { w } ( \cdot )$ . If the count of an active block drops to 0, then no degree-w vertex remains in it; recompute $\widehat { w } ( B _ { i } ) > w$ and move it to the corresponding larger-weight set $\mathcal { S } _ { \widehat { w } \left( B _ { i } \right) }$

(f) Delete the completed layer set $\mathcal { S } _ { w }$ and continue with the next nonempty weight layer.

5. Output root set R and vertex-to-root map $\phi$ (including both weight-1 preclusion and layered greedy assignments).

For one update step of Algorithm 3 or Algorithm 4 on a current residual family F and block-weight layer $w ,$ let $\mathcal { Q }$ be the block collection absorbed by that step:

$$
{ \mathcal Q } = \{ B _ { t } ^ { \star } \}
$$

for the covering algorithm, and

$$
\mathcal { Q } = \{ B _ { j } : j \in \mathcal { T } _ { w } ( u ^ { \star } ) \}
$$

for the clustering algorithm. Let

$$
A ( \mathcal { Q } ) : = \bigcup _ { B \in \mathcal { Q } } B
$$

be the vertices covered or clustered by the step.

Proposition 4 (Implicit suppression for block-weight witnesses). If $a \in A ( \mathcal { Q } )$ is a current blockweight witness,

$$
\begin{array} { r } { d _ { \mathcal { F } } ( a ) = w , } \end{array}
$$

and an active vertex b satisfies

$$
I _ { \mathcal { F } } ( a ) \subseteq I _ { \mathcal { F } } ( b ) ,
$$

then $b \in A ( \mathcal { Q } )$ as well. Consequently, a vertex whose current support is a superset ofan absorbed blockweight witness cannot survive beyond that absorbing step. Thus the low-to-high flow of Algorithms 3 and 4 performs a local, implicit form of superset suppression for current minimum-degree witnesses, without running a global support-containment pass.

Proof. Since $a \in A ( \mathcal { Q } )$ , there exists some absorbed block $B \in \mathcal { Q }$ with $a \in B$ . Hence the index of B belongs to $I _ { \mathcal { F } } ( a )$ . The containment $I _ { \mathcal { F } } ( a ) \subseteq I _ { \mathcal { F } } ( b )$ implies $b \in B$ . Therefore $b \in A ( \mathcal { Q } )$

In Algorithm 3, all vertices in $A ( \mathcal { Q } ) = B _ { t } ^ { \star }$ are added to the covered set and removed from subsequent residual blocks. In Algorithm 4, all currently unassigned vertices in $A ( \mathcal { Q } )$ are assigned to the chosen root and removed from later layer processing. Thus any active support-superset b is suppressed no later than the step that absorbs the current block-weight witness a. □

This proposition is deliberately weaker than the fixed-point statement of Algorithm 1. It does not claim that covering or clustering computes a full dominance closure, nor does it claim a suppression property for arbitrary nonminimum vertices incidentally absorbed by a selected block. Rather, it explains why explicit superset reduction is unnecessary inside their low-to-high inner loops for the vertices that actually drive the layer order: once a selected block absorbs a current block-weight witness, every active vertex whose support contains that witness support is absorbed automatically.

This restriction to block-weight witnesses is also the right structural focus. The closed-form bound charges each block by the inverse of its minimum incident degree, so the vertices attaining, or nearly attaining, block weights determine the bottleneck terms in the certificate and drive both the covering score $n _ { t } ( B )$ and the clustering score $n _ { \mathrm { m i n } } ( B )$ . Superset efects involving only higher-degree nonminimum vertices may exist, but they do not control the first-order block-weight certificate. The low-to-high algorithms therefore spend their local update budget on the near-minimum witnesses that dominate the bound, rather than on a global dominance closure over all vertices.

This algorithm is genuinely layered: each weight level is cleaned first, then resolved by repeated $\mathrm { m a x i m u m } { - } n _ { \mathrm { m i n } } ( B )$ choices driven by the current degree-w vertex-to-block index. It therefore keeps the two structural priorities suggested by Theorem 5 and Theorem 6 while localizing all updates to the blocks touched by the newly attached degree-w vertices.

Remark 7 (Eficient within-layer maintenance). Algorithm 4 reuses the same degree-w vertex-toblock index $\mathcal { T } _ { w } ( \cdot )$ both to select the blocks incident to a new root and to update the afected $n _ { \mathrm { m i n } } ( \cdot )$ counters after selection. Consequently, a fixed layer can be processed without rescanning all surviving blocks after each root choice: each degree-w vertex is deleted from $\mathcal { T } _ { w } ( \cdot )$ at most once, and each current-layer incidence is touched only when its degree-w endpoint is attached away. With a bucketed queue or equivalent priority structure keyed by the current $n _ { \mathrm { m i n } } ( B )$ values, the within-layer implementation is therefore essentially linear in the active layer incidences.

Remark 8 (Score-indexed next-block retrieval). In an implementation of Step $4 ( \mathrm { c } )$ , the next active block with largest current $n _ { \mathrm { m i n } } ( B )$ can be maintained explicitly rather than found by rescanning the whole layer. Besides the degree-w vertex-to-block mapping table $\mathcal { T } _ { w } ( \cdot )$ , one keeps a current score table with one entry per active layer block, together with the inverse table that stores the active block indices at each score level. Then the next block is obtained from the largest nonempty score class, and each selection step updates only the blocks incident to the removed degree-w vertices by decrementing their current score and moving their index between score classes. Thus maximum- $\cdot n _ { \mathrm { m i n } } ( B )$ maintenance remains local to the touched incidences and avoids repeated full-layer scans.

The following worked example continues the running example from Examples $1 ( \mathrm { a } ) { - } 1 ( \mathrm { d } )$ . Here the focus is no longer on the incidence data or the closed-form sum, but on how the layered greedy procedure realizes that bound and why it naturally leads to the exactness theorem.

Example 1(f) (Worked example for the greedy layered clustering algorithm). For the running example, now partition the block indices into

$$
\mathcal { I } _ { 1 } = \{ 1 \} , \qquad \mathcal { I } _ { 2 } = \{ 2 , 3 \} , \qquad \mathcal { I } _ { 3 } = \{ 4 , 5 \} , \qquad \mathcal { I } _ { 4 } = \{ 6 , 7 , 8 \} , \qquad \mathcal { I } _ { 5 } = \{ 9 , 1 0 , 1 1 , 1 2 \} ,
$$

with canonical quotient representatives

$$
z _ { 1 } = a , \qquad z _ { 2 } = b , \qquad z _ { 3 } = e , \qquad z _ { 4 } = c , \qquad z _ { 5 } = r .
$$

Component contraction would still select only one vertex on this connected family, while the dominance-reduced block certificate gives the optimal value 5. The remaining question is whether

the greedy layered clustering algorithm can realize that value without an explicit global dominance pass. The key mechanism is support-superset suppression. The vertex u satisfies

$$
I ( u ) = \{ 1 , 2 \} ,
$$

so it strictly contains $I ( a ) = \{ 1 \}$ . The smaller-weight branch applies because its extra block is attached to the lower-weight block $B _ { 1 }$

$$
w ( B _ { 1 } ) = 1 < 2 .
$$

The gate vertices $f$ and g similarly satisfy

$$
I ( f ) = \{ 2 , 3 , 9 \} , \qquad I ( g ) = \{ 4 , 5 , 1 0 \} ,
$$

with

$$
I ( b ) = \{ 2 , 3 \} \subsetneq I ( f ) , \qquad I ( e ) = \{ 4 , 5 \} \subsetneq I ( g ) .
$$

Thus $f$ and g are suppressed by the weight-2 representatives b and e, respectively. Before they are suppressed, they gate the blocks $B _ { 9 }$ and $B _ { 1 0 }$ down to apparent weight 3; after suppression, those blocks recover their true reduced weight 4.

The greedy layered clustering algorithm now proceeds as follows.

(i) Weight-1 preclusion chooses a from $B _ { 1 }$ , clustering away the support-superset distractor u. This is the algorithmic version of the first dominance deletion used by Theorem 4: $B _ { 2 }$ loses u before the weight-2 layer is processed.

(ii) In the weight-2 layer, blocks $B _ { 2 } , B _ { 3 }$ have b as their degree-2 representative, while $B _ { 4 } , B _ { 5 }$ have e as their degree-2 representative. The algorithm chooses b and clusters the union $B _ { 2 } \cup B _ { 3 }$ thereby removing f and the helpers $p , q$ from those two blocks; it then chooses e from $B _ { 4 } , B _ { 5 }$ removing g from the remaining active family. This exposes $B _ { 9 }$ and $B _ { 1 0 }$ as true weight-4 blocks in the dominance-reduced view.

(iii) In the weight-3 layer, the residual blocks are

$$
\begin{array} { r l r } & { } & { B _ { 6 } = \{ c , x \} , \qquad B _ { 7 } = \{ c , y \} , \qquad B _ { 8 } = \{ c , x , y \} , } \\ & { } & { B _ { 9 } = \{ r , x \} , \qquad B _ { 1 0 } = \{ r , y \} , \qquad B _ { 1 1 } = \{ r , x \} , \qquad B _ { 1 2 } = \{ r , y \} . } \end{array}
$$

The only degree-3 representative in this layer is $^ { c , }$ so the algorithm chooses c and nullifies $B _ { 6 } , B _ { 7 } , B _ { 8 }$ , thereby removing x and y. The remaining weight-4 block then reduces to blocks containing only $r ,$ so the algorithm chooses r.

The final root set is

$$
R _ { G } = \{ a , b , e , c , r \} ,
$$

so

$$
| R _ { G } | = 5 .
$$

Thus the greedy layered clustering algorithm exactly attains the dominance-reduced closed-form bound, while connected-component contraction would retain only one vertex.

Example 5 (Unified trace for puncturing, covering, and greedy clustering). Consider the pair/parity family from Example 4:

$$
P _ { t } = \{ x _ { 2 t - 1 } , x _ { 2 t } \} \quad ( t = 1 , \ldots , n ) , \qquad O = \{ x _ { 1 } , x _ { 3 } , \ldots , x _ { 2 n - 1 } \} , \qquad E = \{ x _ { 2 } , x _ { 4 } , \ldots , x _ { 2 n } \} ,
$$

with $n \geq 3$ . Every vertex has degree 2, so every block has weight 2 and the closed-form certificate is

$$
U ( B ) = { \frac { n + 2 } { 2 } } .
$$

This is loose because $\alpha ( B ) = 2 \because$ : one can retain, for example, $\{ x _ { 1 } , x _ { 4 } \}$ , while $O$ and E allow at most one odd and one even retained vertex.

The puncturing algorithm sharpens the certificate by deleting the pair blocks. Initially

$$
n _ { \mathrm { m i n } } ( P _ { t } ) = 2 , \qquad n _ { \mathrm { m i n } } ( O ) = n _ { \mathrm { m i n } } ( E ) = n ,
$$

so the high-to-low puncturing schedule, with ${ \mathrm { m i n i m u m } } { \cdot } n _ { \mathrm { m i n } }$ priority inside the layer, selects the pair blocks before the parity blocks. Puncturing the pair blocks leaves the residual family $\{ O , E \}$ After the next weight-1 preclusion rebuild, O and $E$ each contribute one canonical unit and the residual family is empty. Thus

$$
U _ { \mathrm { p u n c t } } = 2 .
$$

The covering algorithm reaches the same certificate from the opposite direction. It starts in the minimum layer $w = 2$ and chooses maximum ${ \mathbf { - } } n _ { t }$ blocks. Initially

$$
n _ { t } ( O ) = n _ { t } ( E ) = n , \qquad n _ { t } ( P _ { s } ) = 2 \quad ( s = 1 , \ldots , n ) ,
$$

so it selects one parity block, say $O _ { i }$ , before any pair block. After this dynamic update, all odd vertices are covered, the residual counts satisfy $n _ { t } ( E ) = n$ and $n _ { t } ( P _ { s } ) = 1$ , and the decreasing-n<sub>t</sub> rule selects $E$ next. Thus the selected covering family is $B ^ { \star } = \{ O , E \}$ , whose intrinsic block weights are both one. Therefore

$$
U _ { \mathrm { c o v e r } } = U ( B ^ { \star } ) = 2 .
$$

Finally, the greedy layered clustering algorithm also follows the parity structure. In the weight-2 layer it first selects one of $O$ and $E ;$ suppose it selects O and chooses root $x _ { 1 }$ . The associated blocks of $x _ { 1 }$ are $O$ and $P _ { 1 }$ , so the step clusters away all odd-indexed vertices and $x _ { 2 }$ . The remaining parity block $E$ still contains the even vertices $x _ { 4 } , x _ { 6 } , \ldots , x _ { 2 n }$ and has the largest current $n _ { \mathrm { m i n } }$ among surviving blocks. The algorithm then chooses one even root, say $x _ { 4 } .$ , and clusters the remaining even side. The final retained set has two roots, for instance

$$
R _ { G } = \{ x _ { 1 } , x _ { 4 } \} ,
$$

so

$$
| R _ { G } | = U _ { \mathrm { p u n c t } } = U _ { \mathrm { c o v e r } } = \alpha ( B ) = 2 .
$$

This example is complementary to the previous running example: the closed-form bound is loose, but puncturing, covering, and greedy clustering all identify the same sharper two-representative structure.

The worked example separates the mechanisms behind greedy attainment of the structural optimum from Theorem 4: support-superset distractors are first absorbed by their dominators, and this can reveal higher-weight blocks that were hidden by low-degree gate vertices. The theorem below isolates the algorithmic add-on. The structural optimality is already certified by Theorem 4; the additional conditions ensure that the greedy layered clustering algorithm selects the corresponding representatives.

For this algorithmic statement, assume the block family has been incidence-twin compressed. Let $D \subseteq V$ be a set of support-superset vertices that the greedy run suppresses before any vertex in $D$ can be selected as a root. Define

$$
\begin{array} { r } { \mathcal { B } ^ { - } : = \{ B \setminus D : B \in \mathcal { B } , B \setminus D \neq \emptyset \} = \{ B _ { i } ^ { - } : i \in \mathcal { T } ^ { - } \} , } \end{array}
$$

and let $I _ { - } ( \cdot ) , d _ { - } ( \cdot )$ , and $w _ { - } ( \cdot )$ denote supports, degrees, and block weights in $B ^ { - }$ . Suppose the block-index set $\mathcal { T } ^ { - }$ is partitioned into nonempty blocks

$$
\mathcal { I } _ { 1 } , \ldots , \mathcal { I } _ { m } ,
$$

with quotient vertices $z _ { 1 } , \ldots , z _ { m }$ satisfying

$$
I _ { - } ( z _ { t } ) = \mathcal { T } _ { t } .
$$

Write $w _ { t } : = | \mathcal { I } _ { t } |$ and

$$
Z : = \{ z _ { 1 } , \ldots , z _ { m } \} .
$$

For each block, write

$$
c _ { t } : = c _ { w _ { t } } ( z _ { t } )
$$

for the current collision score used when the first surviving block from block $\mathcal { T } _ { t }$ is processed. With this notation, the suficient conditions for greedy attainment have three roles: suppress the support-superset distractors before they become roots, align each block’s block weights with its representative support size, and ensure that every same-layer competitor is either removed earlier or loses the root-selection tie-break.

Theorem 9 (Greedy attainment after support-superset suppression). Assume:

(i) every $v \in D$ is clustered to some representative $z _ { t }$ with $I ( z _ { t } ) \subsetneq I ( v )$ before v can be selected as a root;

(ii) for every t and every $i \in \mathcal { I } _ { t }$

$$
w _ { - } ( B _ { i } ^ { - } ) = w _ { t } ;
$$

(iii) for every $t ,$ every $i \in \mathcal { I } _ { t }$ , and every vertex $u \in B _ { i } ^ { - }$ with

$$
d _ { - } ( u ) = w _ { t } \qquad a n d \qquad u \neq z _ { t } ,
$$

at least one of the following holds:

(a) there exists

$$
j \in I _ { - } ( u ) \setminus \mathcal { T } _ { t }
$$

such that

$$
w _ { - } ( B _ { j } ^ { - } ) < w _ { t } ;
$$

(b) if u is still active when the first surviving block from $\mathcal { T } _ { t }$ is processed, then it loses to $z _ { t }$ under the current root-selection key:

$$
\left( c _ { t } , \mathrm { i d } ( z _ { t } ) \right) < \left( c _ { w _ { t } } ( u ) , \mathrm { i d } ( u ) \right) .
$$

Then the Greedy Layered Clustering Algorithm creates exactly the root set

$$
R _ { G } = \{ z _ { 1 } , \ldots , z _ { m } \} ,
$$

and therefore

$$
| R _ { G } | = U ( B ^ { - } ) = \alpha ( B ) .\tag{58}
$$

Proof. By Theorem 4, the set $\{ z _ { 1 } , \ldots , z _ { m } \}$ is maximum and $\alpha ( B ) = m$ . Also, by assumption (ii),

$$
U ( B ^ { - } ) = \sum _ { t = 1 } ^ { m } \sum _ { i \in \mathcal { I } _ { t } } \frac { 1 } { w _ { t } } = m .
$$

It remains only to prove that the greedy algorithm selects these representatives.

By assumption (i), the vertices in $D$ are already clustered to representatives before they can become roots. We may therefore analyze the subsequent block choices on the active reduced family $B ^ { \cdot }$

Proceed in the actual order in which the algorithm reaches unresolved blocks. Fix such a block $\mathcal { I } _ { t }$ . Since $I _ { - } ( z _ { t } ) = \mathcal { T } _ { t }$ in the reduced family and every reduced block in $\mathcal { T } _ { t }$ has layer $w _ { t } ,$ the representative $z _ { t }$ cannot be removed by a smaller-weight outside block. Previously resolved block representatives have supports equal to their own disjoint blocks, so they do not nullify blocks in $\mathcal { T } _ { t }$ Hence $z _ { t }$ is active when the first surviving block $B _ { i } ^ { - }$ with $i \in \mathcal { I } _ { t }$ is processed in layer $w _ { t }$

Let $u \in M ( B _ { i } ^ { - } )$ with $u \neq z _ { t }$ . If condition (iii.a) holds for $u ,$ then there exists $j \in I _ { - } ( u ) \setminus \mathcal { T } _ { t }$ with $w _ { - } ( B _ { i } ^ { - } ) < w _ { t }$ . That lower-weight block is processed before layer $w _ { t }$ . Since $u \in B _ { j } ^ { - }$ but $d _ { - } ( u ) = w _ { t } > w _ { - } ( B _ { j } ^ { - } )$ , vertex u cannot be chosen as the root of $B _ { j } ^ { - }$ ; when $B _ { j } ^ { - }$ is processed, all currently unassigned vertices in the absorbed block collection containing $B _ { j } ^ { - }$ are clustered to the root chosen there, so u is removed before layer $w _ { t }$ begins. Thus any degree-w<sub>t</sub> competitor still present in $M ( B _ { i } ^ { - } )$ must satisfy condition (iii.b), and hence loses to $z _ { t }$ under the current root-selection key.

Therefore the algorithmic choice

$$
u ^ { \star } \in \arg \operatorname* { m i n } _ { v \in M ( B _ { i } ^ { - } ) } \big ( c _ { w _ { t } } ( v ) , \mathrm { i d } ( v ) \big )
$$

must select $\boldsymbol { u } ^ { \star } = \boldsymbol { z } _ { t }$ . The associated-block update for $z _ { t }$ nullifies exactly the blocks indexed by $\mathcal { T } _ { t }$ and no second root can later be chosen from that block.

Repeating this argument until all blocks have been resolved shows that the only roots created are $z _ { 1 } , \ldots , z _ { m }$ . Thus $R _ { G } = \{ z _ { 1 } , \dots , z _ { m } \}$ , and the displayed equality follows from the first paragraph.

The governing invariant is the following.

Lemma 2 (block-root uniqueness invariant). At every step of the algorithm, no block contains more than one active root.

Proof. Initially there are no roots, so the invariant holds. During the weight-1 pass, exactly one root is assigned in each processed block. During a later layer-w step, once a root $u ^ { \star }$ is chosen, every block in $\mathcal { T } _ { w } ( u ^ { \star } )$ is immediately clustered to $u ^ { \star }$ and nullified. Hence no later step can create a second active root in any block already containing $u ^ { \star }$ . No other operation increases the number of active roots in a surviving block beyond one. □

By construction of the weight-1 preclusion pass, every $B \in B _ { 1 }$ is assigned a degree-1 root before layered greedy processing starts.

Theorem 10 (Feasibility of greedy output). The set of active roots returned by the Greedy Layered Clustering Algorithm is feasible.

Proof. By the block-root uniqueness invariant, each block contains at most one active root at termination. Since the retained set is exactly the set of active roots, it satisfies the feasibility condition. □

Let $R _ { G }$ denote the final active-root set returned by the Greedy Layered Clustering Algorithm.

Proposition 5 (Maximality of greedy output). $R _ { G }$ is a maximal feasible set: for every $x \in V \backslash R _ { G }$ 2 the set $R _ { G } \cup \{ x \}$ is infeasible.

Proof. Every non-root vertex is attached during processing to a block that already contains an active root, or is attached to a newly created root in that block. Hence for each $x \notin R _ { G }$ , there exists a block $B _ { x } \in B$ with $x \in B _ { x }$ and $B _ { x } \cap R _ { G } \neq \emptyset$ . Adding x would create at least two retained elements in $B _ { x } ,$ , violating feasibility. □

The next result records a generic component-baseline consequence of maximality.

For any $R \subseteq V$ , write

$$
\mathcal { B } | _ { R } : = \{ B \cap R : \ B \in \mathcal { B } , \ B \cap R \neq \emptyset \}
$$

for the induced block family. Let $m$ be the number of connected components of the block-overlap graph $G _ { B }$

Proposition 6 (Maximal feasible sets meet every component). For any feasible set $R \subseteq V$ , every induced block in $| \boldsymbol { B } | _ { R }$ has size at most one. $H ,$ in addition, R is maximal feasible, then R contains at least one vertex in every connected component $\kappa _ { i }$ of $G _ { B }$ . Consequently,

$$
| R | \geq m .
$$

In particular, the greedy layered clustering output $R _ { G }$ has size at least the number of block-overlap components.

Proof. If R is feasible, then by definition $| R \cap B | \leq 1$ for every $B \in B _ { \mathrm { i } }$ , so every induced block B ∩ R has size at most one.

Now assume that R is maximal feasible. Let $\boldsymbol { \mathcal { K } } _ { 1 } , \ldots , \boldsymbol { \mathcal { K } } _ { m }$ be the connected components of $G _ { B }$ with corresponding vertex domains $V _ { i } = \cup _ { B \in \kappa _ { i } } B$ . These domains are pairwise disjoint: otherwise a vertex shared by two domains would belong to blocks in two diferent components, creating an overlap edge between them. If some component domain $V _ { i }$ contained no retained vertex, then for any $x \in V _ { i }$ the set $R \cup \{ x \}$ would remain feasible: blocks outside $\kappa _ { i }$ are disjoint from $V _ { i }$ , and blocks inside $\kappa _ { i }$ contain no element of R. This contradicts maximality. Hence every component contributes at least one retained vertex, so $| R | \geq m$ . Applying this with $R = R _ { G }$ gives the final claim, since $R _ { G }$ is feasible and maximal by the previous two results. □

The result explains why component contraction is a weak baseline for the strong-independence objective. In the minimal chain

$$
B _ { 1 } = \{ a , b \} , \qquad B _ { 2 } = \{ b , c \} ,
$$

the feasible set $R = \{ a , c \}$ induces only singleton blocks,

$$
B _ { 1 } \cap R = \{ a \} , \qquad B _ { 2 } \cap R = \{ c \} .
$$

The overlap path through b is a property of the constraint representation, not a reason to forbid selecting both a and c.

The next proposition records the first-order reason for the layer schedule: lower block weight means larger contribution to the closed-form obstruction.

Proposition 7 (Upper-bound alignment). $I f w ( B _ { 1 } ) \leq w ( B _ { 2 } )$ , then block $B _ { 1 }$ contributes at least as much to the block-weight upper bound as $B _ { 2 }$ :

$$
{ \frac { 1 } { w ( B _ { 1 } ) } } \geq { \frac { 1 } { w ( B _ { 2 } ) } } .\tag{59}
$$

Proof. The reciprocal function is decreasing on positive block weights.

Within a fixed current layer, Theorem 6 further motivates prioritizing blocks with larger $n _ { \mathrm { m i n } } ( B )$ Thus the layered schedule first resolves the most constraining weight level and then, inside that level, the blocks with stronger minimum-weight covering pressure.

This block-first viewpoint is the decisive diference from generic hypergraph MIS methods. The latter are typically vertex-centric, whereas the certificates and greedy schedule here operate directly on the materialized incidence relation: block lists, vertex-to-block support lists, and root assignments.

## 6 Algorithmic Interpretation and Complexity

The previous section specified the greedy clustering routine and its feasibility, maximality, and conditional optimality guarantees. We now record three complementary facts: an exactness regime, an algorithm-aware upper bound induced by the greedy chronology itself, and coarse implementation costs in incidence-list units.

## 6.1 Exactness and Algorithm-Aware Bounds

A first sanity check is the decomposable case.

Proposition 8 (Exactness on disjoint block families). If the blocks are pairwise disjoint, the Greedy Layered Clustering Algorithm is optimal.

Proof. When the blocks are pairwise disjoint, the feasibility constraint decomposes block by block, and retaining one representative per nonempty block is optimal. The greedy routine processes each nonempty block once: choosing a root in one block cannot remove or constrain any vertex in another block because there is no block overlap. Thus the algorithm creates exactly one root per nonempty block, attaining the decomposed optimum. □

It is also useful to treat the closed-form value $U ( B )$ as an instance-level certificate target. Since Theorem 5 gives $\alpha ( B ) \leq U ( B )$ , if $R _ { G }$ denotes the retained set returned by the greedy algorithm, then

$$
\eta _ { G } : = \frac { | R _ { G } | } { U ( B ) }\tag{60}
$$

measures how closely the constructed feasible set approaches the explicit structural upper bound. This ratio is meaningful even when exact optimum computation is unavailable, because both $| R _ { G } |$ and $U ( B )$ are computed directly from the materialized incidence data.

The next theorem gives a finer, algorithm-aware upper bound. Whereas $U ( B )$ depends only on static block weights, this bound uses the actual layer-by-layer assignments made by Algorithm 4. For the comparison, assume $d ( v ) \geq 1$ for all $v \in V$ . Let

$$
\begin{array} { r } { \mathcal B _ { 1 } : = \{ B \in \mathcal B : w ( B ) = 1 \} , \qquad \mathcal B _ { > 1 } : = B \setminus \mathcal B _ { 1 } . } \end{array}\tag{61}
$$

Let $O ^ { \star }$ be an optimal feasible set satisfying the canonical weight-1 conclusion of Theorem 2, and write

$$
Z : = \left\{ z _ { B } : B \in B _ { 1 } \right\} \subseteq O ^ { \star } .\tag{62}
$$

For each layer $w ,$ let $R _ { w } \subseteq R _ { G }$ be the set of roots created in layer w. For each $r \in R _ { w }$ , define the associated block family consumed by that root as

$$
\begin{array} { r } { A _ { w } ( r ) : = \{ B _ { j } : j \in \mathcal { I } _ { w } ( r ) \} . } \end{array}\tag{63}
$$

For each vertex $x \in V \setminus Z$ , let $\tau ( x ) = w$ be the layer in which x is first removed from further processing by the greedy algorithm, including the case where x itself is chosen as a root in layer w. Define the layer-w witness graph

$$
G _ { w } : = \left( X _ { w } , \Gamma _ { w } , E _ { w } \right)\tag{64}
$$

with left side

$$
X _ { w } : = \{ x \in V \setminus Z : \tau ( x ) = w \} ,\tag{65}
$$

right side

$$
\Gamma _ { w } : = \{ ( r , B ) : r \in R _ { w } , B \in \mathcal { A } _ { w } ( r ) \} ,\tag{66}
$$

and edge relation

$$
x \sim ( r , B ) \quad \Longleftrightarrow \quad x \in B .\tag{67}
$$

Write $\nu ( G _ { w } )$ for the maximum matching size in $G _ { w }$

Theorem 11 (Layered witness-matching upper bound).

$$
\alpha ( \boldsymbol { B } ) \leq | \beta _ { 1 } | + \sum _ { w } \nu ( \boldsymbol { G } _ { w } ) .\tag{68}
$$

Proof. By Theorem 2, choose $O ^ { \star }$ so that $Z \subseteq O ^ { \star }$ and $\left| Z \right| = \left| B _ { 1 } \right|$ . Let

$$
O ^ { + } : = O ^ { \star } \backslash Z .
$$

Then

$$
| O ^ { \star } | = | B _ { 1 } | + | O ^ { + } | .
$$

For each layer w, let

$$
O _ { w } ^ { + } : = \{ x \in O ^ { + } : \tau ( x ) = w \} .
$$

We claim that $| O _ { w } ^ { + } | \le \nu ( G _ { w } )$ for every w. Fix w and $x \in O _ { w } ^ { + }$ . By definition of $\tau ( x )$ , the vertex x is first removed in layer w when some root $r _ { x } \in R _ { w }$ is chosen and one of its associated blocks $B _ { x } \in \mathcal { A } _ { w } ( r _ { x } )$ contains x. Hence $( r _ { x } , B _ { x } ) \in \Gamma _ { w }$ and $\boldsymbol { x } \sim \left( r _ { x } , B _ { x } \right)$

Moreover $B _ { x } \in B _ { > 1 }$ . If $B _ { x } \in B _ { 1 }$ , then $z _ { B _ { x } } \in Z \subseteq O ^ { \star }$ and also $x \in B _ { x } \cap O ^ { \star }$ with $x \neq z _ { B _ { x } }$ ， contradicting feasibility of $O ^ { \star }$

The edges $\left\{ \left( x , \left( r _ { x } , B _ { x } \right) \right) : x \in O _ { w } ^ { + } \right\}$ form a matching in $G _ { w }$ . Distinct vertices of $O _ { w } ^ { + }$ are distinct on the left. They are also distinct on the right: if two distinct vertices $x , y \in O _ { w } ^ { + }$ were assigned

to the same right vertex $( r , B )$ , then $x , y \in O ^ { \star } \cap B .$ , contradicting feasibility of $O ^ { \star }$ . Therefore $| O _ { w } ^ { + } | \le \nu ( G _ { w } )$

Summing over all layers,

$$
| O ^ { + } | = \sum _ { w } | O _ { w } ^ { + } | \leq \sum _ { w } \nu ( G _ { w } ) .
$$

Thus

$$
\alpha ( \boldsymbol { B } ) = | O ^ { \star } | \leq | \mathcal { B } _ { 1 } | + \sum _ { w } \nu ( G _ { w } ) .
$$

Example 5 gives a concrete witness picture. There are no weight-1 blocks, so $Z = \varnothing$ . If the greedy roots are $x _ { 1 }$ and $x _ { 4 }$ , then the right side of $G _ { 2 }$ consists of block-root pairs consumed by those two choices, such as $( x _ { 1 } , O ) , ( x _ { 1 } , P _ { 1 } ) , ( x _ { 4 } , E )$ , and $( x _ { 4 } , P _ { 2 } )$ . Any feasible optimum can be injected into these consumed pairs by assigning each optimal vertex to a block that first removes it. The matching condition is exactly feasibility: two optimal vertices cannot be assigned to the same consumed block.

## 6.2 Complexity

The algorithms above are intended for instances where the incidence relation is materialized. Let

$$
I : = \sum _ { v \in V } d ( v ) = \sum _ { B \in B } | B |\tag{69}
$$

be the total vertex-block incidence. Let $M _ { \mathrm { r e s } } : = | \beta ^ { \mathrm { r e s } } |$ be the number of residual blocks after incidence-twin compression and weight-1 preclusion.

Proposition 9 (Preprocessing cost). Computing vertex degrees, performing weight-1 preclusion, removing clustered vertices, and initializing residual block weights require $O ( I )$ time.

Proof. Using vertex-to-block incidence lists, the initial degree computation scans each incidence once. During weight-1 preclusion, each clustered vertex class is removed from later consideration once, and afected block records are updated through their incident lists. Residual block weights are initialized by scanning the surviving residual block records. Each operation is charged to an original or surviving incidence a constant number of times, giving total work $O ( I )$ □

Proposition 10 (Layer-set initialization cost). Placing each residual block index into its initial weight set $\mathcal { S } _ { \widehat { w } ( B ) }$ requires $O ( M _ { \mathrm { r e s } } )$ time.

Proof. After residual weights have been computed, each residual block contributes exactly one index insertion into the set corresponding to its initial weight. □

Let

$$
J _ { \mathrm { r e s } } : = \sum _ { B \in \mathcal { B } ^ { \mathrm { r e s } } } \sum _ { t = 1 } ^ { k _ { B } } | B ^ { ( t ) } |\tag{70}
$$

be the total incidence scanned across all residual block states, where $k _ { B }$ is the number of times residual block B is visited and $B ^ { ( t ) }$ is its live vertex set during the t-th layer-cleaning or layerclustering pass. Let $T _ { \mathrm { i n d e x } }$ denote the total work needed to build and update the layer-local vertex-to-block indices $\mathcal { T } _ { w } ( \cdot )$ , and let $T _ { \mathrm { s c o r e } }$ denote the total work needed to evaluate the collision scores $c _ { w } ( \cdot )$ at the selected blocks.

Proposition 11 (Coarse clustering complexity). If the current $n _ { \mathrm { m i n } } ( B )$ values are maintained in an integer-keyed bucketed queue, or any equivalent priority structure supporting $O ( 1 )$ amortized decrement/update inside a fixed layer, then the layered greedy stage runs in

$$
O ( J _ { \mathrm { r e s } } + T _ { \mathrm { i n d e x } } + T _ { \mathrm { s c o r e } } )\tag{71}
$$

time.

Proof. Residual block scans contribute $J _ { \mathrm { r e s } }$ . Building and updating the layer-local degree-w incidence tables contributes $T _ { \mathrm { i n d e x } }$ . On-demand collision-score evaluations at selected blocks contribute $T _ { \mathrm { s c o r e } }$ A global within-layer sort is unnecessary: once $\mathcal { T } _ { w } ( \cdot )$ and the initial $n _ { \mathrm { m i n } } ( B )$ values are built, each decrement of a current-layer counter is triggered by deleting some degree-w vertex from the index, so every current-layer incidence is touched only when its degree-w endpoint is clustered away. Since each residual block has at least one member, the initialization term $O ( M _ { \mathrm { r e s } } )$ is absorbed by $ { \mathcal Ḋ O Ḍ } ( J _ { \mathrm { r e s } } )$ □

Equivalently, with residual scan amplification

$$
\sigma _ { \mathrm { r e s } } : = \frac { J _ { \mathrm { r e s } } } { I } ,
$$

the coarse bound is

$$
O ( \sigma _ { \mathrm { r e s } } I + T _ { \mathrm { i n d e x } } + T _ { \mathrm { s c o r e } } ) .
$$

Thus the implemented loop is incidence-local: it does not construct induced subhypergraphs, dual graphs, or neighborhood-exchange structures, and its dominant cost is the number of live incidences actually scanned during residual layer processing.

## 7 Conclusion

We formulated a structural theory for maximum strong independent set in finite hypergraphs, focused on the incidence patterns that arise when hyperedges are local incompatibility blocks rather than transitive equivalence classes. The main contributions are exact reductions, block-weight upper bounds, puncturing and covering certificates, and a layered greedy clustering algorithm with feasibility, maximality, witness-matching, complexity, and optimality guarantees under explicit incidence conditions.

The framework emphasizes that local hyperedge constraints should be handled through their incidence structure rather than collapsed into a coarser graph-level surrogate. Future work should sharpen worst-case approximation guarantees for the layered greedy algorithm, characterize when puncturing and covering certificates are tight, and develop graph-native versions of the certificate framework. Since ordinary graph MIS is the 2-uniform special case, the more substantial question is how to recover useful block-like incidence decompositions and certificate-guided greedy schedules for general graph instances whose constraints are not already presented as explicit local blocks.

## References

[1] Andrei Z. Broder. On the resemblance and containment of documents. In Proceedings of the Compression and Complexity of Sequences 1997, pages 21–29. IEEE, 1997. DOI: 10.1109/SEQUEN.1997.666900.

[2] Andrei Z. Broder. Identifying and filtering near-duplicate documents. In Combinatorial Pattern Matching (CPM 2000), volume 1848 of LNCS, pages 1–10. Springer, 2000. DOI: 10.1007/3-540-45123-4\_1.

[3] Gurmeet Singh Manku, Arvind Jain, and Anish Das Sarma. Detecting near-duplicates for web crawling. In Proceedings of the 16th International Conference on World Wide Web, pages 141–150. ACM, 2007. DOI: 10.1145/1242572.1242592.

[4] Moses S. Charikar. Similarity estimation techniques from rounding algorithms. In Proceedings of the Thirty-Fourth Annual ACM Symposium on Theory of Computing, pages 380–388. ACM, 2002. DOI: 10.1145/509907.509965.

[5] Noga Alon, Uri Arad, and Yossi Azar. Independent sets in hypergraphs with applications to routing via fixed paths. In Dorit S. Hochbaum, Klaus Jansen, José D. P. Rolim, and Alistair Sinclair, editors, Randomization, Approximation, and Combinatorial Optimization. Algorithms and Techniques (RANDOM-APPROX 1999), volume 1671 of LNCS, pages 16–27. Springer, 1999. DOI: 10.1007/978-3-540-48413-4\_3.

[6] Georg Gottlob and Gianluigi Greco. Decomposing combinatorial auctions and set packing problems. Journal of the ACM, 60(4):24:1–24:39, 2013. DOI: 10.1145/2508028.2505987.

[7] Yingquan (Cody) Wu and Jason Cong. Bucket-feasible LSH-MinHash for deduplication and query. Companion manuscript prepared for submission to the Journal of Machine Learning Research, 2026.

[8] Richard M. Karp. Reducibility among combinatorial problems. In Raymond E. Miller and James W. Thatcher, editors, Complexity of Computer Computations, pages 85–103. Plenum Press, 1972.

[9] Jack Edmonds. Paths, trees, and flowers. Canadian Journal of Mathematics, 17:449–467, 1965. DOI: 10.4153/CJM-1965-045-4.

[10] Magnus M. Halldorsson and Elena Losievskaja. Independent sets in bounded-degree hypergraphs. Discrete Applied Mathematics, 157(8):1773–1786, 2009. DOI: 10.1016/j.dam.2008.11.013.

[11] Thomas Hofmeister and Hanno Lefmann. Approximating maximum independent sets in uniform hypergraphs. In Automata, Languages and Programming (ICALP 1998), volume 1443 of LNCS, pages 562–570. Springer, 1998. DOI: 10.1007/BFb0055806.

[12] Geir Agnarsson, Magnus M. Halldorsson, and Elena Losievskaja. SDP-based algorithms for maximum independent set problems on hypergraphs. Theoretical Computer Science, 470:1–9, 2013. DOI: 10.1016/j.tcs.2012.11.025.

[13] Ernestine Großmann, Christian Schulz, Darren Strash, and Antonie Wagner. Data reductions for the strong maximum independent set problem in hypergraphs. arXiv preprint arXiv:2602.10781, 2026. DOI: 10.48550/arXiv.2602.10781.

[14] C. A. J. Hurkens and A. Schrijver. On the size of systems of sets every t of which have an SDR, with an application to the worst-case ratio of heuristics for packing problems. SIAM Journal on Discrete Mathematics, 2(1):68–72, 1989. DOI: 10.1137/0402008.

[15] Elad Hazan, Shmuel Safra, and Oded Schwartz. On the complexity of approximating k-set packing. Computational Complexity, 15(1):20–39, 2006. DOI: 10.1007/s00037-006-0205-6.

[16] Maxim Sviridenko and Justin Ward. Large neighborhood local search for the maximum set packing problem. In Proceedings of the 40th International Colloquium on Automata, Languages, and Programming (ICALP 2013, Part I), volume 7965 of LNCS, pages 792–803. Springer, 2013. DOI: 10.1007/978-3-642-39206-1\_67.

[17] Marek Cygan. Improved approximation for 3-dimensional matching via bounded pathwidth local search. In Proceedings of the 54th IEEE Annual Symposium on Foundations of Computer Science (FOCS), pages 509–518, 2013. DOI: 10.1109/FOCS.2013.61.

[18] Davi Castro-Silva, Fernando Mário de Oliveira Filho, Lucas Slot, and Frank Vallentin. A recursive theta body for hypergraphs. Combinatorica, 43:909–938, 2023. DOI: 10.1007/ s00493-023-00040-9.