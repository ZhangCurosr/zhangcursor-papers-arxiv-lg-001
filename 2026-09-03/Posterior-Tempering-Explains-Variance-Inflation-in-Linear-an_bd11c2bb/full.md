# Posterior Tempering Explains Variance Inflation in Linear and Generalized Linear Thompson Sampling

Prateek Jaiswal<sup>\*</sup> Debdeep Pati<sup>†</sup> Anirban Bhattacharya<sup>‡</sup> Bani K. Mallick<sup>§</sup>

## Abstract

We study a variant of the Thompson Sampling (TS) algorithm, called α-TS, for solving stochastic generalized linear bandit problems. Existing analyses of TS require inflating the posterior variance to derive near-optimal regret guarantees. We formalize the idea of variance inflation by introducing α- TS that uses a fractional or α-posterior instead of the standard posterior. Our main contribution is to identify general regularity conditions on the prior and reward distributions that enable a regret analysis of $\alpha { - } \mathrm { T S }$ without assuming any tractable approximation of the posterior distribution, unlike previous works. For a specific choice of $\alpha \propto d ^ { - 1 }$ , our general regret bound yields the best known regret bound of $O ( d ^ { 3 / 2 } { \sqrt { T } } \log T )$ for both the exponential and sub-Gaussian families of reward distributions. We further provide an α-dependent lower bound showing that the regret constant depends on the product αd, and that when $\alpha \propto d ^ { - 1 }$ the regret scales as $\Omega ( d ^ { 3 \bar { / } 2 } \sqrt { T } )$ , explaining the origin of the $d ^ { 3 / 2 }$ factor in the upper bound. Our proof technique adapts and combines recent advancements in the analysis of linear bandit problems with first- and second-order posterior concentration theory from the Bayesian statistics literature.

Keywords: Thompson sampling, generalized linear models, frequentist regret bounds, linear bandits, finitesample Bernstein-von Mises, posterior concentration

## 1 Introduction

The bandit framework [25] is widely used to model various sequential decision-making problems with many applications in healthcare, advertising, resource allocation, robotics, material discovery, etc. A central challenge in such problems is balancing exploration and exploitation. To solve such sequential decision-making problems, the decision-maker must balance exploration and exploitation. In particular, the decision-maker must choose between exploring less-understood actions to gain new information and exploiting existing knowledge to select the (statistically) best action. This work considers a stochastic generalized linear bandit problem with compact decision space $( \mathbb { A } \subset \mathbb { R } ^ { d } )$ and reward distribution with generalized linear mean.

Thompson Sampling (TS) [27] and optimism-in-the-face-of-uncertainty (OFU) [19] are two broad classes of algorithms to solve various versions of the bandit problem. The OFU algorithms compute a confidence set for the unknown coefficients in the reward model and then select an optimistic estimate of the coefficient and decision that maximizes the estimated outcome. TS instead uses a Bayesian heuristic (posterior distri bution) to address the exploration-exploitation dilemma. TS samples model parameters from the posterio and selects actions that are optimal under the sampled parameters. The theoretical performance of such algorithms is typically measured by computing a bound on a term called regret, which is the total sum of the loss incurred by the algorithm over past trials. At each round, regret arises from selecting a suboptimal action instead of the oracle’s optimal action. In this work, we theoretically analyze a version of TS to solve the generalized linear bandit (LB) problem.

Agrawal and Goyal [4], in their pioneering work, analyze TS for LB problems and derive a state-of-the-art regret bound of $O ( d ^ { 3 / 2 } \sqrt { T } \log T )$ , where T is the number of trials. Their regret bound is very close to the lower bound of $\Omega ( d \sqrt { T } )$ for LB derived in [11]. The other important work by [2] computes a similar upper bound by using a novel proof technique. Both works consider a TS algorithm for LB with sub-Gaussian errors; however, the sampling distribution coincides with the posterior distribution only when the errors are centered Gaussian and the prior distribution on the unknown coefficients is also Gaussian (conjugate setting). The closed-form expression of the Gaussian sampling distribution enables them to compute anti-concentration bounds; the availability of which is a crucial component for the analysis in both works. However, recall that the Bayesian heuristic in TS is more general as it is applicable for any prior and reward combination [10, 15, 20, 28]. This work contributes to further generalizing the analysis of TS with a more general reward and prior model while maintaining sampling from the Bayesian posterior rather than relying on Gaussian approximations. Our regularity conditions on the reward distributions are satisfied by both sub-Gaussian and exponential families. While the sub-Gaussian family has been extensively used in most of the previous works on LB [1–3, 11], the use of the exponential family (generalized linear model (GLM) [22]) is novel to the best of our knowledge.

We study a version of the TS algorithm, termed as $\alpha { \mathrm { - } } \mathrm { T } \mathbf { S } ,$ that uses fractional or α-posterior [7] instead of the standard posterior distribution. To compute the α-posterior distribution, the likelihood in the definition of the standard posterior distribution is tempered with a factor α. For α-TS, we compute a regret bound for any $\alpha \in ( 0 , 1 )$ , but it grows exponentially with d. However, by optimally choosing α for the exponential and sub-Gaussian families of distribution, we show that the regret bound is of $O ( d ^ { 3 / 2 } { \sqrt { T } } \log T )$ for $\alpha = d ^ { - 1 }$ Intuitively, setting α to a value in (0, 1) inflates the variance of the posterior distribution. A similar variance inflation factor is also considered in the sampling distribution (or posterior distribution) for the analysis in [3] and [2]. The $\alpha { - } \mathrm { T } \mathbf { S }$ formulation provides a principled way to formalize this variance inflation through fractional posterior sampling. Moreover, we remove the dependence on a hyperparameter $\delta \in ( 0 , 1 )$ from the TS algorithm and derive regret bounds in expectation, as conjectured by [3].

Our focus on $\alpha { - } \mathrm { T } \mathbf { S }$ is primarily motivated by theoretical considerations. In many analyses of TS for linear and contextual bandits [2, 3], the sampling distribution is effectively inflated relative to the standard posterior in order to ensure a constant probability of optimism. This inflation typically appears as an additional scaling factor in the covariance and is introduced ad hoc in the analysis. The $\alpha { - } \mathrm { T } \mathbf { S }$ formulation provides a principled way to formalize this phenomenon through fractional posteriors [7]: the tempering parameter $\alpha \in ( 0 , 1 )$ induces a posterior distribution with inflated covariance while preserving a coherent Bayesian interpretation. Importantly, $\alpha { - } \mathrm { T } \mathbf { S }$ does not introduce a new algorithmic procedure—the resulting method coincides with variance-inflated Linear Thompson Sampling (LinTS) as studied in [2, 3]. This perspective unifies existing variance-inflation heuristics within a coherent probabilistic framework. Our contribution is therefore theoretical: we provide a principled statistical interpretation of variance inflation in TS and develop a general regret analysis framework that applies under broad structural conditions on the prior and reward model.

The role of α is particularly important in the regret analysis. Without variance inflation, the posterior concentrates rapidly, making optimistic samples increasingly unlikely as the dimension grows. This leads to regret bounds with an undesirable exponential dependence on $d .$ Choosing $\alpha \leq d ^ { - 1 }$ inflates the sampling covariance sufficiently to maintain a constant probability of optimism, effectively counteracting this concentration and eliminating the exponential dependence. This yields the near-optimal $O ( d ^ { 3 / 2 } \sqrt { T } \log T )$ regret rate. We further clarify this phenomenon in the analysis and provide a complementary lower bound showing how the regret depends explicitly on the product αd through the tempering of the posterior.

A closely related work by [16] also considers α-posterior for TS, but they focus on studying the MAB problem. Also, it is unclear whether their analysis can be extended to a linear bandit setting. While the Karmed bandit can be viewed as a special case of the linear bandit obtained by restricting the action set to the canonical basis vectors, it is well known that regret guarantees for linear bandits do not automatically recover the sharp bounds in the MAB setting. This is because the analyses rely on fundamentally different structural properties: in MAB, the uncertainty of each arm decreases directly with the number of pulls, whereas in linear bandits the uncertainty is governed by the geometry of the design matrix. As discussed in [3], this mismatch prevents linear bandit analyses from recovering MAB-optimal rates. By substituting A with a Kdim basis (to convert it into a K-arm Bandit problem), our bound does not recover the bounds in [16] (same as the bounds in [3] do not recover the state-of-the-art bounds for the MAB setting). Moreover, the bounds in [16] hold for any $\alpha \in ( 0 , 1 )$ , whereas here, α must scale as $d ^ { - 1 }$ to derive optimal dimension-dependent bounds.

Our regret analysis builds on the idea of partitioning actions into saturated and unsaturated sets, as introduced in [3]. However, we provide a different set of proof arguments to derive regret bounds in expectation rather than in high probability. Computing the regret bounds requires deriving both posterior concentration and anti-concentration properties. Unlike previous works [2, 3], which derive these concentration and anti concentration properties using closed-form expressions of the sampling distribution (e.g., using Mill’s ratio for Gaussian density), we develop a more general approach. Specifically, we adapt ideas from the Bayesian statistics literature [7, 12, 23, 26, 31] to establish these properties under very general conditions on the prior and the reward model. While deriving the first-order concentration results for the α-posterior, we redefine the conventional metric (which measures the distance of a model parameter from the true parameter) to align with the metric used to measure regret in the linear bandit analysis. To derive anti-concentration properties for any reward and prior distribution without relying on closed-form expressions, we adapt finite-sample version of the second-order posterior concentration results, popularly referred to as the Bernstein-von Mises theorem, as developed in [23, 26]. Notably, our results hold under very general conditions on the prior and reward model, enabling a broad generalization of the regret analysis for α-TS.

## 2 Literature review

In addition to the works discussed in the introduction, there are various research directions that recent works have pursued for TS to solve LB problems. [14, 21] focus on improving the regret bound for TS so that it matches the lower bound of $\Omega ( d \sqrt { T } )$ . In particular, [21] proposes a new algorithm that switches between OFU and TS algorithms based on the history of observations and computes minimax optimal frequentist regret guarantees. [32] proposes Feel-Good Thompson Sampling and studies the theoretical properties of Thompson Sampling without imposing structural assumptions on the posterior sampling distribution. In particular, [32] introduces a modified likelihood in which the negative reward log-likelihood is defined as a weighted squared difference between the true reward and a sampled reward. This modified likelihood favors sampling of optimistic models from the posterior distribution and enables the author to derive regret bounds that match the lower bound for linear bandits. However, the construction in [32] relies on a finite model class where the suboptimal action produces identical rewards under all candidate models—repeatedly selecting that action yields no information and the posterior remains unchanged. This degeneracy violates the regularity conditions we impose, which require the existence of informative actions that distinguish nearby parameters and allow posterior concentration.

There is also increasing interest in using more complex reward models, such as semi-parametric [13], non-

parametric [17, 24], and high-dimensional [6, 9] reward models in contextual bandit settings.

Several recent works propose frequentist algorithms that achieve strong regret guarantees for contextual bandits [14], and [18] proposes the DDRTS algorithm for the GLM bandit setting. A key distinction between these works and ours is the structure of the action set. These results are typically developed for thefinite-arm contextual bandit setting, where regret bounds depend on the number of arms K, and the analysis exploits concentration together with union bounds over the finite action set. Our work, by contrast, considers the linear bandit formulation where the action set is infinite (e.g., a compact subset $\boldsymbol { \mathcal { X } } \subset \mathbb { R } ^ { d } )$ , requiring uniform control over an uncountable family of arms—a substantially different analytical challenge. Moreover, as emphasized above, $\alpha { - } \mathrm { T } \mathbf { S }$ does not introduce a new algorithmic procedure: it coincides with variance-inflated LinTS, and our contribution lies in the general theoretical framework it enables for analyzing posteriorsampling algorithms under structural conditions on the prior and reward model.

## 3 Problem setup

We consider a stochastic generalized linear bandit problem. We denote the arbitrary action space as $\mathbb { A } \subset \mathbb { R } ^ { d }$ and the time horizon as $T$ . For any $n \in \mathbb { N } .$ , we denote [n] to represent $\{ 1 , 2 , 3 , \ldots , n \}$ . In this problem, at each time step $t \in [ T ]$ , the learner chooses an action $\boldsymbol { a } _ { t } \in \mathbb { A }$ (according to some rule, possibly randomized) and observes a reward $r _ { t }$ as feedback from an unknown environment in response to the learner’s action. We model the environment by assuming that the reward at time t is generated from a distribution $P _ { \theta _ { 0 } } ( \cdot | a _ { t } )$ with conditional density function denoted as $p _ { 0 } ( \cdot | a _ { t } )$ . Here, $\theta _ { 0 } \in \Theta \subseteq \mathbb { R } ^ { d }$ is an unknown but fixed parameter. While interacting with the environment, the learner collects the history of observations, which we denote as $X _ { t } : = \{ a _ { s } , r _ { s } \} _ { s = 1 } ^ { t }$ . We define the filtration generated by $X _ { t }$ as $\mathcal { F } _ { t } : = \mathcal { F } _ { 0 } \cup \{ \sigma ( X _ { t } ) \}$ , that contains all the information till time t including any prior information $\mathcal { F } _ { 0 }$ . At time $t + 1$ , the learner takes an action according to a randomized action selection policy $q ( \cdot | \mathcal { F } _ { t } )$ , that is $a _ { t + 1 } \sim q ( \cdot | \mathcal { F } _ { t } )$ . We also assume that the conditional mean reward under distribution $P _ { \theta } ( \cdot | a _ { t } )$ can be represented using a strictly monotonic and Lipschitz link function $g : \mathbb { R } \mapsto \mathbb { R } \operatorname { a s } g ( a _ { t } ^ { \top } \theta )$

We evaluate a learner (or algorithm) by comparing its action at time t to the oracle best action $a _ { 0 } : =$ arg $\operatorname* { m a x } _ { a \in \mathbb { A } } g ( a ^ { \top } \theta _ { 0 } )$ . In particular, the learner’s objective is to minimize the following cumulative regret on a sample path of observations,

$$
R ( T ) = \sum _ { t = 1 } ^ { T } ( g ( a _ { 0 } ^ { \top } \theta _ { 0 } ) - g ( a _ { t } ^ { \top } \theta _ { 0 } ) ) ,\tag{1}
$$

which quantifies the total regret of taking suboptimal action $a _ { t }$ instead of the oracle’s best action $a _ { 0 }$ . We measure the algorithm’s performance by analyzing the $\mathbb { E } [ R ( T ) ]$ , where the expectation is taken with respect to the distribution that generates the sequence of observations $X _ { T - 1 }$ . Unless stated otherwise, E[·] denotes expectation with respect to the data-generating distribution.

## 3.1 α-Thompson Sampling

We study a variant of the standard Thompson sampling (TS) algorithm, where we use α-posterior instead of the standard posterior distribution $( \alpha = 1 )$ . We call this new algorithm as $\alpha { - } \mathrm { T } \mathbf { S }$ . Recall that TS was proposed by [27] to solve a multiarmed bandit problem, where the exploration-exploitation dilemma is addressed using a posterior distribution. The pseudo-code of α-TS is provided in Algorithm 1. In $\alpha { - } \mathrm { T } \mathbf { S }$ , we posit a prior distribution $\Pi ( \cdot )$ on a measurable space $( \Theta , \mathcal { Q } )$ . For any $t \geq 1$ and $\alpha \in ( 0 , 1 )$ , we define the α-posterior distribution $\Pi _ { t , \alpha } ( \cdot )$ for any measurable set $Q \in \mathcal { Q }$ as

$$
\Pi _ { t , \alpha } ( Q | \mathcal { F } _ { t } ) = \frac { \int _ { Q } \prod _ { s = 1 } ^ { t } [ p _ { \theta } ( r _ { s } | a _ { s } ) q ( a _ { s } | \mathcal { F } _ { s - 1 } ) ] ^ { \alpha } \Pi ( d \theta ) } { \int _ { \Theta } \prod _ { s = 1 } ^ { t } [ p _ { \theta } ( r _ { s } | a _ { s } ) q ( a _ { s } | \mathcal { F } _ { s - 1 } ) ] ^ { \alpha } \Pi ( d \theta ) } = \frac { \int _ { Q } \prod _ { s = 1 } ^ { t } [ p _ { \theta } ( r _ { s } | a _ { s } ) ] ^ { \alpha } \Pi ( d \theta ) } { \int _ { \Theta } \prod _ { s = 1 } ^ { t } [ p _ { \theta } ( r _ { s } | a _ { s } ) ] ^ { \alpha } \Pi ( d \theta ) } ,\tag{2}
$$

where $q ( a _ { s } | \mathcal { F } _ { s - 1 } )$ is the likelihood (or the randomized action selection rule) of choosing $a _ { s } \in \mathbb { A }$ given $\mathcal { F } _ { s - 1 }$ and $p _ { \theta } ( r _ { s } | a _ { s } )$ is the likelihood of observing $r _ { s } \mathrm { g i v e n } a _ { s }$ . Note that $a _ { s }$ is adapted to $\mathcal { F } _ { s - 1 }$ . Essentially, step 3 and 4 of Algorithm 1, combines together to sample from the randomized action selection rule $q ( a _ { s } | \mathcal { F } _ { s - 1 } )$ . In case if the action space A is a discrete set, then $q ( a _ { s } | \mathcal { F } _ { s - 1 } )$ can be precisely defined as the α-posterior probability of choosing $a _ { s }$ as the optimal action, that is equals to $\begin{array} { r } { \Pi _ { t , \alpha } \big ( \big \{ { a } _ { s } ^ { \top } \theta = \operatorname* { m a x } _ { a \in \mathbb { A } } { a } ^ { \top } \theta \big \} | \mathcal { F } _ { s - 1 } \big ) } \end{array}$ . Observe that the definition of the α-posterior distribution in equation 2 is unaffected by the probability of choosing the best action $a _ { s }$ . In the rest of the paper, we write $E _ { \alpha } [ \cdot | \mathcal { F } _ { t } ]$ for expectation with respect to the α-posterior distribution.

It is evident from Algorithm 1 that the true likelihood of generating data in α-TS is $\begin{array} { r l r } { p _ { 0 } ^ { ( T ) } ( X _ { T } ) } & { : = } & { \prod _ { t = 1 } ^ { T } \left[ p _ { 0 } ( r _ { t } | a _ { t } ) q _ { \alpha } ( a _ { t } | \mathcal { F } _ { t - 1 } ) \right] } \end{array}$ , and we denote the corresponding data-generating distribution as $P _ { 0 } ^ { ( T ) }$ till time T. The expectation of the regret is taken with respect to $P _ { 0 } ^ { ( T - 1 ) }$ For any $\theta \in \Theta$ , we also denote the reward generating distribution given a sequence of actions $a ^ { ( T ) } : = \{ a _ { 1 } , a _ { 2 } , . . . , a _ { T } \}$ as $P _ { \theta , a } ^ { ( T ) }$ and the corresponding density as $\begin{array} { r } { p _ { \theta } ^ { ( T ) } ( r ^ { ( T ) } | a ^ { ( T ) } ) : = \prod _ { t = 1 } ^ { T } p _ { \theta } ( r _ { t } | a _ { t } ) } \end{array}$ For any $\beta ~ > ~ 0$ , we denote the β-Renyi divergence between two reward densities´ $p _ { \theta } ^ { ( t ) } ( r ^ { ( t ) } | a ^ { ( t ) } )$ and $p _ { \vartheta } ^ { ( t ) } ( \boldsymbol { r } ^ { ( t ) } | a ^ { ( t ) } )$ for any $\theta , \vartheta \in \Theta$ as $\begin{array} { r l r } { D _ { \beta } ^ { ( t ) } ( \theta , \vartheta ) } & { : = } & { \frac { 1 } { \beta - 1 } \log \int p _ { \theta } ^ { ( t ) } ( r ^ { ( t ) } | a ^ { ( t ) } ) ^ { \beta } p _ { \vartheta } ^ { ( t ) } ( r ^ { ( t ) } | \bar { a } ^ { ( t ) } ) ^ { 1 - \beta } d r ^ { ( t ) } \ = \ } \end{array}$ $\begin{array} { r } { \sum _ { s = 1 } ^ { t } \frac { 1 } { \beta - 1 } \log \int p _ { \theta } ( r | a _ { s } ) ^ { \beta } p _ { \vartheta } ( r | a _ { s } ) ^ { 1 - \beta } d r ~ = : ~ \sum _ { s = 1 } ^ { t } D _ { \beta } ^ { s } ( \theta , \vartheta ) } \end{array}$ , where $\boldsymbol { r } ^ { ( t ) } : = \{ r _ { 1 } , r _ { 2 } , \ldots , r _ { t } \}$ and the second equality is due to the additivity property of α-Renyi divergence [30, Theorem 28].´

## 4 Assumptions

In this section, we provide the list of regularity conditions that we impose on the action space, prior, and the reward model to derive our general regret bound. We first assume that the action space is compact, which is a standard assumption in the linear bandit literature [2, 3, 11].

Assumption 1 (Action space). We assume the A is a closed and bounded subset of $\mathbb { R } ^ { d } .$ . In particular, without loss of generality, we assume that for any $a \in \mathbb { A } , \| a \| \leq 1$ , where $\| \cdot \|$ is the Euclidean norm.

We also use $\| \cdot \|$ to denote the operator norm if used around a matrix. We prove our regret bounds under some mild regularity conditions on the reward and prior distributions. The first assumption is on the link function g(·).

Algorithm 1 α-Thompson Sampling (α-TS)   
Set $X _ { 0 } = \{ \} , g ( \cdot )$ , and prior distribution Π on Θ   
for time step $t = 1 , 2 , \dots T$ do   
Update α-posterior $\Pi _ { t , \alpha } ( \cdot | \mathcal { F } _ { t - 1 } )$   
Generate a sample $\theta _ { t } \sim \Pi _ { t , \alpha } ( \cdot | \mathcal { F } _ { t - 1 } )$   
Compute an optimal action $a _ { t } = \arg \operatorname* { m a x } _ { a \in \mathbb { A } } g ( a ^ { \top } \theta _ { t } )$   
Observe $r _ { t }$ from $p _ { 0 } ( \cdot | \mathcal { F } _ { t - 1 } , a _ { t } )$   
Update $X _ { t } = X _ { t - 1 } \cup \{ a _ { t } , r _ { t } \}$   
end for

Assumption 2 (Link function). The link function is Lipschitz continuous and strictly monotonic, that is, $f o r a n y x , y \in \mathbb { R }$ , there exists a constant $C _ { g } > 0 ,$ , such that $| g ( x ) - g ( y ) | \leq C _ { g } | x - y |$ , and there exists an $m > 0$ such that $g ^ { \prime } ( x ) > m$

In many works on LB, it is common to assume that the rewards are generated from the following linear model: $r _ { t } = a _ { t } ^ { \top } \theta _ { 0 } + \eta _ { t }$ , where $\eta _ { t }$ is a zero mean sub-Gaussian error. Note that for such models, the link function is identity. Moreover, for exponential family reward distributions (defined in Definition 3 formally), the link function is a standard terminology that defines the mean of the respective distribution in terms of its parameters.

Next, we impose another technical condition on the reward distribution that can be shown to be easily satisfied by both sub-Gaussian (Lemma 8) and exponential (Lemma 4) family distributions.

Assumption 3 (Renyi divergence). At any time t, given $a ^ { ( t ) }$ , we assume that for any $\theta , \vartheta \in \Theta$ and for some constant $\begin{array} { r } { C > 0 , \ : | a _ { t } ^ { \top } \theta - a _ { t } ^ { \top } \vartheta | \leq C \sqrt { \frac { 2 } { \alpha } D _ { \alpha } ^ { t } ( \theta , \vartheta ) } } \end{array}$

Regularity conditions for α-posterior contraction. The following regularity condition is a joint condi tion on the prior and the reward-generating distribution, which is crucial for obtaining near optimal minimax rate of convergence of the α-posterior distribution.

Assumption 4 (Prior thickness). We assume that for a fixed $\alpha \in ( 0 , 1 )$ and a positive sequence $\{ \epsilon _ { t } \} _ { t \ge 0 } .$ there exist a $t _ { 0 } \geq 1$ , such that for all $t \geq t _ { 0 } , t \epsilon _ { t } ^ { 2 } \geq 2 . 4 /$ α and $\Pi ( B \left( \theta _ { 0 } , \epsilon _ { t } \right) ) \ge e ^ { - \frac { D \alpha t \epsilon _ { t } ^ { 2 } } { 4 } }$ , where $B \left( \theta _ { 0 } , \epsilon _ { t } \right) : =$ $\begin{array} { r } { \left\{ \theta \in \Theta : D _ { 2 } ^ { ( t ) } ( \theta _ { 0 } , \theta ) \leq \frac { D \alpha t \epsilon _ { t } ^ { 2 } } { 4 } \right\} } \end{array}$ is a neighborhood of $\theta _ { 0 }$ defined for any given $a ^ { ( t ) }$ and a positive constant $\dot { D } .$

The above assumption requires that, for any action sequence, the prior distribution places an exponentially decaying mass to a shrinking neighborhood of the true model parameter $\theta _ { 0 }$ . The neighborhood $B \left( \theta _ { 0 } , \epsilon _ { t } \right)$ of $\theta _ { 0 }$ is defined using a 2-Renyi divergence measure´ $D _ { 2 } ^ { ( t ) } ( \theta _ { 0 } , \theta )$ . Such type of assumptions are common in the works studying non-asymptotic convergence rate of the Bayesian posteriors [7, 12, 31] and are satisfied by a large class of prior-likelihood combination for both parametric and non-parametric problems. Typically, $\epsilon _ { t }  0$ as $t  \infty$ and determines the rate of convergence of the α-posterior distribution. We will later see in Lemma 1 that this assumption enables us to construct (near) minimax optimal rate of convergence of the α-posterior distribution that is required for computing the bounds on the regret of $\alpha { - } \mathrm { T } \mathbf { S }$ . In particular, observe that if $\epsilon _ { t }$ satisfies Assumption 4, then any $\epsilon _ { t } ^ { \prime } \geq \epsilon _ { t }$ satisfies it, and thus $\epsilon _ { t }$ is optimal. A common recipe that we follow to show this assumption is to locally bound the 2-Renyi divergence term by´ $t \| \theta - \theta _ { 0 } \| ^ { 2 }$ (assuming Θ is an Euclidean space) and then appropriately control the rate at which prior mass of the shrinking 2-norm neighborhood decays. We show that the assumption above is satisfied by any bounded prior density with both exponential and sub-Gaussian families in Lemmas 5 and $1 0 ,$ respectively. This assumption is different from a similar assumption in [16]. Their assumption depends on the instance gap, which does not hold for an arbitrarily small instance gap. We also impose another regularity condition on the prior density.

Assumption 5 (sub-Gaussian prior)We assume that . $\begin{array} { r } { \int _ { \Theta } \Pi ( d \theta ) e ^ { \frac { \gamma } { 2 } \| \theta \| ^ { 2 } } < C _ { \pi } } \end{array}$ for some $\gamma \leq 1$

The assumption above can be easily satisfied by any sub-Gaussian prior distribution.

Regularity conditions for the finite sample Bernstein-von Mises for α-posterior distribution. In this section, we specify the regularity conditions developed in [23, 26] for finite sample Bernstein-von Mises $( \mathrm { B v M } )$ theorem to hold for parametric models. Given $a ^ { ( t ) }$ , we denote the log-likelihood of the observed rewards $r ^ { ( t ) }$ as $\begin{array} { r } { \mathbb { L } ( \theta ) = \sum _ { s = 1 } ^ { t } \log p _ { \theta } ( r _ { s } | a _ { s } ) } \end{array}$ , where the dependence on $r ^ { ( t ) }$ is implicit in the notation.

$\nabla \mathbb { L } ( \theta )$ denotes the gradient of $\mathbb { L } ( \cdot )$ evaluated at $\theta$ and $\nabla ^ { 2 } \mathbb { E } _ { a } \mathbb { L } ( \theta )$ stands for the Hessian of the expected log-likelihood, where $\mathbb { E } _ { a } [ \cdot ] : = \mathbb { E } [ \cdot | a ^ { ( t ) } ]$ is the expectation with respect to $P _ { 0 , a } ^ { ( t ) } ,$ conditioned on the action sequence $a ^ { ( t ) }$ . Define $\mathbb { D } _ { 0 } ^ { 2 } : = - \nabla ^ { 2 } \mathbb { E } a \mathbb { L } ( \theta _ { 0 } )$ . Note that $\mathbb { D } _ { 0 } ^ { 2 }$ is defined akin to the Fisher information matrix of $P _ { \theta , a } ^ { ( t ) }$ at $\theta _ { 0 }$ . Also define $\mathbb { D } _ { 0 } ^ { 2 } ( \theta ) : = - \nabla ^ { 2 } \mathbb { E } _ { a } \mathbb { L } ( \theta )$ and note that $\mathbb { D } _ { 0 } ^ { 2 } = \mathbb { D } _ { 0 } ^ { 2 } ( \theta _ { 0 } )$ . We also denote by $\mathbb { D } _ { 0 }$ the positive definite square root of $\mathbf { \dot { \mathbb { D } } _ { 0 } ^ { 2 } }$ . Denote the maximum likelihood estimator as $\widehat { \theta } _ { t } : = \arg \operatorname* { m a x } _ { \theta \in \Theta } \mathbb { L } ( \theta )$ and $\theta _ { 0 } = \arg \operatorname* { m a x } _ { \theta \in \Theta } \mathbb { E } _ { a } [ \mathbb { L } ( \theta ) ]$ ]. The stochastic part of the log-likelihood is denoted as $\zeta ( \theta ) : = \mathbb { L } ( \theta ) - \mathbb { E } [ \mathbb { L } ( \theta ) ]$ The regularity conditions required for the finite sample BvM to hold are divided into local and global. The local conditions only describe the properties of $\mathbb { L } ( \boldsymbol { \theta } )$ for $\theta \in \Theta ( \mathbf { r } _ { 0 } )$ with some fixed value $\mathbf { r } _ { 0 } .$ , where $\Theta _ { 0 } ( \mathbf { r } _ { 0 } ) : = \big \{ \theta \in \Theta \colon \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \| \le { \mathbf { r } } _ { 0 } \big \}$ . The global conditions have to be fulfilled on the whole $\Theta .$ These conditions are constructed to replace the celebrated local asymptotic normality (LAN) condition [29, Chapter 7] on the $\mathbb { L } ( \boldsymbol { \theta } )$ , which is required for the asymptotic Bernstein-von Mises theorem. Recall that LAN is essentially a local quadratic approximation of $\mathbb { L } ( \boldsymbol { \theta } )$ in the vicinity of $\theta _ { 0 }$   
Assumption 6. We start with some exponential moment conditions.

(ED<sub>0</sub>) There exists a constant $\nu _ { 0 } > 0 ;$ , and a constant $\mathbf g > 0$ such that sup $\gamma \in \mathbb { R } ^ { d }$ log $\begin{array} { r } { \mathbb { E } _ { a } \exp \left\{ \mathbf { m } \frac { \left. \nabla \zeta ( \theta _ { 0 } ) , \gamma \right. } { \left\| \mathbb { D } _ { 0 } \gamma \right\| } \right\} \leq } \end{array}$ $\frac { \nu _ { 0 } ^ { 2 } \mathbf { m ^ { 2 } } } { 2 } , \forall \mathbf { m } : | \mathbf { m } | \leq \mathbf { g } .$

(ED<sub>1</sub>) There are constants $\omega > 0$ and for each $\mathbf { r } > 0$ a constant $\mathbf { g } ( \mathbf { r } ) > 0$ such that for all $\theta \in \Theta _ { 0 } ( \mathbf { r } )$ $\begin{array} { r } { \operatorname* { s u p } _ { \gamma _ { 1 } , \gamma _ { 2 } \in \mathbb { R } ^ { d } } \operatorname* { s u p } _ { \theta \in \Theta _ { 0 } ( \mathbf { u } ) } \log \mathbb { E } _ { a } \exp \Big \{ \frac { \mathbf { m } } { \omega } \frac { \gamma _ { 1 } ^ { \top } \nabla ^ { 2 } \zeta ( \theta ) \gamma _ { 2 } } { \| \mathbb { D } _ { 0 } \gamma _ { 1 } \| \| \mathbb { D } _ { 0 } \gamma _ { 2 } \| } \Big \} \leq \frac { \nu _ { 0 } ^ { 2 } \mathbf { m } ^ { 2 } } { 2 } , \ \forall \mathbf { m } : | \mathbf { m } | \leq \mathbf { g } ( \mathbf { r } ) . } \end{array}$

(L<sub>0</sub>) There exists a constant $\delta ( \mathbf { r } )$ such thatfor all $\theta \in \Theta _ { 0 } ( \mathbf { r } )$ and for all $\begin{array} { r } { \mathbf { r } \leq \mathbf { r } _ { 0 } , \left\| \mathbb { D } _ { 0 } ^ { - 1 } \mathbb { D } _ { 0 } ^ { 2 } ( \theta ) \mathbb { D } _ { 0 } ^ { - 1 } - I _ { d } \right\| \leq } \end{array}$ $\delta ( \mathbf { r } )$

The following is the required global condition.

(Lr) For any $\mathbf { r } > 0$ there exists a value $\mathbf { b } ( \mathbf { r } ) > 0 ,$ , such that ${ \bf r } b ( { \bf r } )  \infty a s { \bf r }  \infty a n d - \mathbb { E } \mathbb { L } ( \theta , \theta _ { 0 } ) \geq$ $\begin{array} { r l } { \mathbf { r } ^ { 2 } b ( \mathbf { r } ) } & { { } f o r a l l \theta \ : w i t h \ : \mathbf { r } = \left\| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \right\| . } \end{array}$

At a very high level, the conditions $( E D _ { 0 } )$ and $\left( E D _ { 1 } \right)$ are the exponential moment conditions that essentially require the tails of the reward distribution to be exponentially decaying. Moreover, $( \mathcal { L } _ { 0 } )$ imposes a local identifiablity condition, that ensures that $- \mathbb { E } _ { a } [ \mathbb { L } ( \theta ) - \mathbb { L } ( \theta _ { 0 } ) ]$ are bounded above and below by a quadratic function of $\lVert \theta - \theta _ { 0 } \rVert . \ ( { \mathcal { L } } \mathbf { r } )$ is a global identifiability condition that requires the gap $- \mathbb { E } _ { a } [ \mathbb { L } ( \theta ) - \mathbb { L } ( \theta _ { 0 } ) ]$ grows in a controlled fashion as ${ \bf r } = \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \|$ increases. More, discussion regarding these assumptions can be found in Section 2 of [26] and partly in [23].

Note that Assumption 4, which is needed to derive first-order concentration properties of the α-posterior distribution, effectively requires no regularity condition on the reward distribution. However, to conduct a more refined second-order BvM-type analysis, a more comprehensive set of local and global moment and identifiability conditions is required. In Section 7, we show that the exponential (Definition 3) and sub-Gaussian (Definition 4 with some mild smoothness conditions on the density of the error distribution) families satisfy all the conditions in Assumption 6. More examples can be found in [23, 26].

To extend the lower bound computed in [23, Theorem 4] to general priors, we need to control the prior density on the set $\Theta _ { 0 } ( { \bf r } _ { 0 } )$ . In particular, observe that $\begin{array} { r }  \Pi _ { t , \alpha } ( \Theta _ { 0 } ( \mathbf { r } _ { 0 } ) | F _ { t } ) = \frac { \int _ { \Theta } \exp \big \{ \alpha \mathbb { L } ( \theta , \theta _ { 0 } ) \big \} \Pi ( d \theta ) \mathbf { 1 } \big \{ \theta \in \Theta _ { 0 } ( \mathbf { \bar { r } } _ { 0 } ) \big \} } { \int _ { \Theta } \exp \big \{ \alpha \mathbb { L } ( \theta , \theta _ { 0 } ) \big \} \big \} \Pi ( d \theta ) } \end{array}$ Note, that $\Theta _ { 0 } ( \mathbf { r } _ { 0 } ) = \big \{ \theta \in \Theta \colon \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \| \le { \mathbf { r } _ { 0 } } \big \}$ . So for a fixed $\mathbf { r } _ { 0 } ,$ , the prior density $( \pi ( \theta ) : = \Pi ( d \theta ) )$ can easily be lower bounded on $\Theta _ { 0 } ( { \bf r } _ { 0 } )$ by fixed number that depends on $\mathbf { r } _ { 0 }$ and $\theta _ { 0 }$ . In fact, if $\textstyle \mathbf { r } _ { 0 } = { \frac { c } { \sqrt { t } } }$ , for some constant $c > 0$ , then note that mi $\begin{array} { r } { \mathrm { \mathrm { n } } _ { \theta \in \Theta _ { 0 } ( \mathbf { r } _ { 0 } ) } \pi ( \theta ) \ge \mathrm { m i n } _ { \theta \in \Theta _ { 0 } ( c ) } \pi ( \theta ) } \end{array}$ , because, $\Theta _ { 0 } ( \mathbf { r } _ { 0 } ) \subseteq \Theta _ { 0 } ( c )$ for all $t \geq 1$ . Therefore,

Assumption 7 (Prior). We makefollowing assumption on the prior distribution.

1. The prior density is continuous on Θ.

2. There exists a positive constant M, such that the prior density, $\pi ( \theta ) \leq \mathbf { M }$ for all $\theta \in \Theta$

3. For any compact set $K \subset \Theta , \pi ( \theta ) > 0 f o r a l l \theta \in K .$

As our focus is solely on the lower bound outcome described in [23], we observe that, under the stated assumption, the outcome in Theorem 4 of [23] readily follows with a factor denoted by $\begin{array} { r } { \frac { \operatorname* { m i n } _ { \theta \in \Theta _ { 0 } ( c ) } \pi ( \theta ) } { \mathbf { M } } : = \mathbb { C } } \end{array}$ For the parametric problems of interest in this study, the two conditions (stated in Assumptions 4 and 7) imposed on the prior distribution are quite lenient, merely necessitating the prior density to be positive, continuous, and bounded. However, Assumption 5 requires the prior distribution to have sub-Gaussian tails.

## 5 Lower Bound for $\alpha { \mathbf { - } } { \mathbf { T } } { \mathbf { S } }$

We present a lower bound construction illustrating how the regret of α-TS depends on the dimension d and the tempering parameter α. The construction extends the classical hypercube instance used in linear bandit lower bounds.

Proposition 1. Consider the linear bandit model with action set $\mathbb { A } = [ - 1 , 1 ] ^ { d }$ , and rewards $r _ { t } = a _ { t } ^ { \top } \theta _ { 0 } +$ $\begin{array} { r l } { \eta _ { t } , } & { { } \ \eta _ { t } \sim \mathcal { N } ( 0 , 1 ) } \end{array}$ . Let the parameter satisfy $\theta _ { 0 } \in \{ \pm \mu \} ^ { d }$ . for some $\mu > 0$ Then the regret of α-TS with standard Gaussian prior satisfies

$$
{ \mathbb { E } } [ R _ { T } ] \geq 2 \mu d \sum _ { t = 1 } ^ { T } { \mathbb { E } } \left[ \Phi \left( - \frac { \vert \mu _ { t , i } ^ { ( \alpha ) } \vert } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } \right) \right] ,
$$

where $\mu _ { t } ^ { \alpha }$ and $B _ { t } ^ { \alpha }$ denote the α-posterior mean and precision matrix. Moreover, the signal-to-noise ratio satisfies $\begin{array} { r l r } { \frac { | \mu _ { t , i } ^ { ( \alpha ) } | } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } } & { { } \le } & { \mu \sqrt { 1 + \alpha t } + \mu \sqrt { d } + \sqrt { \alpha } | Z _ { t , i } | } \end{array}$ , where $Z _ { t , i }$ is a conditionally centered Gaussian random variable with variance at most 1.

The proof of the above proposition is provided in Appendix B. Proposition 1 characterizes the quantity controlling the probability of a coordinate sign error under $\alpha { - } \mathrm { T } \mathbf { S }$ . In particular, the error probability is governed by the signal-to-noise ratio $\frac { | \mu _ { t , i } ^ { ( \alpha ) } | } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } }$ , which determines the probability that the sampled parameter disagrees with the sign of the true coordinate. Using the bounds derived in the proof, this ratio satisfies $\begin{array} { r l r } { \frac { | \mu _ { t , i } ^ { ( \alpha ) } | } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } } & { { } \le } & { \mu \sqrt { 1 + \alpha t } + \mu \sqrt { d } + \sqrt { \alpha } | Z _ { t , i } | } \end{array}$ , where $Z _ { t , i }$ is a conditionally centered Gaussian random variable with variance at most 1. This expression reveals the key interaction between the signal magnitude $\mu ,$ the dimension $d ,$ and the tempering parameter α. The dominant deterministic component grows as $\mu { \sqrt { 1 + \alpha t } }$ , which determines the rate at which the posterior concentrates around the true parameter.

To make this dependence explicit, consider the parameter choice $\textstyle \mu = c { \sqrt { \frac { d } { T } } }$ for a constant $c > 0$ . From the first paragraph in the proof of Proposition 1, recall that the coordinate error probability can be lower bounded as $\begin{array} { r } { \mathrm { \mathbb { P } } ( a _ { t , i } \neq \mathrm { s i g n } ( \mathbf { \dot { \theta } } _ { 0 , i } ) ) ~ \geq ~ \mathbb { E } \bigg [ \Phi \bigg ( - \frac { | \mu _ { t , i } ^ { ( \alpha ) } | } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } \bigg ) \bigg ] } \end{array}$ . Let $E _ { t , i } = \{ | Z _ { t , i } | \leq u \}$ . Conditioning on this event and using the law of total expectation, we obtain

$$
\begin{array} { r l r } {  { \mathbb { P } ( a _ { t , i } \neq \mathrm { s i g n } ( \theta _ { 0 , i } ) ) \geq \mathbb { E } [ \Phi ( - \frac { \vert \mu _ { t , i } ^ { ( \alpha ) } \vert } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } ) \mathbf { 1 } \{ E _ { t , i } \} ] } } \\ & { } & { = \mathbb { P } ( E _ { t , i } ) \mathbb { E } [ \Phi ( - \frac { \vert \mu _ { t , i } ^ { ( \alpha ) } \vert } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } ) \Bigg \vert E _ { t , i } ] . } \end{array}
$$

On the event $E _ { t , i } ,$ , we have the deterministic upper bound $\begin{array} { r l r } { \frac { | \mu _ { t , i } ^ { ( \alpha ) } | } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } } & { \le } & { c \sqrt { \frac { d } { T } + \alpha d } + c \frac { d } { \sqrt { T } } + \sqrt { \alpha } u . } \end{array}$ Since the Gaussian CDF $\Phi ( - x )$ is decreasing in $x ,$ this implies that, on $\begin{array} { r l } { E _ { t , i } , \Phi \Bigg ( - \frac { | \mu _ { t , i } ^ { ( \alpha ) } | } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } \Bigg ) } & { { } \geq } \end{array}$ $\begin{array} { r } { \Phi \biggl ( - c \sqrt { \frac { d } { T } + \alpha d } - c \frac { d } { \sqrt { T } } - \sqrt { \alpha } u \biggr ) } \end{array}$ . Taking expectation conditional on $E _ { t , i }$ yields the same lower bound, and therefore

$$
\mathbb { P } ( a _ { t , i } \neq \operatorname { s i g n } ( \theta _ { 0 , i } ) ) \geq \mathbb { P } ( | G | \leq u ) \Phi \left( - c \sqrt { \frac { d } { T } + \alpha d } - c \frac { d } { \sqrt { T } } - \sqrt { \alpha } u \right) ,
$$

where $G ~ \sim ~ \mathcal { N } ( 0 , 1 )$ For sufficiently large $T ,$ the second term in the argument of Φ becomes negligible, and the coordinate error probability is therefore bounded below by a constant multiple of $\Phi ( - c \sqrt { \alpha d } )$ Substituting this bound into the regret decomposition yields $\begin{array} { r l r } { \mathbb { E } [ R _ { T } ] } & { { } \ge } & { d \mu T \mathbb { P } ( | G | \le } \end{array}$ u) $\begin{array} { r } { \Phi \left( - c \sqrt { \alpha d } - c \frac { d } { \sqrt { T } } - \sqrt { \alpha } u \right) } \end{array}$ . Using $\mu = c \sqrt { d / T }$ , we obtain

$$
\mathbb { E } [ R _ { T } ] \ge c d ^ { 3 / 2 } \sqrt { T } \mathbb { P } ( | G | \le u ) \Phi \left( - c \sqrt { \alpha d } - c \frac { d } { \sqrt { T } } - \sqrt { \alpha } u \right) .
$$

This expression makes the dimension–temperature interaction explicit. In particular, when αd remains bounded by a constant and T is sufficiently large, the Gaussian tail term remains bounded away from zero, and the regret scales as $\mathbb { E } [ R _ { T } ] = \Omega ( d ^ { 3 / 2 } \sqrt { T } )$ . Thus the $d ^ { 3 / 2 }$ dependence appearing in the upper bound cannot be avoided for this class of posterior-sampling algorithms. We formally write the outcome of the discussion above in the following proposition (without proof).

Proposition 2 (Dimension–temperature lower bound). Suppose the conditions of Proposition 1 hold and consider the parameter choice $\begin{array} { r } { \mu = c \sqrt { \frac { d } { T } } f o r } \end{array}$ a constant $c > 0$ . Then for any $u > 0$ the expected regret satisfies

$$
\mathbb { E } [ R _ { T } ] \ge c d ^ { 3 / 2 } \sqrt { T } \mathbb { P } ( | G | \le u ) \Phi \left( - c \sqrt { \frac { d } { T } + \alpha d } - c \frac { d } { \sqrt { T } } - \sqrt { \alpha } u \right) .
$$

In particular, if αd $= O ( 1 )$ and $d = o ( \sqrt { T } )$ , then $\mathbb { E } [ R _ { T } ] = \Omega ( d ^ { 3 / 2 } \sqrt { T } )$

More generally, the bound shows that the regret constant depends on the product αd through the factor $\Phi ( - c \sqrt { \alpha d } )$ , illustrating how the tempering parameter controls the effective signal-to-noise ratio of posterior sampling. Intuitively, smaller values of α inflate the posterior covariance and increase the likelihood of sampling parameters whose signs disagree with the true parameter, while larger values of $\alpha$ lead to faster posterior concentration and reduce the probability of coordinate errors. The above construction therefore highlights the fundamental role of the scaling of α with the dimension d in determining the regret behavior of $\alpha { - } \mathrm { T } \mathbf { S }$

## 6 Frequentist Regret Bound

In this section, we present our main result on bounding the regret of α-TS. The general bound on the expected regret of α-TS is provided below.

Theorem 1 (General regret bound). Under Assumptions 2, 3, 4, 5, 6, and 7, we show for any $\eta > 0$ and $\alpha ( 1 - \alpha ) \lambda < 1$ that

$$
\begin{array} { r l } & { \mathbb { E } [ R ( T ) ] \leq C _ { g } \left( 2 d \log \big ( 1 + \lambda ^ { - 1 } T \big ) \displaystyle \sum _ { t = 1 } ^ { T } t \epsilon _ { t } ^ { 2 } \right) ^ { 1 / 2 } \Big [ \big ( \bar { p } _ { 0 } ( d ) ( 1 - 2 e ^ { - \eta } ) \big ) ^ { - 1 / 2 } } \\ & { \quad \quad \quad \quad \quad + \big ( ( 1 - \alpha ) ^ { - 1 } ( D + 2 ) C ( \alpha , \lambda , \theta _ { 0 } ) \big ) ^ { 1 / 2 } \left( 1 + \big ( \bar { p } _ { 0 } ( d ) ( 1 - 2 e ^ { - \eta } ) \big ) ^ { - 1 / 2 } \right) \Big ] . } \end{array}\tag{3}
$$

$$
\begin{array} { r } { \bar { p } _ { 0 } ( d ) = \mathbb { C } \exp \Big ( - 2 \alpha \Delta _ { t } ( d , \eta ) - 8 e ^ { - \eta } \Big ) \left\{ \frac { C \sqrt { \alpha ( d + \eta ) } } { 1 + C ^ { 2 } \alpha ( d + \eta ) } e ^ { - C ^ { 2 } \alpha ( d + \eta ) / 2 } \right\} - e ^ { - \eta } - C ( \alpha , \lambda , \theta _ { 0 } ) 2 e ^ { - \frac { \alpha } { 4 } t _ { 0 } c _ { t _ { 0 } } ^ { 2 } } } \end{array}
$$

for some $\Delta _ { t } ( d , \eta )$ and t<sub>0</sub>, and $C ( \alpha , \lambda , \theta _ { 0 } )$ grows with $\alpha ( 1 - \alpha )$ and also depends on $\begin{array} { r l } { C _ { \pi } . } & { { } \Delta _ { t } ( d , \eta ) } \end{array}$ is a known non-randomfunction ofη,d, and t and it decreases as t increases.

The upper bound on the regret above depends on the time horizon $T$ and the dimension of the action space $d ,$ among other terms defined in the regularity conditions. In many parametric modeling instances, the term $\epsilon _ { t } ^ { 2 } \operatorname { i s } O \left( { \frac { d } { \alpha } } { \frac { \log t } { t } } \right)$ . Observe that $\begin{array} { r } { d \sum _ { t = 1 } ^ { T } t \epsilon _ { t } ^ { 2 } = O ( \frac { d ^ { 2 } } { \alpha } \sum _ { t = 1 } ^ { T } \log t ) = O ( \frac { d ^ { 2 } } { \alpha } T \log T ) } \end{array}$ , which could provide the desired upper bound that matches the lower bound in [11] for linear bandit problems with respect to both d and $T .$ . However, note that the term $\bar { p } _ { 0 } ( d )$ also depends on $d ,$ and for any $\alpha \in ( 0 , 1 )$ ), it will result in a regret bound that grows exponentially with d. To balance this, we must choose a d-dependent $\alpha .$ . Since, $\Delta _ { t } ( d , \eta )$ decreases as t increases, for $\eta = O ( d )$ , we can choose sufficiently large warm-up iterations $t _ { 1 } = \Omega ( d )$ to ensure that $\Delta _ { t } ( d , \eta ) \leq 1$ . Consequently, for both exponential (Corollary 1) and sub-Gaussian (Corollary 2) families, we choose $\alpha = d ^ { - 1 }$ and obtain the state-of-the-art regret bounds of $O ( d ^ { 3 / 2 } { \sqrt { T } } \log T )$ . For this choice of α, other α-dependent terms such as $C ( \alpha , \lambda , \theta _ { 0 } )$ and $( 1 - \alpha ) ^ { - 1 }$ do not grow with d and are bounded away by 0. Note that $\alpha = d ^ { - 1 }$ is an optimal choice of α because to nullify the exponential effect due to $\alpha ( d + \eta )$ , we must choose $\alpha = d ^ { - ( 1 + \varepsilon ) }$ for any $\varepsilon \geq 0$ which would results into an upper bound of $O ( d ^ { 3 / 2 + \varepsilon / 2 } \sqrt { T } \log T )$

As noted in the introduction, our analysis adapts the idea of constructing saturated and unsaturated sets of action introduced by [3] and combines it with the first and second-order properties of the α-posterior distribution. To present the proof sketch of the theorem above, we state the following two definitions.

Definition 1 (Precision matrix). Define $\begin{array} { r } { V _ { t } = \lambda I + \sum _ { s = 1 } ^ { t - 1 } a _ { s } a _ { s } ^ { \top } } \end{array}$ for any $\lambda > 0$ , and $\| x \| _ { A } : = { \sqrt { x ^ { \top } } }$ Ax is the matrix norm of x for any square matrix A.

This is a standard definition of the precision matrix that is derived while solving a Bayesian linear regression problem with Gaussian prior (with covariance λI) and standard Gaussian error. Next, we define the set of saturated actions.

Definition 2 (Saturated actions). Any action $a \in \mathbb { A }$ is called saturated at time t or $a \in { \mathcal { C } } _ { t }$ , where $\mathcal { C } _ { t }$ is the set of saturated actions, if $\begin{array} { r } { g ( a _ { 0 } ^ { \top } \theta _ { 0 } ) - g ( a ^ { \top } \theta _ { 0 } ) \geq C _ { g } \sqrt { t } \epsilon _ { t } \Vert a \Vert _ { V _ { \epsilon } ^ { - 1 } } } \end{array}$ and otherwise it is called unsaturated.

This definition is adapted from [3]. Here, $\epsilon _ { t }$ is the same as defined in Assumption 4, which characterizes the α-posterior contraction rate. Note that at any time t, the best action $a _ { 0 }$ is always unsaturated. Next, we provide a proof sketch of our main result, which also highlights the need for computing first and secondorder properties of the α-posterior. We would also like to remark that to derive regret bounds in expectation, we use a separate set of techniques than those used in [3] and [2].

Proof sketch of Theorem 1. Recall the definition $R ( T )$ and let $\begin{array} { r } { \bar { a } _ { t } : = \arg \operatorname* { m i n } _ { a \notin \mathcal { C } _ { t } } \left\| a \right\| _ { V _ { t } ^ { - 1 } } } \end{array}$ . Now observe that

$$
g ( a _ { 0 } ^ { \top } \theta _ { 0 } ) - g ( a _ { t } ^ { \top } \theta _ { 0 } ) = \underbrace { g ( a _ { 0 } ^ { \top } \theta _ { 0 } ) - g ( \bar { a } _ { t } ^ { \top } \theta _ { 0 } ) } _ { \mathrm { ( I ) } } + \underbrace { g ( \bar { a } _ { t } ^ { \top } \theta _ { 0 } ) - g ( a _ { t } ^ { \top } \theta _ { t } ) } _ { \mathrm { ( I I ) } } + \underbrace { g ( a _ { t } ^ { \top } \theta _ { t } ) - g ( a _ { t } ^ { \top } \theta _ { 0 } ) } _ { \mathrm { ( I I I ) } } .\tag{4}
$$

Using Definition 2, observe that the expected cumulative sum of (I) can be bounded above by $\begin{array} { r l } & { C _ { g } \left( \breve { \sum _ { t = 1 } ^ { T } } t \epsilon _ { t } ^ { 2 } \right) ^ { 1 / 2 } \left( \sum _ { t = 1 } ^ { T } \mathbb { E } [ \| \bar { a } _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } ] \right) ^ { 1 / 2 } } \end{array}$ . The expected cumulative sum of (II) can be bounded by $\begin{array} { r } { C _ { g } \left( \mathbb { E } \left[ \sum _ { t = 1 } ^ { T } \left. \bar { a } _ { t } \right. _ { V _ { t } ^ { - 1 } } ^ { 2 } \right] \right) ^ { 1 / 2 } \left( \sum _ { t = 1 } ^ { T } \mathbb { E } \left[ \Vert \theta _ { t } - \theta _ { 0 } \Vert _ { V _ { t } } ^ { 2 } \right] \right) ^ { 1 / 2 } } \end{array}$ using Assumption 2 and the fact that $\begin{array} { r l r } { g ( a _ { t } ^ { \top } \theta _ { t } ) } & { { } > } & { g ( \bar { a } _ { t } ^ { \top } \theta _ { t } ) } \end{array}$ Similarly, the expected cumulative sum of (III) can be bounded above by $\begin{array} { r l } { C _ { g } \left( \mathbb E \left[ \sum _ { t = 1 } ^ { T } \left. a _ { t } \right. _ { V _ { t } ^ { - 1 } } ^ { 2 } \right] \right) ^ { 1 / 2 } } & { { } \left( \sum _ { t = 1 } ^ { T } \mathbb E \left[ \left. \theta _ { t } - \theta _ { 0 } \right. _ { V _ { t } } ^ { 2 } \right] \right) ^ { 1 / 2 } } \end{array}$

To further simplify the bound observe that the term $\left( \mathbb { E } \left[ \sum _ { t = 1 } ^ { T } \| a _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } \right] \right) ^ { 1 / 2 }$ can be bounded by $\sqrt { 2 d \log { ( 1 + \lambda ^ { - 1 } T ) } }$ using elliptical potential lemma from [5, Lemma 3.1] and [8, Proposition 1]. Observe that quantifying a bound on $\left( \sum _ { t = 1 } ^ { T } \mathbb { E } \left[ \| \theta _ { t } - \theta _ { 0 } \| _ { V _ { t } } ^ { 2 } \right] \right) ^ { 1 / 2 }$ requires understanding the first order concentration properties of the α-posterior, that we derive in Lemma 1. Now, the only term that remains to be analyzed is E $\left[ \left. \bar { a } _ { t } \right. _ { V _ { t } ^ { - 1 } } ^ { 2 } \right]$ . Observe that E $\begin{array} { r } { \left[ \Vert a _ { t } \Vert _ { V _ { t } ^ { - 1 } } ^ { 2 } | \mathcal { F } _ { t - 1 } \right] \ \geq \ \mathbb { E } \left[ \Vert a _ { t } \Vert _ { V _ { t } ^ { - 1 } } ^ { 2 } | \mathcal { F } _ { t - 1 } , a _ { t } \not \in \mathcal { C } _ { t } \right] \Pi _ { \alpha , t } ( a _ { t } \ \notin \ \mathcal { C } _ { t } | \mathcal { F } _ { t - 1 } ) \ \geq } \end{array}$ $\lVert \bar { a } _ { t } \rVert _ { V _ { \star } ^ { - 1 } } ^ { 2 } \Pi _ { \alpha , t } ( { \bar { a } } _ { t } \ \notin \ { \mathcal C } _ { t } | { \mathcal F } _ { t - 1 } )$ , where the second inequality uses the definition of ${ { \bar { a } } _ { t } }$ and the fact that it is completely determined by $\mathcal { F } _ { t - 1 }$ . The remaining steps establish a lower bound on $\Pi _ { \alpha , t } ( a _ { t } \notin { \mathcal C } _ { t } | { \mathcal F } _ { t - 1 } )$ to upper bound E $\left[ \left. \bar { \boldsymbol { a } } _ { t } \right. _ { V _ { t } ^ { - 1 } } ^ { 2 } \right] \mathsf { b y } \mathbb { E } \left[ \left. \boldsymbol { a } _ { t } \right. _ { V _ { t } ^ { - 1 } } ^ { 2 } \right]$ upto some time-dependent term and then use elliptical potential lemma. If $g ( a _ { 0 } ^ { \top } \theta _ { t } ) > \dot { g } ( a _ { j } ^ { \top } \theta _ { t } )$ for all saturated actions, $\mathrm { i } . \mathrm { e } . \forall j \in \mathcal { C } _ { t }$ , then one of the unsaturated actions must be played because $a _ { 0 }$ is always unsaturated. Using this fact, we establish a lower bound on the probability of selecting an unsaturated action as

$$
\begin{array} { r l } & { \Pi _ { \alpha , t } \big ( a _ { t } \notin \mathcal { C } _ { t } | \mathcal { F } _ { t - 1 } \big ) \ge \Pi _ { \alpha , t } \big ( g \big ( a _ { 0 } ^ { \top } \theta _ { t } \big ) > g \big ( a _ { j } ^ { \top } \theta _ { t } \big ) , \forall j \in \mathcal { C } _ { t } | \mathcal { F } _ { t - 1 } \big ) } \\ & { \qquad \ge \Pi _ { \alpha , t } \big ( a _ { 0 } ^ { \top } \theta _ { t } > a _ { 0 } ^ { \top } \theta _ { 0 } | \mathcal { F } _ { t - 1 } \big ) - \Pi _ { \alpha , t } \big ( \| \theta _ { t } - \theta _ { 0 } \| _ { V _ { t } } \ge \sqrt { t } \epsilon _ { t } | \mathcal { F } _ { t - 1 } \big ) . } \end{array}\tag{5}
$$

To establish a lower bound on $\Pi _ { \alpha , t } ( a _ { t } \notin { \mathcal C } _ { t } | { \mathcal F } _ { t - 1 } )$ , we upper bound $\Pi _ { \alpha , t } ( \lVert { \boldsymbol { \theta } } _ { t } - { \boldsymbol { \theta } } _ { 0 } \rVert _ { V _ { t } } \geq \sqrt { t } \epsilon _ { t } | { \mathcal { F } } _ { t - 1 } )$ by using α-posterior contraction result in Lemma 1 and lower bound $\Pi _ { \alpha , t } ( a _ { 0 } ^ { \top } \theta _ { t } > a _ { 0 } ^ { \top } \theta _ { 0 } | \mathcal { F } _ { t - 1 } )$ using the lower bound of the finite sample BvM stated in Lemma 2. The detailed proof of the theorem and the associated lemmas are provided in the Appendix C.

## 6.1 First and second-order properties of the α-posterior

In this section, we present two important results that characterize the first and second-order properties of the α-posterior distribution using the regularity conditions on the prior and reward distribution in the previous section.

Lemma 1 (α-posterior contraction). Fix $\alpha \in ( 0 , 1 )$ . Under Assumptions 4 and 5 for any given sequence of actions $a ^ { ( t ) }$ that is adapted to $\mathcal { F } _ { t }$ and $t > 0$ , we have $\forall j > 0$ and $\alpha ( 1 - \alpha ) \lambda < 1$ that,

$$
P _ { 0 } \left( \Pi _ { t , \alpha } \left( \frac { \lambda \alpha } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \geq \frac { ( D + j ) \alpha } { ( 1 - \alpha ) ^ { 2 } } t \epsilon _ { t } ^ { 2 } | \mathcal { F } _ { t } \right) \right) \leq 2 C ( \alpha , \lambda , \theta _ { 0 } ) e ^ { - \frac { j \alpha } { 4 } t \epsilon _ { t } ^ { 2 } } , a n d\tag{6}
$$

$$
\mathbb { E } \left[ E _ { \alpha } \left[ \frac { \lambda \alpha } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) | \mathcal { F } _ { t } \right] \right] \leq \frac { ( D + 2 ) \alpha } { ( 1 - \alpha ) 2 } t \epsilon _ { t } ^ { 2 } + \frac { 4 C ( \alpha , \lambda , \theta _ { 0 } ) e ^ { - \frac { \alpha } { 4 } t \epsilon _ { t } ^ { 2 } } } { ( 1 - \alpha ) } .\tag{7}
$$

The first result computes a bound on the posterior probability of $\theta$ being away from $\theta _ { 0 }$ . This bound computes the rate at which the α-posterior shrinks. The second result quantifies a concentration bound in expectation. Intuitively, since, $D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } )$ scales with $t , \epsilon _ { t }$ determines the rate at which the α-posterior distribution concentrates to the truth. The proof of both results is adapted from the posterior concentration analysis in the Bayesian statistics literature.

Remark: Typical analysis of the α-posterior concentration bounds measure deviation using only $D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } )$ term. However, in the analysis of the regret, we needed to quantify a bound on the expectation of $\| \theta - \theta _ { 0 } \| _ { V _ { t } }$ where $V _ { t }$ is the precision matrix defined in Definition 1, therefore, we modify the analysis to incorporate this additional term $\lambda \lVert \boldsymbol { \theta } - \boldsymbol { \theta } _ { 0 } \rVert$ due to $V _ { t } – \mathrm { n o r m }$ . Also, it is only due to this modified definition of the discrepancy measure; we need an additional assumption (Assumption 5) on the prior that requires its tails to be sub Gaussian. Recall that $C _ { \pi }$ is the bound on the exponential moment on the prior as defined in Assumption 5.

Next, we present below the relevant part of the [23, Theorem 4].

Lemma 2 (Finite BvM). Under Assumptions $( E D _ { 0 } ) , ( E D _ { 1 } ) , ( \mathcal { L } _ { 0 } ) , ( \mathcal { L } \mathbf { r } )$ on the reward distribution, and Assumption 7 on the prior density,for any measurable set $A \subseteq \mathbb { R } ^ { d }$ and $\alpha \in ( 0 , 1 )$

$$
\Pi _ { t , \alpha } ( \mathbb { D } _ { 0 } ( \theta - \bar { \theta } _ { t } ) \in A | \mathcal { F } _ { t } ) \geq \mathbb { C } \exp \left( - 2 \alpha \Delta _ { t } ( d , \eta ) - 8 e ^ { - \eta } \right) \left\{ P ( \phi _ { d } / \sqrt { \alpha } \in A ) \right\} - e ^ { - \eta } ,\tag{8}
$$

with $P _ { 0 , a } ^ { ( t ) }$ probability of at least $1 - e ^ { - \eta }$ , where $\phi _ { d }$ is a standard Gaussian random variable in $\mathbb { R } ^ { d } , \bar { \theta } _ { t } =$ $\theta _ { 0 } + \mathbb { D } _ { 0 } ^ { - 2 } \nabla \mathbb { L } ( \theta _ { 0 } ) , \Delta _ { t } ( d , \eta )$ is a known non-random function of $\eta , d ,$ and t and it decreases as t increases. Moreover, $\| \mathbb { D } _ { 0 } ( \theta _ { 0 } - \bar { \theta } _ { t } ) \| \le C ^ { \dagger } \sqrt { d + \eta }$ with $P _ { 0 , a } ^ { ( t ) }$ -probability of at least $1 - e ^ { - \eta }$ for some positive constant $C ^ { \dagger }$

The proof of the result above is a direct consequence of [23, Theorem 4] and the result in the display (33) of [23, Theorem 9]. Analogous to the classical BvM result [29, Section 10.2], observe that $\mathbb { D } _ { 0 } ( \theta - \bar { \theta } _ { t } )$ perturbs parameter θ by ${ \bar { \theta } } _ { t } .$ , which is the first order approximation of the maximum likelihood estimate and scale it by $\mathbb { D } _ { 0 }$ which accounts for the Fisher information matrix. Intuitively, as $t  \infty$ , the result above recovers (lower limit of) the classical Bernstein-von Mises theorem. The factor α accounts for the effect of tempering the likelihood in the definition of the α-posterior distribution. In particular, it can be observed from the lower bound above that $\alpha \in ( 0 , 1 )$ inflates the posterior variance by a factor $\alpha ^ { - 1 }$ . Recall, $\Delta _ { t } ( d , \eta )$ appears in the expression for $\bar { p } _ { 0 } ( d )$ in Theorem 1. We will show in the examples that $\Delta _ { t } ( d , \eta ) \leq 1$ , for any $t \geq t _ { 1 }$

## 7 Examples

We consider two broad classes of reward distributions: the sub-Gaussian and the exponential families.   
Below, we clearly state the definitions and assumptions for these families.

## 7.1 Exponential family

First, we specify the conditions for the exponential family reward models.

Definition 3 (Exponential). We assume for $\Theta \subseteq \mathbb { R } ^ { d }$ that

i. $p _ { \theta } ( r | a _ { t } ) \ = \ e ^ { r a _ { t } ^ { \top } \theta - A ( a _ { t } ^ { \top } \theta ) + C ( r ) }$ , with conditional mean as $A ^ { \prime } ( a _ { t } ^ { \top } \theta )$ and conditional variance as $A ^ { \prime \prime } ( a _ { t } ^ { \top } \theta )$ , where $A ( \cdot )$ is the log-partition function and $h ( \cdot )$ is some known mapping. We also define the link function $g ( \cdot ) : = A ^ { \prime } ( \cdot )$ .

ii. the link function and its derivative are Lipschitz continuous, that is, for any $x , y \in \Omega , ( \mathrm { a } )$ there exists a constant $C _ { g } > 0$ , such that $| g ( x ) - g ( y ) | \leq C _ { g } | x - y |$ and (b) there exists a constant $L > 0$ , such that $| g ^ { \prime } ( x ) - g ^ { \prime } ( y ) | \leq L | x - y |$

iii. the log-partition function is strongly convex with parameter m, that is for any $\alpha \in ( 0 , 1 ) , x , y \in \Omega$ there exists an $m > 0$ , such that $x A ( x ) + ( 1 - \alpha ) A ( y ) - A ( \alpha x + ( 1 - \alpha ) y ) \geq { \frac { 1 } { 2 } } \alpha ( 1 - \alpha ) m | x - y | ^ { 2 }$

Typically, if $\operatorname* { m a x } _ { x \in \mathbb { R } } | g ^ { \prime } ( x ) | < \infty$ , then the condition $i i ( \mathrm { a } )$ above is satisfied for $C _ { g } = \operatorname* { m a x } _ { x \in \mathbb { R } } | g ^ { \prime } ( x ) |$ Moreover, when $| A ^ { \prime \prime } ( \cdot ) | \geq m > 0$ , then $A ( \cdot )$ is strongly convex with parameter $m$ . Note that conditions (ii(a)) and (iii) above follow when $| A ^ { \prime \prime } ( \cdot ) | \in [ m , C _ { g } ]$ . This condition is restrictive for exponential family distribution as it requires the variance of many exponential family distributions (such as Poisson or Exponential) to lie in a compact space. We show in the Appendix D that the above exponential family of distributions satisfies all the conditions required for the first and second-order properties of α-posterior. Consequently, we have the following corollary of Theorem 1.

Corollary 1. For the exponential family reward distributions (Definition 3), under Assumptions $4 , 5 ,$ , and 7 on the prior distribution and for some prior dependent positive constant $C _ { \phi } ,$ , we show for $\eta = O ( d )$ and $\alpha ( 1 - \alpha ) \lambda \ < \ 1$ that $\begin{array} { r } { \epsilon _ { t } ^ { 2 } = \frac { 4 d C _ { g } \log ( t ) } { D \alpha C _ { \phi } t } , \Delta _ { t } ( d , \eta ) \leq 1 } \end{array}$ for sufficiently large $t \ \geq \ t _ { 1 }$ , and for $\alpha = d ^ { - 1 }$ $\mathbb { E } [ { \pmb R } ( T ) ] = O ( d ^ { 3 / 2 } \sqrt { T } \log ( T ) )$

The expression for $\epsilon _ { t }$ is derived in Lemma 5 and $\Delta _ { t } ( d , \eta )$ is computed in Lemma 7, where we satisfy all the conditions required for the second-order analysis of α-posterior for exponential family.

## 7.2 Sub-Gaussian Family

We begin with the definition of the sub-Gaussian family and state the required conditions.

Definition 4 (Sub-Gaussian). For any $\Theta \subseteq \mathbb { R } ^ { d }$ , we assume that

i. the reward distribution is conditionally sub-Gaussian with bounded sub-Gaussian parameter $\sigma _ { \theta }$ , that is for any $t \geq 0$ , and given $a _ { t } , \mathbb { E } [ e ^ { s r _ { t } } | \dot { a } _ { t } ] \leq e ^ { s a _ { t } ^ { \top } \theta + \sigma _ { \theta } s ^ { 2 } / 2 }$ for all $s \in \mathbb { R }$

ii. $\sigma _ { \theta }$ is bounded by 1, which implies $\mathbb { E } [ e ^ { s r _ { t } } | a _ { t } ] \leq e ^ { s a _ { t } ^ { \top } \theta + s ^ { 2 } / 2 }$ for all $s \in \mathbb { R }$ , and

iii. the true mean reward $a ^ { \top } \theta _ { 0 }$ lies in [0, 1] for any $a \in \mathbb { A }$

We show in the Appendix E that the above sub-Gaussian family of distributions satisfies all the conditions required for the first and second-order properties of α-posterior. Consequently, we have the following corol lary of Theorem 1 for the sub-Gaussian family.

Corollary 2. For the sub-Gaussian family of reward distributions (Definition 4) with some additional regularity conditions on the error density, under Assumptions 4, 5, and 7 on the prior distribution and for some prior dependent positive constant $C _ { \phi }$ and a positive constant ${ \tilde { C } } ,$ we show for any $\eta \ : = \ : O ( d )$ and $\alpha ( 1 - \alpha ) \lambda < 1$ that $\begin{array} { r } { \epsilon _ { t } ^ { 2 } = \frac { 4 d \tilde { C } \log ( t ) } { D \alpha C _ { \phi } t } , \Delta _ { t } ( d , \eta ) \leq 1 } \end{array}$ for sufficiently large $t \geq t _ { 1 }$ , and for $\alpha = d ^ { - 1 }$ $\mathbb { E } [ { \pmb R } ( T ) ] = O ( d ^ { 3 / 2 } \sqrt { T } \log ( T ) )$

We derive the expression for $\epsilon _ { t }$ in Lemma 10 and for $\Delta _ { t } ( d , \eta )$ in Lemma 11, where we show that sub-Gaussian family satisfies all the conditions required for the second-order analysis of α-posterior. A similar regret bound can be computed when the mean of the reward distribution is generalized to $g ( a _ { t } ^ { \top } \theta )$ by assuming that g satisfies Assumption 2.

## 8 Conclusion

We studied α-Thompson Sampling (α-TS), a posterior-sampling algorithm for stochastic generalized lin ear bandit problems that uses a fractional or α-posterior instead of the standard posterior distribution. Our main contribution is a general frequentist regret analysis framework built on structural regularity conditions on the prior and reward model, without requiring any tractable closed-form representation of the posterior distribution. Importantly, $\alpha { - } \mathrm { T } \mathbf { S }$ does not introduce a new algorithmic procedure, but rather provides a prin cipled interpretation of the variance inflation commonly used in existing analyses of Thompson Sampling. For the optimal choice $\alpha = d ^ { - 1 }$ , our framework recovers the best known regret bound of $O ( d ^ { 3 / 2 } \sqrt { T } \log T )$ for both exponential-family and sub-Gaussian reward distributions, matching the guarantees of state-of-theart Thompson Sampling analyses. The proof technique combines ideas from the linear bandit literature with first- and second-order posterior concentration theory (including a finite-sample Bernstein-von Mises theorem), and delivers regret bounds in expectation rather than in high probability. We also provide an α-dependent lower bound construction illustrating how the regret scales with the tempering parameter α and the dimension $d ,$ showing that the $d ^ { 3 / 2 }$ dependence in the upper bound is unavoidable for this class of posterior-sampling algorithms.

Future directions. Several directions remain open. First, whether the $\sqrt { d }$ gap between the $O ( d ^ { 3 / 2 } \sqrt { T } )$ upper bound and the $\Omega ( d \sqrt { T } )$ minimax lower bound can be closed for posterior-sampling algorithms without additional structural assumptions is an important open problem. Second, extending the framework to misspecified models, non-stationary environments, or infinite-dimensional parameter spaces (e.g., Gaussian process bandits) would broaden its applicability. Third, developing computationally efficient methods for sampling from the α-posterior in non-conjugate settings is a natural next step.

## Notes

## References

[1] Yasin Abbasi-Yadkori, David P´ al, and Csaba Szepesv´ ari. Improved algorithms for linear stochastic bandits.´ Advances in neural information processing systems, 24, 2011.

[2] Marc Abeille and Alessandro Lazaric. Linear thompson sampling revisited. In Artificial Intelligence and Statis tics, pages 176–184. PMLR, 2017.

[3] Shipra Agrawal and Navin Goyal. Thompson sampling for contextual bandits with linear payoffs. In International conference on machine learning, pages 127–135. PMLR, 2013.

[4] Shipra Agrawal and Navin Goyal. Thompson sampling for contextual bandits with linear payoffs, 2014. URL https://arxiv.org/abs/1209.3352.

[5] Peter Auer, Nicolo Cesa-Bianchi, and Paul Fischer. Finite-time analysis of the multiarmed bandit problem. Machine learning, 47(2):235–256, 2002.

[6] Hamsa Bastani and Mohsen Bayati. Online decision making with high-dimensional covariates. Operations Research, 68(1):276–294, 2020.

[7] Anirban Bhattacharya, Debdeep Pati, and Yun Yang. Bayesian fractional posteriors. The Annals of Statistics, 47 (1):39–66, 2019.

[8] Alexandra Carpentier, Claire Vernade, and Yasin Abbasi-Yadkori. The Elliptical Potential Lemma Revisited, October 2020. URL http://arxiv.org/abs/2010.10182. arXiv:2010.10182 [cs, stat].

[9] Sunrit Chakraborty, Saptarshi Roy, and Ambuj Tewari. Thompson sampling for high-dimensional sparse linear contextual bandits. In International Conference on Machine Learning, pages 3979–4008. PMLR, 2023.

[10] Olivier Chapelle and Lihong Li. An empirical evaluation of thompson sampling. Advances in neural information processing systems, 24, 2011.

[11] Varsha Dani, Thomas P. Hayes, and Sham M. Kakade. Stochastic linear optimization under bandit feedback. In COLT, 2008.

[12] Subhashis Ghosal, Jayanta K. Ghosh, and Aad W. van der Vaart. Convergence rates of posterior distributions. Ann. Statist., 28(2):500–531, 2000. ISSN 00905364. URL http://www.jstor.org/stable/2674039.

[13] Kristjan Greenewald, Ambuj Tewari, Susan Murphy, and Predag Klasnja. Action centered contextual bandits. Advances in neural information processing systems, 30, 2017.

[14] Nima Hamidi and Mohsen Bayati. On Frequentist Regret of Linear Thompson Sampling, April 2023. URL http://arxiv.org/abs/2006.06790. arXiv:2006.06790 [cs, stat].

[15] Joey Hong, Branislav Kveton, Manzil Zaheer, Mohammad Ghavamzadeh, and Craig Boutilier. Thompson sampling with a mixture prior. In Gustau Camps-Valls, Francisco J. R. Ruiz, and Isabel Valera, editors, Proceedings of The 25th International Conference on Artificial Intelligence and Statistics, volume 151 of Proceedings of Machine Learning Research, pages 7565–7586. PMLR, 28–30 Mar 2022. URL https: //proceedings.mlr.press/v151/hong22b.html.

[16] Prateek Jaiswal, Debdeep Pati, Anirban Bhattacharya, and Bani Mallick. Generalized regret analysis of thomson sampling with fractional posteriors, 2022. URL https://arxiv.org/abs/1705.XXXXX.

[17] Dong Woo Kim, Tze Leung Lai, and Huanzhong Xu. Multi-armed bandits with covariates. Statistica Sinica, 31: 2275–2287, 2021.

[18] Wonyoung Kim, Kyungbok Lee, and Myunghee Cho Paik. Double doubly robust thompson sampling for generalized linear contextual bandits. In Proceedings ofthe AAAI Conference on Artificial Intelligence, volume 37-7, pages 8300–8307, 2023.

[19] Tze Leung Lai, Herbert Robbins, et al. Asymptotically efficient adaptive allocation rules. Advances in applied mathematics, 6(1):4–22, 1985

[20] Lihong Li and Olivier Chapelle. Open problem: Regret bounds for thompson sampling. In Conference on Learning Theory, pages 43–1. JMLR Workshop and Conference Proceedings, 2012.

[21] Yuwei Luo and Mohsen Bayati. Geometry-Aware Approaches for Balancing Performance and Theoretical Guarantees in Linear Bandits, December 2023. URL http://arxiv.org/abs/2306.14872. arXiv:2306.14872 [cs, stat].

[22] J. A. Nelder and R. W. M. Wedderburn. Generalized linear models. Journal of the Royal Statistical Society. Series A (General), 135(3):370, 1972. ISSN 0035-9238. doi: 10.2307/2344614. URL http://dx.doi. org/10.2307/2344614.

[23] Maxim Panov and Vladimir Spokoiny. Finite sample bernstein – von mises theorem for semiparametric problems. Bayesian Analysis, 10(3), September 2015. doi: 10.1214/14-ba926. URL https://doi.org/10. 1214/14-ba926.

[24] Philippe Rigollet and Assaf Zeevi. Nonparametric bandits with covariates. COLT 2010, page 54, 2010.

[25] Herbert Robbins. Some aspects of the sequential design of experiments. Bulletin of the American Mathematical Society, 58(5):527 – 535, 1952. doi: bams/1183517370. URL https://doi.org/.

[26] Vladimir Spokoiny. Parametric estimation. finite sample theory. The Annals of Statistics, 40(6), December 2012. doi: 10.1214/12-aos1054. URL https://doi.org/10.1214/12-aos1054.

[27] William R. Thompson. On the likelihood that one unknown probability exceeds another in view of the evidence of two samples. Biometrika, 25(3/4):285, December 1933. doi: 10.2307/2332286. URL https://doi.org/ 10.2307/2332286.

[28] Inigo Urteaga and Chris H Wiggins. Nonparametric gaussian mixture models for the multi-armed contextual˜ bandit. stat, 1050:8, 2018.

[29] Aad W Van der Vaart. Asymptotic statistics, volume 3. Cambridge university press, 2000.

[30] Tim Van Erven and Peter Harremos. Renyi divergence and kullback-leibler divergence.´ IEEE Transactions on Information Theory, 60(7):3797–3820, 2014.

[31] Fengshuo Zhang and Chao Gao. Convergence rates of variational posterior distributions. The Annals of Statistics, 48(4), August 2020. doi: 10.1214/19-aos1883. URL https://doi.org/10.1214/19-aos1883.

[32] Tong Zhang. Feel-good thompson sampling for contextual bandits and reinforcement learning. arXiv preprint arXiv:2110.00871, 2021.

## A Table of Notation

## B Proof of Proposition 1

Proof. Proof For $\theta _ { 0 } \in \{ \pm \mu \} ^ { d }$ , the optimal action is $a _ { 0 } = \mathrm { s i g n } ( \theta _ { 0 } )$ (here sign function is applied coordinatewise) and since $\mathbb { A } = [ - 1 , 1 ] ^ { d }$ , the action selected by $\alpha { - } \mathrm { T } \mathbf { S }$ satisfies $\begin{array} { r } { a _ { t } = \arg \operatorname* { m a x } _ { a \in \mathbb { A } } a ^ { \top } \tilde { \theta } _ { t } = \mathrm { s i g n } ( \tilde { \theta } _ { t } ) } \end{array}$ coordinatewise, up to the zero-probability event $\tilde { \theta } _ { t , i } = 0$ . Thus the instantaneous regret satisfies $\langle \theta _ { 0 } , a _ { 0 } - a _ { t } \rangle =$ $2 \mu \textstyle \sum _ { i = 1 } ^ { d } \mathbf { 1 } ( a _ { t , i } \neq \mathrm { s i g n } ( \theta _ { 0 , i } ) )$ ). Taking expectations yields $\begin{array} { r } { \mathbb { E } [ R _ { T } ] = 2 \mu \sum _ { t = 1 } ^ { T } \sum _ { i = 1 } ^ { d } \mathbb { P } ( a _ { t , i } \neq \mathrm { s i g n } ( \theta _ { 0 , i } ) ) } \end{array}$ Under $\alpha { - } \mathrm { T } \mathbf { S }$ , the sampled parameter satisfies $\tilde { { \boldsymbol { \theta } } } _ { t } \sim \mathcal { N } ( \mu _ { t } ^ { \alpha } , ( B _ { t } ^ { \alpha } ) ^ { - 1 } )$ , where precision $\begin{array} { r } { \bar { B _ { t } ^ { \alpha } } = \bar { I } + \alpha \sum _ { s = 1 } ^ { t - 1 } a _ { s } a _ { s } ^ { \top } } \end{array}$ and mean $\begin{array} { r } { \mu _ { t } ^ { \alpha } = ( B _ { t } ^ { \alpha } ) ^ { - 1 } \alpha \sum _ { s = 1 } ^ { t - 1 } a _ { s } r _ { s } } \end{array}$ . Therefore $\tilde { \theta } _ { t , i } \sim \mathcal { N } ( \mu _ { t , i } ^ { \alpha } , ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } )$ . Hence, $\mathbb { P } ( a _ { t , i } \neq \mathrm { s i g n } ( \theta _ { 0 , i } ) \ |$ $\begin{array} { r } { \mathcal { F } _ { t - 1 } \big ) = \Phi \Bigg ( - \frac { \mathrm { s i g n } ( \theta _ { 0 , i } ) \mu _ { t , i } ^ { \alpha } } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } \Bigg ) } \end{array}$ . Using monotonicity of Φ, we obtain $\Phi ( - \mathrm { s i g n } ( \theta _ { 0 , i } ) x ) \ge \Phi ( - | x | )$ . Thus, $\mathbb { P } ( a _ { t , i } \neq \mathrm { s i g n } ( \theta _ { 0 , i } ) ) \geq \mathbb { E } \left[ \Phi \left( - \frac { | \mu _ { t , i } ^ { ( \alpha ) } | } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } \right) \right]$

Now substituting $r _ { s } = a _ { s } ^ { \top } \theta _ { 0 } + \eta _ { s }$ and using $\begin{array} { r } { \alpha \sum _ { s = 1 } ^ { t - 1 } a _ { s } a _ { s } ^ { \top } = B _ { t } ^ { \alpha } - I } \end{array}$ gives $\mu _ { t } ^ { \alpha } = \theta _ { 0 } - ( B _ { t } ^ { \alpha } ) ^ { - 1 } \theta _ { 0 } +$ $\textstyle ( B _ { t } ^ { \alpha } ) ^ { - 1 } \alpha \sum _ { s = 1 } ^ { t - 1 } { a _ { s } \eta _ { s } }$ . Taking the i-th coordinate and applying the triangle inequality gives

$$
| \mu _ { t , i } ^ { \alpha } | \leq | \theta _ { 0 , i } | + \left| e _ { i } ^ { \top } ( B _ { t } ^ { \alpha } ) ^ { - 1 } \theta _ { 0 } \right| + \left| e _ { i } ^ { \top } ( B _ { t } ^ { \alpha } ) ^ { - 1 } \alpha \sum _ { s = 1 } ^ { t - 1 } a _ { s } \eta _ { s } \right| .
$$

Since $\theta _ { 0 } \in \{ \pm \mu \} ^ { d }$ , we have $| \theta _ { 0 , i } | = \mu$ . Moreover, $\begin{array} { r } { ( B _ { t } ^ { \alpha } ) _ { i i } = 1 + \alpha \sum _ { s = 1 } ^ { t - 1 } a _ { s , i } ^ { 2 } \leq 1 + } \end{array}$ αt, because $| a _ { s , i } | \leq 1$ Since $B _ { t } ^ { \alpha }$ is positive definite, $\begin{array} { r } { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } \ge \frac { 1 } { ( B _ { t } ^ { \alpha } ) _ { i i } } } \end{array}$ , and hence $\frac { | \theta _ { 0 , i } | } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } \leq \mu \sqrt { 1 + \alpha t }$ . For the second term, Cauchy–Schwarz yields $\left| e _ { i } ^ { \top } ( B _ { t } ^ { \alpha } ) ^ { - 1 } \theta _ { 0 } \right| \leq \| \theta _ { 0 } \| _ { 2 } \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } = \mu \sqrt { d } \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } }$ . For the noise term, conditional on $\mathcal { F } _ { t - 1 }$ , the quantity

$$
e _ { i } ^ { \top } ( B _ { t } ^ { \alpha } ) ^ { - 1 } \sum _ { s = 1 } ^ { t - 1 } a _ { s } \eta _ { s }
$$

is Gaussian with mean zero, since it is a linear combination of the independent Gaussian noises $\{ \eta _ { s } \} _ { s = 1 } ^ { t - 1 }$ Its conditional variance is $\begin{array} { r } { e _ { i } ^ { \top } ( B _ { t } ^ { \alpha } ) ^ { - 1 } \Big ( \sum _ { s = 1 } ^ { t - 1 } a _ { s } a _ { s } ^ { \top } \Big ) ( B _ { t } ^ { \alpha } ) ^ { - 1 } e _ { i } } \end{array}$ . Using $\begin{array} { r } { B _ { t } ^ { \alpha } = I + \alpha \sum _ { s = 1 } ^ { t - 1 } a _ { s } a _ { s } ^ { \top } } \end{array}$ , we obtain $\begin{array} { r } { \sum _ { s = 1 } ^ { t - 1 } a _ { s } a _ { s } ^ { \top } = \alpha ^ { - 1 } ( B _ { t } ^ { \alpha } - I ) } \end{array}$ , and therefore the conditional variance is $\alpha ^ { - 1 } e _ { i } ^ { \top } ( B _ { t } ^ { \alpha } ) ^ { - 1 } ( B _ { t } ^ { \alpha } - I ) ( B _ { t } ^ { \alpha } ) ^ { - 1 } e _ { i } \le$ $\begin{array} { r } { \alpha ^ { - 1 } e _ { i } ^ { \top } ( B _ { t } ^ { \alpha } ) ^ { - 1 } e _ { i } = \frac { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } { \alpha } } \end{array}$ . Hence there exists a conditionally centered Gaussian random variable $Z _ { t , i }$ with variance at most 1 such that $\begin{array} { r } { e _ { i } ^ { \top } ( B _ { t } ^ { \alpha } ) ^ { - 1 } \textstyle \sum _ { s = 1 } ^ { t - 1 } a _ { s } \eta _ { s } = \sqrt { \frac { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } { \alpha } } Z _ { t , i } } \end{array}$ . Multiplying by α yields

$$
\left| e _ { i } ^ { \top } ( B _ { t } ^ { \alpha } ) ^ { - 1 } \alpha \sum _ { s = 1 } ^ { t - 1 } a _ { s } \eta _ { s } \right| \leq \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } \sqrt { \alpha } | Z _ { t , i } | .
$$

Combining these bounds yields

$$
\frac { | \mu _ { t , i } ^ { ( \alpha ) } | } { \sqrt { ( ( B _ { t } ^ { \alpha } ) ^ { - 1 } ) _ { i i } } } \leq \mu \sqrt { 1 + \alpha t } + \mu \sqrt { d } + \sqrt { \alpha } | Z _ { t , i } | .
$$

## C Proof of Theorem 1

Proof. Proof of Lemma 1: Let us first define a set for any $D > 0 , j > 0$ , and $a ^ { ( t ) }$ adapted to $\mathcal { F } _ { t }$ as $\begin{array} { r } { F _ { t , \epsilon _ { t } } : = \left\{ \theta \in \Theta : \frac { \alpha \lambda } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \geq \frac { ( D + j ) \alpha } { ( 1 - \alpha ) 2 } t \epsilon _ { t } ^ { 2 } \right\} } \end{array}$ . Note that the $F _ { t , \epsilon _ { t } }$ is adapted to $\mathcal { F } _ { t }$ because the definition of $D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } )$ requires knowledge of $a ^ { ( t ) }$ . The α-posterior measure of the set $F _ { t , \epsilon _ { t } }$ can be defined as

$$
\Pi _ { t , \alpha } ( F _ { t , \epsilon _ { t } } | \mathcal { F } _ { t } ) = \frac { \int _ { F _ { t , \epsilon _ { t } } } \prod _ { s = 1 } ^ { t } [ p _ { \theta } ( r _ { s } | a _ { s } ) ] ^ { \alpha } \Pi ( d \theta ) } { \int \prod _ { s = 1 } ^ { t } [ p _ { \theta } ( r _ { s } | a _ { s } ) ] ^ { \alpha } \Pi ( d \theta ) } = \frac { \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } } { \int \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } } ,\tag{9}
$$

where $\begin{array} { r } { \ell _ { t } ( \theta , \theta _ { 0 } ) = \log \frac { \prod _ { s = 1 } ^ { t } p _ { 0 } ( r _ { s } | \mathcal { F } _ { s - 1 } , a _ { s } ) } { \prod _ { s = 1 } ^ { t } p _ { \theta } ( r _ { s } | a _ { s } ) } } \end{array}$ . Now define a set $\begin{array} { r } { A _ { t , \epsilon _ { t } } = \Bigl \{ X _ { t } : \int _ { \Theta } \tilde { \Pi } ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } \leq e ^ { - \frac { ( D + j ) \alpha } { 4 } t \epsilon _ { t } ^ { 2 } } \Bigr \} } \end{array}$ where for any $B \subseteq \Theta , \tilde { \Pi } ( B ) = \Pi ( B \cap \Theta ) / \Pi ( B ( \theta _ { 0 } , \epsilon _ { t } ) )$ and $B \left( \theta _ { 0 } , \epsilon _ { t } \right)$ is as defined in Assumption 4. (Note: Assumption 4 is a stronger statement as it needs to be satisfied for any $a ^ { ( t ) }$ ; however, for this lemma, we need this assumption only for $a ^ { ( t ) }$ that are adapted to $\mathcal { F } _ { t } . )$ Observe that

$$
\mathbb { E } [ \Pi _ { t , \alpha } ( F _ { t , \epsilon _ { t } } | \mathcal { F } _ { t } ) ] = P _ { 0 } ^ { ( t ) } ( A _ { t , \epsilon _ { t } } ) + \mathbb { E } \left[ \frac { \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } } { \int \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } } \mathbf { 1 } ( A _ { t , \epsilon _ { t } } ^ { \mathrm { c } } ) \right] .\tag{10}
$$

First, let us analyze the second term in equation 10. Note that on the set $A _ { t , \epsilon _ { t } } ^ { \mathrm { c } } , \int \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } \geq$ $\Pi ( B \left( \theta _ { 0 } , \epsilon _ { t } \right) ) e ^ { - \frac { ( D + j ) \alpha } { 4 } t \epsilon _ { t } ^ { 2 } }$ . Therefore, using Assumption 4, it follows that

$$
\begin{array} { r } { \mathbb { E } \left[ \frac { \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } } { \int \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } } { \mathbf 1 } ( A _ { t } ^ { c } ) \right] \leq e ^ { \frac { ( D + j ) \alpha } { 4 } t \epsilon _ { t } ^ { 2 } } \mathbb { E } \left[ \frac { \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } } { \Pi ( B \left( \theta _ { 0 } , \epsilon _ { t } \right) ) } \right] } \\ { \leq e ^ { ( \frac { j \alpha } { 4 } + \frac { D \alpha } { 2 } ) t \epsilon _ { t } ^ { 2 } } \mathbb { E } \left[ \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } \right] . } \end{array}\tag{11}
$$

Now it follows from the definition of α-Renyi divergence that´ $\mathbb { E } \left[ e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } | a ^ { ( t ) } \right] \ = \ e ^ { - ( 1 - \alpha ) D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) }$ Hence, using Fubini’s theorem and the observation above, it follows that

$$
\begin{array} { r l } & { \mathbb E \left[ \mathbb E \left[ \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } \right] | a ^ { ( t ) } \right] = \mathbb E \left[ \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { - ( 1 - \alpha ) D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) } \right] } \\ & { \qquad \le \mathbb E \left[ \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { - ( 1 - \alpha ) \left( \frac { ( D + j ) \alpha } { ( 1 - \alpha ) ^ { 2 } } t \epsilon _ { t } ^ { 2 } - \frac { \alpha \lambda } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } \right) } \right] } \\ & { \qquad \le e ^ { - \frac { ( D + j ) \alpha } { 2 } t \epsilon _ { t } ^ { 2 } } \mathbb E \left[ \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { \frac { \alpha ( 1 - \alpha ) \lambda } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } } \right] , } \end{array}\tag{12}
$$

where the penultimate inequality is due to the definition of $F _ { t , \epsilon _ { t } }$ . Substituting equation 12 into equation 11 yields,

$$
\mathbb { E } \left[ \frac { \int _ { F _ { t , \epsilon _ { t } } } \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } } { \int \Pi ( d \theta ) e ^ { - \alpha \ell _ { t } ( \theta , \theta _ { 0 } ) } } \mathbf { 1 } ( A _ { t } ^ { c } ) \right] \leq e ^ { - \frac { j \alpha } { 4 } t \epsilon _ { t } ^ { 2 } } \int \Pi ( d \theta ) e ^ { \frac { \alpha ( 1 - \alpha ) \lambda } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } } .\tag{13}
$$

Next, we analyze the first term in equation 10. It follows from the Markov inequality that

$$
\begin{array} { r l } & { { P } _ { 0 } ^ { ( t ) } ( [ \displaystyle \int \tilde { \Pi } ( d \theta ) \epsilon ^ { - \alpha \ell _ { i } ( \theta , \theta _ { 0 } ) } ] ^ { - 1 / \alpha } \ge e ^ { - \alpha \frac { D + 1 } { 4 } t \epsilon _ { i } ^ { 2 } } ) } \\ & { \quad \le e ^ { - \frac { D + 1 } { 4 } t \epsilon _ { i } ^ { 2 } } \mathbb { E } ( [ \displaystyle \int \tilde { \Pi } ( d \theta ) e ^ { - \alpha \ell _ { i } ( \theta , \theta _ { 0 } ) } ] ^ { - 1 / \alpha } ) } \\ & { \quad \le e ^ { - \frac { D + 1 } { 4 } t \epsilon _ { i } ^ { 2 } } \mathbb { E } [  \int \tilde { \Pi } ( d \theta ) \mathbb { E } ( e ^ { \varepsilon ( \theta , \theta _ { 0 } ) } | a ^ { ( t ) } ) ]  } \\ & { \quad = e ^ { - \frac { D + 1 } { 4 } t \epsilon _ { i } ^ { 2 } } \mathbb { E } [  \int \tilde { \Pi } ( d \theta ) e ^ { D _ { 2 } ^ { ( t ) } ( \theta _ { 0 } , \theta _ { 0 } ) } ] } \\ & { \quad \le e ^ { - \frac { ( D + 1 ) } { 4 } \alpha \epsilon _ { i } ^ { 2 } } e ^ { \frac { D + 1 } { 4 } t \epsilon _ { i } ^ { 2 } } = e ^ { - \frac { D + 1 } { 4 } \epsilon _ { i } ^ { 2 } } , } \end{array}\tag{14}
$$

where the second inequality is due to Jensen’s and Fubini’s theorems, and the penultimate inequality uses the definition of the set $B \left( \theta _ { 0 } , \epsilon _ { t } \right)$ and the fact that $\alpha \in ( 0 , 1 )$ . Combining equation 13 and equation 14, it follows from equation 10 that for all $j > 0$ and $\alpha \in ( 0 , 1 )$

$$
\begin{array} { r l } & { \mathbb { E } \left[ E _ { \alpha } \left[ \mathbf { 1 } \{ \frac { \lambda \alpha } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \ge \frac { ( D + j ) \alpha } { ( 1 - \alpha ) 2 } t \epsilon _ { t } ^ { 2 } \} | \mathcal { F } _ { t } \right] \right] } \\ & { \quad = \mathbb { E } [ \Pi _ { t , \alpha } ( F _ { t , \epsilon _ { t } } | \mathcal { F } _ { t } ) ] \le 2 e ^ { - \frac { j \alpha } { 4 } t \epsilon _ { t } ^ { 2 } } \int \Pi ( d \theta ) e ^ { \frac { \alpha ( 1 - \alpha ) \lambda } { 2 } } \| \theta - \theta _ { 0 } \| ^ { 2 } . } \end{array}\tag{15}
$$

This proves the first assertion of the lemma. Now for the second assertion, note that the RHS above is non-increasing in j; therefore it follows from the inequality above that for all $s \geq 1$

$$
\begin{array} { r l } & { \mathbb { E } \left[ E _ { \alpha } \left[ { \bf 1 } \{ \frac { \lambda \alpha } { 2 } \| \boldsymbol { \theta } - \boldsymbol { \theta } _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \boldsymbol { \theta } , \boldsymbol { \theta } _ { 0 } ) \geq \frac { ( D + s ) \alpha } { ( 1 - \alpha ) 2 } t \epsilon _ { t } ^ { 2 } \} | \mathcal { F } _ { t } \right] \right] } \\ & { \quad \leq 2 e ^ { - \frac { ( s - 1 ) \alpha } { 4 } t \epsilon _ { t } ^ { 2 } } \displaystyle \int \Pi ( d \boldsymbol { \theta } ) e ^ { \frac { \alpha ( 1 - \alpha ) \lambda } { 2 } \| \boldsymbol { \theta } - \boldsymbol { \theta } _ { 0 } \| ^ { 2 } } . } \end{array}\tag{16}
$$

Since ${ \frac { \lambda \alpha } { \gamma } } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) > 0$ , it is straightforward to see that

$$
\begin{array} { r l } & { E \left[ \frac { \lambda \alpha } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) | \mathcal { F } _ { t } \right] = \displaystyle \int _ { 0 } ^ { \infty } E _ { \alpha } \left[ \mathbf { 1 } \{ \frac { \lambda \alpha } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \ge u \} | \mathcal { F } _ { t } \right] d u } \\ & { \qquad \le \displaystyle \frac { ( D + 2 ) \alpha } { ( 1 - \alpha ) ^ { 2 } } t \epsilon _ { t } ^ { 2 } + \displaystyle \int _ { \frac { ( D + 2 ) \alpha } { ( 1 - \alpha ) ^ { 2 } } t \epsilon _ { t } ^ { 2 } } ^ { \infty } E _ { \alpha } \left[ \mathbf { 1 } \{ \frac { \lambda \alpha } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \ge u \} | \mathcal { F } _ { t } \right] d u . } \end{array}
$$

Now using Fubini’s theorem, we have

$$
\begin{array} { r l } & { \mathbb { E } \left[ E _ { \alpha } \left[ \frac { \lambda \alpha } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) | \mathcal { F } _ { t } \right] \right] } \\ & { \leq \frac { ( D + 2 ) \alpha t \epsilon _ { L } ^ { 2 } } { ( 1 - \alpha ) ^ { 2 } } + \int _ { \frac { ( D + 2 ) c _ { \alpha } } { ( 1 - \alpha ) ^ { 2 } } \epsilon _ { c } ^ { 2 } } \mathbb { E } \left[ E _ { \alpha } \left[ \mathbf { 1 } \{ \frac { \lambda \alpha } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \geq u \} | \mathcal { F } _ { t } \right] \right] d u } \\ & { = \frac { ( D + 2 ) \alpha t \epsilon _ { L } ^ { 2 } } { ( 1 - \alpha ) ^ { 2 } } + \int _ { 2 } ^ { \infty } \mathbb { E } E _ { \alpha } \left[ \mathbf { 1 } \{ \frac { \lambda \alpha \| \theta - \theta _ { 0 } \| ^ { 2 } } { 2 } + D _ { \alpha } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \geq \frac { ( D + s ) \alpha t \epsilon _ { L } ^ { 2 } } { ( 1 - \alpha ) ^ { 2 } } \} | \mathcal { F } _ { t } \right] \frac { \alpha t \epsilon _ { d } ^ { 2 } d s } { ( 1 - \alpha ) ^ { 2 } } } \\ &  \leq \frac { ( D + 2 ) \alpha } { ( 1 - \alpha ) ^ { 2 } } t \epsilon _ { t } ^ { 2 } + \frac { \alpha t \epsilon _ { L } ^ { 2 } } { ( 1 - \alpha ) } e ^ { - \frac { \alpha } { 4 } t \epsilon _ { 1 } ^ { 2 } } \int \Pi ( d \theta ) e ^ { \frac { \alpha ( 1 - \alpha ) \lambda } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } } \int _ { 2 } ^ { \infty } e ^ { - \frac { ( s - 2 ) \alpha } { 4 } t \epsilon _ { 1 } ^ { 2 } } d \end{array}\tag{17}
$$

where the penultimate inequality is due to equation 16 and the last inequality holds for all $\alpha t \epsilon _ { t } ^ { 2 } / 4 \geq 0 . 6$ Now the result follows by using the observation that $\begin{array} { r } { \int \Pi ( d \theta ) e ^ { \frac { \alpha ( 1 - \alpha ) \lambda } { 2 } \| \theta - \theta _ { 0 } \| ^ { 2 } } \ \leq \ C _ { \pi } f e ^ { \frac { \alpha ( 1 - \bar { \alpha } ) \lambda } { 2 } \| \theta _ { 0 } \| ^ { 2 } } \ : = } \end{array}$ $C ( \alpha , \lambda , \theta _ { 0 } )$ for some increasing mapping f of $\alpha ( 1 - \alpha ) \lambda \| \theta _ { 0 } \|$ □ □

The next result computes a lower bound on the probability of sampling from the posterior along the best action sequence.

Lemma 3. Under the conditions ofLemma 2for $\bar { \theta } _ { t } = \theta _ { 0 } + \mathbb { D } _ { 0 } ^ { - 2 } \nabla \mathbb { L } ( \theta _ { 0 } )$ , we have

$$
\begin{array} { r l } & { \Pi _ { \alpha , t } ( a _ { 0 } ^ { \top } \theta _ { t } > a _ { 0 } ^ { \top } \theta _ { 0 } ) | \mathcal { F } _ { t - 1 } ) = \Pi _ { \alpha , t } ( a _ { 0 } ^ { \top } { \mathbb D } _ { 0 } ^ { - 1 } { \mathbb D } _ { 0 } \left( \theta _ { t } - \bar { \theta } _ { t } \right) > - a _ { 0 } ^ { \top } { \mathbb D } _ { 0 } ^ { - 2 } \nabla { \mathbb L } ( \theta _ { 0 } ) | \mathcal { F } _ { t - 1 } ) } \\ & { \qquad \geq \mathbb { C } \exp \left( - 2 \alpha \Delta ( d , \eta ) - 8 e ^ { - \eta } \right) \left\{ \displaystyle \frac { C ^ { \dagger } \sqrt { \alpha ( d + \eta ) } } { 1 + C ^ { \dagger } \alpha ( d + \eta ) } e ^ { - C ^ { \dagger ^ { 2 } } \alpha ( d + \eta ) / 2 } \right\} - e ^ { - \eta } } \end{array}\tag{18}
$$

with $P _ { 0 , a } ^ { ( t ) }$ probability of at least $1 - 2 e ^ { - \eta }$ for any $\eta > 0 .$

Proof. Proof of Lemma 3 Using Lemma 2, we have

$$
\begin{array} { r l } & { \Pi _ { \alpha , t } ( a _ { 0 } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \mathbb { D } _ { 0 } \big ( \theta _ { t } - \bar { \theta } _ { t } \big ) > - a _ { 0 } ^ { \top } \mathbb { D } _ { 0 } ^ { - 2 } \nabla \mathbb { L } ( \theta _ { 0 } ) | \mathcal { F } _ { t - 1 } ) } \\ & { \qquad \geq \Pi _ { \alpha , t } ( a _ { 0 } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \mathbb { D } _ { 0 } \big ( \theta _ { t } - \bar { \theta } _ { t } \big ) > \| a _ { 0 } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \| \| \mathbb { D } _ { 0 } ^ { - 1 } \nabla \mathbb { L } ( \theta _ { 0 } ) \| | \mathcal { F } _ { t - 1 } ) } \\ & { \qquad \geq \Pi _ { \alpha , t } ( a _ { 0 } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \mathbb { D } _ { 0 } \big ( \theta _ { t } - \bar { \theta } _ { t } \big ) > C \| a _ { 0 } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \| \sqrt { d + \eta } | \mathcal { F } _ { t - 1 } \big ) } \\ & { \qquad \geq \mathbb { C } \exp \big ( - 2 \alpha \Delta t ( d , \eta ) - 8 e ^ { - \eta } \big ) \{ P ( \phi _ { d } / \sqrt { \alpha } \in A ) \} - e ^ { - \eta } } \\ & { \qquad = \mathbb { C } \exp \big ( - 2 \alpha \Delta t ( d , \eta ) - 8 e ^ { - \eta } \big ) \{ P ( \phi \geq C ^ { \dag } \sqrt { \alpha ( d + \eta ) } \big ) \} - e ^ { - \eta } } \\ &  \qquad \geq \mathbb { C } \exp \big ( - 2 \alpha \Delta _ { t } ( d , \eta ) - 8 e ^ { - \eta } \big ) \{ \frac { C ^ { \dag } \sqrt { \alpha ( d + \eta ) } } { 1 + C ^ { \dag ^ { 2 } } \alpha ( d + \eta ) } e ^  - C ^  \ d \end{array}\tag{19}
$$

(20)

with $P _ { 0 , a } ^ { ( t ) }$ probability of at least $1 \ : - \ : 2 e ^ { - \eta }$ for any $\eta ~ > ~ 0$ , where ${ \cal A } : = \{ x \in \mathbb { R } ^ { d } : a _ { 0 } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } x \geq$ $C ^ { \dagger } \| a _ { 0 } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \| \sqrt { d + \eta } \}$ . Since, $\Delta _ { t } ( d , \eta )$ is a decreasing function of t, the result follows for $\Delta ( d , \eta ) : =$ $\Delta _ { 1 } ( d , \eta ) \geq \Delta _ { t } ( d , \eta ) . \quad \bigsqcup$ □

Proof. Proof of Theorem 1: Let $\begin{array} { r } { \bar { a } _ { t } : = \arg \operatorname* { m i n } _ { a \notin \mathcal { C } _ { t } } \left\| a \right\| _ { V _ { t } ^ { - 1 } } } \end{array}$ and observe that

$$
\begin{array} { r l } & { \Big ( g ( a _ { 0 } ^ { \top } \theta _ { 0 } ) - g ( a _ { t } ^ { \top } \theta _ { 0 } ) \Big ) } \\ & { = \Big ( g ( a _ { 0 } ^ { \top } \theta _ { 0 } ) - g ( \bar { a } _ { t } ^ { \top } \theta _ { 0 } ) \Big ) + \Big ( g ( \bar { a } _ { t } ^ { \top } \theta _ { 0 } ) - g ( a _ { t } ^ { \top } \theta _ { t } ) \Big ) + \Big ( g ( a _ { t } ^ { \top } \theta _ { t } ) - g ( a _ { t } ^ { \top } \theta _ { 0 } ) \Big ) . } \end{array}\tag{21}
$$

Using the definition of the unsaturated actions 2 observe that the first term in equation 25 is bounded as

$$
\begin{array} { r } { \displaystyle \sum _ { t = 1 } ^ { T } \mathbb { E } \left[ \left( g ( a _ { 0 } ^ { \top } \theta _ { 0 } ) - g ( \bar { a } _ { t } ^ { \top } \theta _ { 0 } ) \right) \right] \leq C _ { g } \sum _ { t = 1 } ^ { T } \sqrt { t } \epsilon _ { t } \mathbb { E } [ \left\| \bar { a } _ { t } \right\| _ { V _ { t } ^ { - 1 } } ] \leq \displaystyle \sum _ { t = 1 } ^ { T } \sqrt { t } \epsilon _ { t } \mathbb { E } [ \left\| \bar { a } _ { t } \right\| _ { V _ { t } ^ { - 1 } } ^ { 2 } ] ^ { 1 / 2 } } \\ { \leq C _ { g } \left( \displaystyle \sum _ { t = 1 } ^ { T } t \epsilon _ { t } ^ { 2 } \right) ^ { 1 / 2 } \left( \displaystyle \sum _ { t = 1 } ^ { T } \mathbb { E } [ \left\| \bar { a } _ { t } \right\| _ { V _ { t } ^ { - 1 } } ^ { 2 } ] \right) ^ { 1 / 2 } , } \end{array}\tag{22}
$$

where the last inequality uses Cauchy-Schwarz (CS) inequality. Consider the third term in equation 25 and note that

$$
\begin{array} { r l } { \displaystyle \sum _ { \ell = 1 } ^ { \mathcal { T } } \bigg [ \big ( g ( a _ { \ell } ^ { \top } \ell _ { \ell } ) - g ( a _ { \ell } ^ { \top } \ell _ { 0 } ) \big ) \bigg ] \leq C _ { \delta } \displaystyle \sum _ { \ell = 1 } ^ { \mathcal { T } } \bigg [ \Big | ( a _ { \ell } ^ { \top } \ell _ { \ell } - a _ { \ell } ^ { \top } \ell _ { 0 } ) \Big | \bigg ] } \\ { \leq C _ { \delta } \displaystyle \sum _ { \ell = 1 } ^ { \mathcal { T } } \mathbb { E } \bigg [ \Big | a _ { \ell } \Big | _ { \ell ^ { * } } - a _ { \ell } \| \ell _ { \ell ^ { * } } \Big ] } \\ { \leq C _ { \delta } \mathbb { E } \Bigg [ \displaystyle \sum _ { \ell = 1 } ^ { \mathcal { T } } \Big \| a _ { \ell } \Big | _ { \ell ^ { * } } ^ { \top } \Big ] ^ { 1 / 2 } \Bigg ( \displaystyle \sum _ { \ell = 1 } ^ { \mathcal { T } } \big | \theta _ { \ell } - \theta _ { 0 } \big | \Big | _ { \ell ^ { * } } ^ { 2 } \Bigg ) ^ { 1 / 2 } \Bigg ] } \\ { \leq C _ { \delta } \Bigg ( \mathbb { E } \Bigg [ \displaystyle \sum _ { \ell = 1 } ^ { \mathcal { T } } \big \| a _ { \ell } \big \| _ { \ell ^ { * } } ^ { 2 } \Big ] \Bigg ) ^ { 1 / 2 } \Bigg ( \displaystyle \sum _ { \ell = 1 } ^ { \mathcal { T } } \mathbb { E } \big [ \big \| \theta _ { \ell } - \theta _ { 0 } \big | _ { \ell ^ { * } } ^ { 2 } \Big ] \Bigg ) ^ { 1 / 2 } } \\ { \leq C _ { \delta } \sqrt { 2 } d \log \big ( 1 + \lambda ^ { - 1 / 2 } \big \gamma \bigg ) \Bigg ( \displaystyle \sum _ { \ell = 1 } ^ { \mathcal { T } } \mathbb { E } \big [ \big \| \theta _ { \ell } - \theta _ { 0 } \big | _ { \ell ^ { * } } ^ { 2 } \Big ] \Bigg ) ^ { 1 / 2 } , } \end \end{array}\tag{23}
$$

where the first inequality is due to Assumption 2, the second inequality uses CS with $\begin{array} { r } { V _ { t } = \lambda I + \sum _ { s = 1 } ^ { t - 1 } a _ { s } a _ { s } ^ { \top } } \end{array}$ for any $\lambda > 0$ , and $\| x \| _ { A } = { \sqrt { x ^ { T } A x } }$ , the third and fourth inequality are also due to CS. The bound on $\left( \mathbb { E } \left[ \sum _ { t = 1 } ^ { T } \| a _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } \right] \right) ^ { 1 / 2 }$ uses elliptical potential lemma from [5, Lemma 3.1] and [8, Proposition 1].

Combined with the fact that $g ( a _ { t } ^ { \top } \theta _ { t } ) > g ( \bar { a } _ { t } ^ { \top } \theta _ { t } )$ , the second term in equation 25 can be bounded in a similar way to obtain

$$
\sum _ { t = 1 } ^ { T } \mathbb { E } \left[ \left( g ( \bar { a } _ { t } ^ { \top } \theta _ { 0 } ) - g ( a _ { t } ^ { \top } \theta _ { t } ) \right) \right] \leq C _ { g } \left( \mathbb { E } \left[ \sum _ { t = 1 } ^ { T } \lVert \bar { a } _ { t } \rVert _ { V _ { t } ^ { - 1 } } ^ { 2 } \right] \right) ^ { 1 / 2 } \left( \sum _ { t = 1 } ^ { T } \mathbb { E } \left[ \lVert \theta _ { t } - \theta _ { 0 } \rVert _ { V _ { t } } ^ { 2 } \right] \right) ^ { 1 / 2 }\tag{24}
$$

Now observe that, using the second result in Lemma 1, we have

$$
\begin{array} { r l } { \displaystyle \sum _ { \ell = 1 } ^ { T } \mathbb { E } \left[ \left\| \theta _ { \ell } - \theta _ { 0 } \right\| _ { \mathcal { Y } _ { \ell } } ^ { 2 } \right] = } & { \displaystyle \sum _ { \ell = 1 } ^ { T } \mathbb { E } \left[ \left( \theta _ { \ell } - \theta _ { 0 } \right) ^ { \top } V _ { \ell } ( \theta _ { \ell } - \theta _ { 0 } ) \right] } \\ & { = \displaystyle \sum _ { \ell = 1 } ^ { T } \mathbb { E } \left[ \lambda \| \theta _ { \ell } - \theta _ { 0 } \| ^ { 2 } + \displaystyle \sum _ { s = 1 } ^ { t - 1 } \left\| ( \theta _ { \ell } - \theta _ { 0 } ) ^ { \top } \alpha _ { s } \right\| ^ { 2 } \right] } \\ & { \le \displaystyle \sum _ { \ell = 1 } ^ { T } \mathbb { E } \left[ \lambda \| \theta _ { \ell } - \theta _ { 0 } \| ^ { 2 } + \displaystyle \frac { 2 } { \alpha } D _ { \alpha } ^ { t - 1 } ( \theta _ { \ell } , \theta _ { 0 } ) \right] } \\ & { \le \displaystyle \sum _ { \ell = 1 } ^ { T } \left[ \displaystyle \frac { ( D + 2 ) \alpha } { t - \alpha } { \displaystyle L _ { \ell } ^ { 2 } } + \frac { 4 e ^ { - \frac { \alpha } { 4 } ( \ell + \frac { \alpha } { \alpha } ) } } { ( 1 - \alpha ) } \int \Pi ( \hat { a } \theta ) e ^ { \frac { \alpha ( 1 - \frac { \alpha } { 2 } ) \beta } { \| \theta _ { \ell } \| _ { \mathcal { Y } _ { \ell } } ^ { 2 } } } \right] } \\ & { \le \displaystyle \frac { ( D + 2 ) C ( \alpha , \lambda , \hat { a } _ { 0 } ) } { ( 1 - \alpha ) } \sum _ { \ell = 1 } ^ { T } t _ { \ell , i } ^ { 2 } , } \end{array}\tag{25}
$$

where the first inequality uses Assumption 3. (Note: We omit factor C while using Assumption 3 just for ease of exposition. The arguments can be easily updated to incorporate this factor.) The only term that

remains to be analyzed is E $\left[ \left. \bar { a } _ { t } \right. _ { V _ { t } ^ { - 1 } } ^ { 2 } \right]$ . Observe that

$$
\begin{array} { r l } & { { E _ { \alpha } } \left[ \| a _ { t } \| _ { { V _ { t } ^ { - 1 } } } ^ { 2 } | \mathcal { F } _ { t - 1 } \right] = { E _ { \alpha } } \left[ \| a _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } | \mathcal { F } _ { t - 1 } , a _ { t } \notin \mathcal { C } _ { t } \right] \Pi _ { \alpha , t } ( a _ { t } \notin \mathcal { C } _ { t } | \mathcal { F } _ { t - 1 } ) } \\ & { \qquad \ge \| \bar { a } _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } \Pi _ { \alpha , t } ( a _ { t } \notin \mathcal { C } _ { t } | \mathcal { F } _ { t - 1 } ) , } \end{array}\tag{26}
$$

where the second inequality uses the definition of $\bar { a } _ { t }$ and the fact that it is completely determined by $\mathcal { F } _ { t - 1 }$ Note that if we can establish a lower bound on $\Pi _ { \alpha , t } ( a _ { t } \notin { \mathcal C } _ { t } | { \mathcal F } _ { t - 1 } )$ then we can upper bound $E _ { \alpha } \left[ \| \bar { a } _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } \right]$ by $E _ { \alpha } \left[ \| a _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } \right]$ upto some time dependent term.

Since $a _ { 0 }$ is always unsaturated, therefore if $g ( a _ { 0 } ^ { \top } \theta _ { t } ) > g ( a _ { j } ^ { \top } \theta _ { t } )$ for all saturated actions, $\mathrm { i } . \mathrm { e } . \forall j \in \mathcal { C } _ { t }$ , then one of the unsaturated actions must be played. Consequently,

$$
\begin{array} { r l } & { \Pi _ { \alpha , t } ( a _ { t } \notin { \mathcal C } _ { t } | { \mathcal F } _ { t - 1 } ) \ge P ( g ( a _ { 0 } ^ { \top } \theta _ { t } ) > g ( a _ { j } ^ { \top } \theta _ { t } ) , \forall j \in { \mathcal C } _ { t } | { \mathcal F } _ { t - 1 } ) } \\ & { \ge \Pi _ { \alpha , t } \| \nabla j \in { \mathcal C } _ { t } \ : \langle g ( a _ { 0 } ^ { \top } \theta _ { t } ) > g ( a _ { j } ^ { \top } \theta _ { t } ) \rangle , \| \nabla j : \langle y ( a _ { j } ^ { \top } \theta _ { 0 } ) \ge g ( a _ { j } ^ { \top } \theta _ { t } ) - C _ { g } \sqrt { t } \epsilon _ { t } \| a _ { j } \| _ { V _ { t - 1 } ^ { - 1 } } \rangle | { \mathcal F } _ { t - 1 } ) } \\ &  \ge \Pi _ { \alpha , t } \{ g ( a _ { 0 } ^ { \top } \theta _ { t } ) > g ( a _ { 0 } ^ { \top } \theta _ { 0 } ) \} \{ \forall j : g ( a _ { j } ^ { \top } \theta _ { 0 } ) \ge g ( a _ { j } ^ { \top } \theta _ { t } ) - C _ { g } \sqrt { t } \epsilon _ { t } \| a _ { j } \| _ { V _ { t - 1 } ^ { - 1 } } \} | { \mathcal F } _ { t - 1 } \} \\ & { \ge \Pi _ { \alpha , t } a _ { 0 } ^ { \top } \theta _ { t } > a _ { 0 } ^ { \top } \theta _ { 0 } , \| \theta _ { t } - \theta _ { 0 } \| _ { V _ { t } } \le \sqrt { t } \epsilon _ { t } | { \mathcal F } _ { t - 1 } \} } \\ &  \ge \Pi _ { \alpha , t } ( a _ { 0 } ^ { \top } \theta _ { t } > a _ { 0 } ^ { \top } \theta _ { 0 } | { \mathcal F } _ { t - 1 } ) - \Pi _ { \alpha , t } \| \theta _ { t } - \theta _ { 0 } \| _ { V _ { t } } \ge \sqrt { t } \epsilon _ { t } |  \ \end{array}\tag{27}
$$

with $P _ { 0 } -$ probability of at least $\begin{array} { r } { 1 - 2 e ^ { - \eta } \operatorname { f o r } p = \mathbb { C } \exp \left( - 2 \alpha \Delta ( d , \eta ) - 8 e ^ { - \eta } \right) \left\{ \frac { C ^ { \dagger } \sqrt { \alpha ( d + \eta ) } } { 1 + C ^ { \dagger } \alpha ( d + \eta ) } e ^ { - C ^ { \dagger } ^ { 2 } \alpha ( d + \eta ) / 2 } \right\} - } \end{array}$ $e ^ { - \eta } .$ The third inequality follows the definition of saturated arms to imply $\begin{array} { r l } { g ( a _ { 0 } ^ { \top } \theta _ { 0 } ) } & { { } \geq } \end{array}$ $g ( a _ { j } ^ { \top } \theta _ { 0 } ) + C _ { g } \sqrt { t } \epsilon _ { t } \| a _ { j } \| _ { V _ { \star } ^ { - 1 } } \geq g ( a _ { j } ^ { \top } \theta _ { t } )$ . The penultimate inequality uses the definition of the saturated actions and the result in Lemmas 3 and 1. So, combining equation 26 and equation 27 and denoting $\bar { p } = p - C ( \alpha , \lambda , \theta _ { 0 } ) 2 e ^ { - \frac { \alpha } { 4 } t \epsilon _ { t } ^ { 2 } }$ for brevity, we have

$$
\begin{array} { r } { E _ { \alpha } \left[ \| a _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } | \mathcal { F } _ { t - 1 } \right] = E _ { \alpha } \left[ \| a _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } | \mathcal { F } _ { t - 1 } , a _ { t } \notin \mathcal { C } _ { t } \right] \Pi _ { \alpha , t } ( a _ { t } \notin \mathcal { C } _ { t } | \mathcal { F } _ { t - 1 } ) \ge \| \bar { a } _ { t } \| _ { V _ { t } ^ { - 1 } } ^ { 2 } \bar { p } } \end{array}\tag{28}
$$

with $P _ { 0 } -$ probability of at least $1 - 2 e ^ { - \eta }$ . Since, $t \epsilon _ { t } ^ { 2 }$ increases with t, for sufficiently large $t ( \mathrm { s a y } t _ { 1 } ) , \bar { p }$ will be positive. Denoting the above high probability event as E observe that

$$
\begin{array} { r } { \mathbb { E } \left[ E _ { \alpha } \left[ \left. a _ { t } \right. _ { V _ { t } ^ { - 1 } } ^ { 2 } \middle | \mathcal { F } _ { t - 1 } \right] \right] \geq \mathbb { E } \left[ E _ { \alpha } \left[ \left. a _ { t } \right. _ { V _ { t } ^ { - 1 } } ^ { 2 } \middle | \mathcal { F } _ { t - 1 } \right] \middle | \mathbf { E } \right] P _ { 0 } ( \mathbf { E } ) \geq \mathbb { E } \left[ \left. \bar { a } _ { t } \right. _ { V _ { t } ^ { - 1 } } ^ { 2 } \right] \bar { p } P _ { 0 } ( \mathbf { E } ) . } \end{array}
$$

Therefore, we have

$$
\mathbb { E } \left[ \sum _ { t = 1 } ^ { T } \lVert \bar { a } _ { t } \rVert _ { V _ { t } ^ { - 1 } } ^ { 2 } \right] \leq \frac { 1 } { \bar { p } _ { 0 } ( 1 - 2 e ^ { - \eta } ) } \mathbb { E } \left[ \sum _ { t = 1 } ^ { T } \lVert a _ { t } \rVert _ { V _ { t } ^ { - 1 } } ^ { 2 } \right] \leq \frac { 2 d \log \left( 1 + \lambda ^ { - 1 } T \right) } { \bar { p } _ { 0 } ( 1 - 2 e ^ { - \eta } ) } ,\tag{29}
$$

where p¯0 = C exp  − 2α∆(d, η) −8e<sup>−η</sup> <sub>C</sub>†√<sub>α(d+η</sub> <sup>)</sup> e−C<sup>†2</sup>α(d+η)/2 − e<sup>−η</sup> − C(α, λ, θ<sub>0</sub>)2e<sup>−</sup> <sup>α</sup>4 <sup>t0ϵ2</sup>t0 1+C<sup>†2</sup>α(d+η) for a sufficiently small $t _ { 0 } ( \geq t _ { 1 } )$ and the second inequality uses elliptical potential lemma. We can choose

such $t _ { 0 }$ because $\bar { p }$ increases as t increases (since $t \epsilon _ { t } ^ { 2 }$ increases as t increases). Now, combining equations 21, 22, 23, 24, 25, and 26, we have

$$
\begin{array} { r l r } {  { \mathbb { E } [ R ( T ) ] \leq C _ { g } ( \sum _ { t = 1 } ^ { T } t \epsilon _ { t } ^ { 2 } ) ^ { 1 / 2 } ( \frac { 2 d \log ( 1 + \lambda ^ { - 1 } T ) } { \bar { p } _ { 0 } ( 1 - 2 e ^ { - \eta } ) } ) ^ { 1 / 2 } } } \\ & { } & { \quad + C _ { g } \sqrt { 2 d \log ( 1 + \lambda ^ { - 1 } T ) } ( \frac { ( D + 2 ) C ( \alpha , \lambda , \theta _ { 0 } ) } { ( 1 - \alpha ) } \sum _ { t = 1 } ^ { T } t \epsilon _ { t } ^ { 2 } ) ^ { 1 / 2 } } \\ & { } & { \quad + C _ { g } ( \frac { 2 d \log ( 1 + \lambda ^ { - 1 } T ) } { \bar { p } _ { 0 } ( 1 - 2 e ^ { - \eta } ) } ) ^ { 1 / 2 } ( \frac { ( D + 2 ) C ( \alpha , \lambda , \theta _ { 0 } ) } { ( 1 - \alpha ) } \sum _ { t = 1 } ^ { T } t \epsilon _ { t } ^ { 2 } ) ^ { 1 / 2 } . ~ \mathbb { E } [ \frac { \alpha } { \epsilon } ] _ { } ^ { 1 / 2 } } \end{array}\tag{30}
$$

## D Verifying assumptions for the Exponential family

We first provide the expression for the α-Renyi divergence for exponential family distributions. Observe ´ that for any $\begin{array} { r } { \alpha \in ( 0 , 1 ) , D _ { \alpha } ^ { t } ( \theta , \vartheta ) = \frac { 1 } { 1 - \alpha } \left[ \alpha A ( a _ { t } ^ { \top } \theta ) + ( 1 - \alpha ) A ( a _ { t } ^ { \top } \vartheta ) - A ( \alpha a _ { t } ^ { \top } \theta + ( 1 - \alpha ) a _ { t } ^ { \top } \vartheta ) \right] } \end{array}$ . Using this definition, we show that the exponential family satisfies Assumption 3 for $C = { \sqrt { \frac { 1 } { m } } } .$

Lemma 4. Fix $\alpha \in ( 0 , 1 )$ . For distribution lying in exponentialfamily distribution satisfying conditions in Definition 3, we have, $\begin{array} { r } { | { a } _ { t } ^ { \top } \theta - { a } _ { t } ^ { \top } \vartheta | \leq \sqrt { \frac { 1 } { m } } \sqrt { \frac { 2 } { \alpha } { D } _ { \alpha } ^ { t } ( \theta , \vartheta ) } } \end{array}$

Proof. Proof: The proof of this lemma is a direct consequence of the condition (iii) in Definition 3. □

Our next result shows that the exponential family satisfies Assumption 4.

Lemma 5. Under Assumption 1, the exponential family of distribution as defined in Definition 3, satisfies Assumption 4 for $\begin{array} { r } { \epsilon _ { t } ^ { 2 } ~ = ~ \frac { 4 d C _ { g } \log ( t ) } { D \alpha C _ { \phi } t } } \end{array}$ , where $C _ { g }$ is the Lipschitz constant of the link function and $\begin{array} { r } { C _ { \phi } \ : = \ : \operatorname* { m i n } _ { i \in [ d ] , x \in B ( \theta _ { 0 } ^ { i } , 1 ) } \phi _ { i } ( x ) , \ : B ( \theta _ { 0 } ^ { i } , u ) \subset \mathbb { R } } \end{array}$ is a ball of radius u centered at $\theta _ { 0 } ^ { i } \in \Theta$ and $\phi _ { i } ( \cdot )$ is the density $o f \Pi _ { t } ^ { i }$

Proof. Proof of Lemma 5: Observe that

$$
\begin{array} { r l } & { \displaystyle { D _ { 2 } ^ { ( t ) } ( \theta , \theta _ { 0 } ) = \log \int p _ { \theta } ^ { ( t ) } ( r ^ { ( t ) } | a ^ { ( t ) } ) ^ { 2 } p _ { 0 } ^ { ( t ) } ( r ^ { ( t ) } | a ^ { ( t ) } ) ^ { - 1 } d \mu ^ { ( t ) } } } \\ & { \displaystyle { = \sum _ { s = 1 } ^ { t } \log \int p _ { \theta } ( r _ { s } | \mathcal { F } _ { t - 1 } , a _ { s } ) ^ { 2 } p _ { 0 } ( r _ { s } | \mathcal { F } _ { s - 1 } , a _ { s } ) ^ { - 1 } d \mu } } \\ & { \displaystyle { - \sum _ { s = 1 } ^ { t } \left[ A ( a _ { s } ^ { \top } ( 2 \theta - \theta _ { 0 } ) ) - 2 A ( a _ { s } ^ { \top } \theta ) + A ( a _ { s } ^ { \top } \theta _ { 0 } ) \right] } } \\ & { \displaystyle { \le C _ { g } \sum _ { s = 1 } ^ { T } \left| ( a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) \right| ^ { 2 } } } \\ & { \displaystyle { \le i C _ { g } \| ( \theta - \theta _ { 0 } ) \| ^ { 2 } } . } \end{array}\tag{31}
$$

where the third equality uses the definition of 2-Renyi divergence for an exponential family of distributions,´ the first inequality follows due to condition (ii) in Definition 3 (since $| A ^ { \prime \prime } ( x ) | \le C _ { g }$ , the result follows by second-order mean value theorem), the last inequality follows from Cauchy-Schwartz inequality and the Assumption 1 that $\begin{array} { r } { \sum _ { t = 1 } ^ { T } \| a _ { t } \| \leq T } \end{array}$ . Using equation 31 observe that

$$
\begin{array} { l } { \displaystyle \Pi \left( D _ { 2 } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \leq \frac { D \alpha } { 4 } t \epsilon _ { t } ^ { 2 } \right) \geq \Pi \left( t C _ { g } \| \theta - \theta _ { 0 } ) \| ^ { 2 } \leq \frac { D \alpha } { 4 } t \epsilon _ { t } ^ { 2 } \right) } \\ { \displaystyle \qquad = \Pi \left( ( \theta - \theta _ { 0 } ) ^ { \top } ( \theta - \theta _ { 0 } ) \leq \frac { D \alpha } { 4 C _ { g } } \epsilon _ { t } ^ { 2 } \right) } \\ { \displaystyle \qquad \geq \displaystyle \prod _ { i = 1 } ^ { d } \Pi ^ { i } \left( ( \theta ^ { i } - \theta _ { 0 } ^ { i } ) ^ { 2 } \leq \frac { D \alpha } { 4 d C _ { g } } \epsilon _ { t } ^ { 2 } \right) . } \end{array}\tag{32}
$$

Fix $\begin{array} { r } { \epsilon _ { t } ^ { 2 } = \frac { \log ( t ) } { C ^ { 2 } t C _ { \phi } } } \end{array}$ for $\begin{array} { r } { C ^ { 2 } \ = \ \frac { D \alpha } { 4 d C _ { q } } } \end{array}$ and observe that $\begin{array} { r } { C \epsilon _ { t } = \sqrt { \frac { \log ( t ) } { t C _ { \phi } } } \le 1 } \end{array}$ for all $t \geq 1$ . Now it follows that $\begin{array} { r } { \operatorname* { m i n } _ { i \in [ d ] , x \in B ( \theta _ { 0 } ^ { i } , C \epsilon _ { T } ) } \phi ( x ) \ge \operatorname* { m i n } _ { i \in [ d ] , x \in B ( \theta _ { 0 } ^ { i } , 1 ) } \phi _ { i } ( x ) = C _ { \phi } } \end{array}$ for all $t \geq t _ { 0 }$ and any $i \in [ d ]$ , where $B ( \theta _ { 0 } ^ { i } , u ) \subset$ R is a ball of radius u centered at $\theta _ { 0 } ^ { i } \in \Theta$ and $\phi _ { i } ( \cdot )$ is the density of Π<sup>i</sup>. Note that $C _ { \phi } > 0$ since the prior density is strictly positive everywhere. Consequently, for all $t \geq t _ { 0 }$ , it follows from our choice of $\epsilon _ { t }$ that

$$
\Pi ^ { i } \left( | \theta ^ { i } - \theta _ { 0 } ^ { i } | ^ { 2 } \le \frac { D \alpha } { 4 d C _ { g } } \epsilon _ { t } ^ { 2 } \right) \ge 2 \operatorname* { m i n } _ { x \in B ( \theta _ { 0 } ^ { i } , C \epsilon _ { t } ) } \phi ( x ) C \epsilon _ { t } \ge C _ { \phi } \sqrt { \frac { 4 \log ( t ) } { t C _ { \phi } ^ { 2 } } } \ge \sqrt { \frac { \log ( t ) } { t } } .\tag{33}
$$

Now the assertion of the lemma follows using the fact that $\begin{array} { r } { \sqrt { \frac { \log n } { n } } \ge \frac { 1 } { n } = e ^ { - C ^ { 2 } C _ { \phi } n \epsilon _ { n } ^ { 2 } } = e ^ { - \frac { D \alpha C _ { \phi } } { 4 d C _ { g } } n \epsilon _ { n } ^ { 2 } } } \end{array}$ for any $n \geq 2$ . In particular, we have from equation 32, equation 33 and the arguments above that

$$
\Pi \left( D _ { 2 } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \le \frac { D \alpha } { 4 } t \epsilon _ { t } ^ { 2 } \right) \ge \prod _ { i = 1 } ^ { d } \Pi ^ { i } \left( ( \theta ^ { i } - \theta _ { 0 } ^ { i } ) ^ { 2 } \le \frac { D \alpha } { 4 d C _ { g } } \epsilon _ { T } ^ { 2 } \right) \ge e ^ { - \frac { D \alpha C _ { \phi } } { 4 C _ { g } } t \epsilon _ { t } ^ { 2 } } ,\tag{34}
$$

and the result follows for any prior for which $C _ { \phi } \leq C _ { g }$ (otherwise, this term will appear in the form of a constant in the main result). □ □

To show that the exponential family satisfies Assumption 6, the conditions required for the finite BvM to hold, we first write down various expressions used in their definition for exponential family models. First recall the definition of the conditional log-likelihood of generating $\bar { X _ { t } } | a ^ { ( t ) }$ , that is $\mathbb { L } ( \theta ) ~ =$ log $\textstyle \prod _ { s = 1 } ^ { t } [ p _ { \theta } ( r _ { s } | a _ { s } ) ]$ . Using this definition, the gradient of the log-likelihood can be derived as $\nabla \mathbb { L } ( \theta ) =$ $\begin{array} { r } { \sum _ { s = 1 } ^ { t } \dot { \nabla } \log p _ { \theta } ( r _ { s } | \boldsymbol { a } _ { s } ) = \sum _ { s = 1 } ^ { t } \big ( r _ { s } \boldsymbol { a } _ { s } ^ { \top } - \boldsymbol { A } ^ { \prime } ( \boldsymbol { a } _ { s } ^ { \top } \boldsymbol { \theta } ) \boldsymbol { a } _ { t } ^ { \top } \big ) } \end{array}$ . Similarly, $\begin{array} { r } { \nabla ^ { 2 } \mathbb { L } ( \theta ) = - \sum _ { s = 1 } ^ { t } A ^ { \prime \prime } ( a _ { t } ^ { \top } \theta ) a _ { t } a _ { t } ^ { \top } } \end{array}$ . Recall that $\mathbb { E } _ { a } [ ] = \mathbb { E } [ | a ^ { ( t ) } ]$ . Now observe that

$$
\mathbb { E } _ { a } [ \nabla \mathbb { L } ( \theta ) ] = \sum _ { s = 1 } ^ { t } \mathbb { E } _ { a } [ \nabla \mathbb { L } ( \theta ) ] = \sum _ { s = 1 } ^ { t } \left( A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) a _ { s } ^ { \top } - A ^ { \prime } ( a _ { s } ^ { \top } \theta ) a _ { s } ^ { \top } \right) .
$$

and $\mathbb { D } _ { 0 } ^ { 2 }$ and $\begin{array} { r c l r } { { \mathbb D } _ { 0 } ^ { 2 } ( \theta ) } & { = } & { { \displaystyle - \nabla ^ { 2 } { \mathbb E } _ { a } [ { \mathbb L } ( \theta ) ] } ~ = ~ \sum _ { s = 1 } ^ { t } A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta ) a _ { s } a _ { s } ^ { \top } } \end{array}$ The stochastic part of the condi tional log-likelihood is denoted as $\zeta ( \theta ) : = \mathbb { L } ( \theta ) - \mathbb { E } _ { a } [ \mathbb { L } ( \theta ) ]$ and $\nabla \zeta ( \theta ) ~ = ~ \nabla \mathbb { L } ( \theta ) - \mathbb { E } _ { a } [ \nabla \mathbb { L } ( \theta ) ] ~ =$ $\begin{array} { r l r } { \sum _ { s = 1 } ^ { t } \left( \bar { r _ { s } } a _ { s } ^ { \top } - A ^ { \prime } ( a _ { s } ^ { \top } \theta ) a _ { s } ^ { \top } \right) - } & { \mathbb { E } _ { a } \left[ \sum _ { s = 1 } ^ { t } \left( r _ { s } a _ { s } ^ { \top } - A ^ { \prime } ( a _ { s } ^ { \top } \theta ) a _ { s } ^ { \top } \right) \right] } & { = } & { \ \sum _ { s = 1 } ^ { t } \left( r _ { s } a _ { s } ^ { \top } - \mathbb { E } _ { a } \left[ \bar { r _ { s } } a _ { s } ^ { \top } \right] \right) ^ { \top } \ = } \end{array}$ $\begin{array} { r l } { \sum _ { s = 1 } ^ { t } \big ( r _ { s } a _ { s } ^ { \top } - A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) a _ { s } ^ { \top } \big ) } \end{array}$ and therefore $\mathbb { E } _ { a } [ \nabla \zeta ( \theta _ { 0 } ) ] ~ = ~ 0$ Note that $\nabla \zeta ( \theta )$ is independent of $\theta ,$ therefore $\nabla ^ { 2 } \zeta ( \theta ) = 0$ . First, we present a technical lemma that is satisfied by exponential families.

Lemma 6. There exists some constant $\begin{array} { r l r l } { v _ { 0 } } & { { } > } & { 0 } \end{array}$ and $\begin{array} { r l r } { \mathbf { g } _ { 1 } } & { { } > } & { 0 , } \end{array}$ , for every $s \_ a$ constant $\begin{array} { r l r } { \sigma _ { s } ^ { 2 } } & { { } = } & { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) } \end{array}$ (given $\mathcal { F } _ { s - 1 }$ and $a _ { s } )$ such that $\begin{array} { r l r } { \mathbb { E } [ ( r _ { s } - A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ) ^ { 2 } / \sigma _ { i } ^ { 2 } | \mathcal { F } _ { s - 1 } , a _ { s } ] } & { \le } & { 1 } \end{array}$ and log $\mathbb { E } [ \exp ( \lambda ( r _ { s } - A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ) / \sigma _ { s } ) | \mathcal { F } _ { s - 1 } , a _ { s } ] \leq v _ { 0 } ^ { 2 } \lambda ^ { 2 } / 2 , ~ | \lambda | < \mathbf { g } _ { 1 }$

Proof. Proof: The proof is a direct consequence of Lemma 2.14 [26](in their supplement). □ □

Next, we have the main result that verifies all the conditions in Assumption 6.

Lemma 7. The exponential family of distribution (Definition 3) satisfies Assumption 6. The condition $( E D _ { 0 } )$ is satisfied with $\mathbf { g } : = \mathbf { g } _ { 1 } \mathbb { T } ^ { 1 / 2 }$ , where $\begin{array} { r } { \mathbb { T } ^ { - 1 / 2 } : = \operatorname* { m a x } _ { s \in [ t ] } \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } } \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \dot { \theta _ { 0 } } ) ^ { 1 / 2 } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } } \end{array}$ and condition $\left( E D _ { 1 } \right)$ can be satisfied for any $\omega > 0$ and $\mathbf { g } > 0$ . Condition $\mathcal { L } _ { 0 }$ follows for $\delta ( \mathbf { r } ) = L \mathbb { T } _ { 2 } ^ { - 1 / 2 } \mathbf { r }$ , where $\begin{array} { r } { \mathbb { T } ^ { - 1 / 2 } : = \operatorname* { m a x } _ { s \in [ t ] } \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } } \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 1 / 2 } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } } \end{array}$

Proof. Proof: We derive all the conditions in seriatim. The proofs are adapted from [23], and [26].

1. Proof for $\left( E D _ { 0 } \right)$ : Observe using the definition of $\nabla \zeta ( \theta _ { 0 } )$ that

$$
\begin{array} { r l r } & { } & { \mathbb { E } _ { a } \exp \left\{ { \bf m } \frac { \langle \nabla \zeta ( \theta _ { 0 } ) , \gamma \rangle } { \| \mathbb { D } _ { 0 } \gamma \| } \right\} = \mathbb { E } _ { a } \exp \left\{ { \bf m } \frac { \sum _ { s = 1 } ^ { t } \left( r _ { s } a _ { s } ^ { \top } \gamma - A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) a _ { s } ^ { \top } \gamma \right) } { \| \mathbb { D } _ { 0 } \gamma \| } \right\} } \\ & { } & { = \mathbb { E } _ { a } \displaystyle \prod _ { s = 1 } ^ { t } \exp \left\{ { \bf m } \frac { a _ { s } ^ { \top } \gamma \left( r _ { s } - A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) \right) } { \| \mathbb { D } _ { 0 } \gamma \| } \right\} } \\ & { } & { = \mathbb { E } _ { a } \left[ \displaystyle \prod _ { s = 1 } ^ { t - 1 } \exp \left\{ { \bf m } \frac { a _ { s } ^ { \top } \gamma \left( r _ { s } - A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) \right) } { \| \mathbb { D } _ { 0 } \gamma \| } \right\} \mathbb { E } _ { a } \left[ \exp \left\{ { \bf m } \frac { a _ { s } ^ { \top } \gamma \left( r _ { s } - A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) \right) } { \| \mathbb { D } _ { 0 } \gamma \| } \right\} \right] \right] . } \end{array}\tag{35}
$$

Define $\begin{array} { r } { \mathbb { T } ^ { - 1 / 2 } : = \operatorname* { m a x } _ { s \in [ t ] } \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } } \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 1 / 2 } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } } \end{array}$ . By this definition, $\begin{array} { r } { \mathbb { T } ^ { - 1 / 2 } \ge \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 1 / 2 } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } } \end{array}$ and therefore m $\begin{array} { r } { \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 1 / 2 } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } \leq \mathbf { m } \mathbb { T } ^ { - 1 / 2 } \leq \mathbf { g } _ { 1 } } \end{array}$ , for $\mathbf { g } : = \mathbf { g } _ { 1 } \mathbb { T } ^ { 1 / 2 }$ . Therefore, using Lemma 6 it follows that

$$
\mathbb { E } _ { a } \left[ \exp \left\{ \mathbf { m } \frac { a _ { s } ^ { \top } \gamma \left( r _ { s } - A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) \right) } { \| \mathbb { D } _ { 0 } \gamma \| } \right\} \right] \leq \exp \left( v _ { 0 } ^ { 2 } \mathbf { m } ^ { 2 } \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 2 } | a _ { s } ^ { \top } \gamma | ^ { 2 } } { 2 \| \mathbb { D } _ { 0 } \gamma \| ^ { 2 } } \right) .\tag{36}
$$

Now substituting equation 36 into equation 35, we have for $| { \bf m } | \le { \bf g } : = { \bf g } _ { 1 } \mathbb { T } ^ { 1 / 2 }$

$$
\begin{array} { r l } & { \mathbb { E } _ { a } \exp \left\{ \mathbf { m } \frac { \langle \nabla \zeta ( \theta _ { 0 } ) , \gamma \rangle } { \| \mathbb { D } _ { 0 } \gamma \| } \right\} } \\ & { \quad \leq \mathbb { E } _ { a } \left[ \prod _ { s = 1 } ^ { t - 1 } \exp \left\{ \mathbf { m } \frac { a _ { s } ^ { \top } \gamma \left( r _ { s } - A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) \right) } { \| \mathbb { D } _ { 0 } \gamma \| } \right\} \exp \left( v _ { 0 } ^ { 2 } \mathbf { m } ^ { 2 } \frac { A ^ { \prime \prime } ( a _ { t } ^ { \top } \theta _ { 0 } ) ^ { 2 } | a _ { t } ^ { \top } \gamma | ^ { 2 } } { 2 \| \mathbb { D } _ { 0 } \gamma \| ^ { 2 } } \right) \right] } \\ & { \quad \leq \exp \left( v _ { 0 } ^ { 2 } \mathbf { m } ^ { 2 } \frac { \sum _ { s = 1 } ^ { t } A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 2 } | a _ { s } ^ { \top } \gamma | ^ { 2 } } { 2 \| \mathbb { D } _ { 0 } \gamma \| ^ { 2 } } \right) = \exp \left( v _ { 0 } ^ { 2 } \mathbf { m } ^ { 2 } / 2 \right) . } \end{array}\tag{37}
$$

The result follows for any γ, so it must follow for the supremum over $\gamma \in \mathbb { R } ^ { d }$

2. Proof for $\left( E D _ { 1 } \right)$ : This assumption follows immediately for any $\omega > 0$ because $\nabla ^ { 2 } \zeta ( \theta ) = 0$ by definition.

3. Proof for $( \mathcal { L } _ { 0 } )$ : For $I _ { d } = \mathbb { D } _ { 0 } ^ { - 1 } \mathbb { D } _ { 0 } ^ { 2 } \mathbb { D } _ { 0 } ^ { - 1 }$ , we have by the definition of operator norm

$$
\begin{array} { r l } { \| \mathbb { D } _ { 0 } ^ { - 1 } ( \mathbb { D } _ { 0 } ^ { 2 } ( \theta ) - \mathbb { D } _ { 0 } ^ { 2 } ) \mathbb { D } _ { 0 } ^ { - 1 } \| = } & { \underset { \gamma \in \mathbb { R } ^ { d _ { * } / 1 } \times \mathbb { Y } \parallel = 1 } { \operatorname* { s u p } } \left| \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } ( \mathbb { D } _ { 0 } ^ { 2 } ( \theta ) - \mathbb { D } _ { 0 } ^ { 2 } ) \mathbb { D } _ { 0 } ^ { - 1 } \gamma \right| } \\ & { = \underset { \gamma \in \mathbb { R } ^ { d _ { * } / 1 } \times \mathbb { Y } \parallel = 1 } { \operatorname* { s u p } } \left| \displaystyle \sum _ { s = 1 } ^ { t } ( A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta ) - A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ) \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } a _ { s } a _ { s } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \gamma \right| } \\ & { \leq \underset { \gamma \in \mathbb { R } ^ { d _ { * } / 1 } \times \mathbb { Y } \parallel = 1 } { \operatorname* { s u p } } \displaystyle \sum _ { s = 1 } ^ { t } \left| A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta ) - A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ) \right| \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } a _ { s } a _ { s } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \gamma } \\ &  \leq \underset { \gamma \in \mathbb { R } ^ { d _ { * } / 1 } \times \mathbb { Y } \parallel = 1 } { \operatorname* { s u p } } \displaystyle \sum _ { s = 1 } ^ { t } L \left| a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) \right| \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } a _ { s } a _ { s } ^   \end{array}\tag{38}
$$

Define $\begin{array} { r } { \mathbb { T } _ { 2 } ^ { - 1 / 2 } : = \operatorname* { m a x } _ { s } \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } } \frac { \vert a _ { s } ^ { \top } \gamma \vert } { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) \Vert \mathbb { D } _ { 0 } \gamma \Vert } \geq \frac { \vert a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) \vert } { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) \Vert \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \Vert } } \end{array}$ and observe that

$$
\begin{array} { r l } { \| \mathbb { D } _ { 0 } ^ { - 1 } ( \mathbb { D } _ { 0 } ^ { 2 } ( \theta ) - \mathbb { D } _ { 0 } ^ { 2 } ) \mathbb { D } _ { 0 } ^ { - 1 } \| \le \displaystyle \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } : \| \gamma \| = 1 } \displaystyle \sum _ { s = 1 } ^ { t } L \left| a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) \right| \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } a _ { s } a _ { s } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \gamma } & { } \\ { \le L \mathbb { T } _ { 2 } ^ { - 1 / 2 } \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \| \displaystyle \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } : \| \gamma \| = 1 } \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \displaystyle \sum _ { s = 1 } ^ { t } A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) a _ { s } a _ { s } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \gamma } & { } \\ { = L \mathbb { T } _ { 2 } ^ { - 1 / 2 } \mathbf { r } . } & { ( 3 \mathbb { T } _ { 2 } ^ { - 1 / 2 } \mathbf { r } , } \end{array}\tag{39}
$$

Consequently, $\delta ( \mathbf { r } ) = L \mathbb { T } _ { 2 } ^ { - 1 / 2 } \mathbf { r }$

4. Proof for $( { \mathcal { L } } \mathbf { r } )$ : For any $\mathbf { r } > 0$ there exists a value $\mathbf { b } ( \mathbf { r } ) > 0$ , such that $\mathbf { r b } ( \mathbf { r } ) $ ∞ as $\mathbf { r }  \infty$ and $- \mathbb { E } _ { a } \mathbb { L } ( \theta , \theta _ { 0 } ) \ge { \bf r } ^ { 2 } b ( { \bf r } )$ for all θ with ${ \bf r } = \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \|$ . Note that

$$
\begin{array} { r l } & { \mathbb { E } _ { a } [ \mathbb { L } ( \theta , \theta _ { 0 } ) ] = \mathbb { E } _ { a } [ \mathbb { L } ( \theta ) - \mathbb { L } ( \theta _ { 0 } ) ] = \displaystyle \sum _ { s = 1 } ^ { t } \mathbb { E } _ { a } [ \log ( \frac { p \theta ( r _ { s } | a _ { s } ) } { p _ { \theta _ { 0 } } ( r _ { s } | \mathcal { F } _ { s - 1 } , a _ { s } ) } ) ] = \displaystyle \sum _ { s = 1 } ^ { t } \mathbb { E } _ { a } [ r _ { s } a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) ] } \\ & { - A ( a _ { s } ^ { \top } \theta ) + A ( a _ { s } ^ { \top } \theta _ { 0 } ) ] = \displaystyle \sum _ { s = 1 } ^ { t } [ A ( a _ { s } ^ { \top } \theta _ { 0 } ) - A ( a _ { s } ^ { \top } \theta ) + A ^ { \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) ] } \\ & { = - \displaystyle \sum _ { s = 1 } ^ { t } A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta ^ { * } ) ( \theta - \theta _ { 0 } ) ^ { \top } a _ { s } a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) = - \| \mathbb { D } _ { 0 } ( \theta ^ { * } ) ( \theta - \theta _ { 0 } ) \| ^ { 2 } , } \end{array}\tag{0}
$$

where the penultimate equality uses second-‘order Taylor’s theorem for a $\theta ^ { * }$ that lies between $\theta$ and $\theta _ { 0 }$ . Now observe that $\begin{array} { r }  \frac { - \frac { 1 } { \mathbb { E } _ { a } [ \mathbb { L } ( \dot { \theta _ { , } } \theta _ { 0 } ) ] } { { \bf r } ^ { 2 } } = \frac { \| \mathbb { D } _ { 0 } ( \theta ^ { * } ) ( \theta - \theta _ { 0 } ) \| ^ { 2 } } { \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \| ^ { 2 } } } \end{array}$ . Note that, by definition $\theta ^ { * }$ is a mapping from r and so is $\frac { \lVert \mathbb { D } _ { 0 } ( \theta ^ { * } ) ( \theta - \theta _ { 0 } ) \rVert ^ { 2 } } { \lVert \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \rVert ^ { 2 } }$ . Therefore, we can always find a mapping $\mathbf { b } ( \mathbf { r } )$ such that $\mathbf { r b } ( \mathbf { r } ) \to \infty \mathrm { a s }$ $\begin{array} { r } { \mathbf { r }  \infty \mathrm { a n d } \frac { - \mathbb { E } _ { a } [ \mathbb { L } ( \theta , \theta _ { 0 } ) ] } { \mathbf { r } ^ { 2 } } } \end{array}$ □

Now recall the definition of $\Delta ( \mathbf { r } _ { 0 } , \eta )$ from [23, Theorem 9], that is $\Delta ( { \bf r } _ { 0 } , \eta ) = \{ \delta ( { \bf r } _ { 0 } ) + 6 v _ { 0 } z _ { \mathbb { H } } ( \eta ) \omega \} { \bf r } _ { 0 } ^ { 2 } .$ where $z _ { \mathbb H } ( \eta ) : = 2 \sqrt d + \sqrt { 2 \eta } + \mathbf g ^ { - 1 } ( \mathbf g ^ { - 2 } \eta + 1 ) 4 d .$ . Note that, we denote $\Delta ( \mathbf { r } _ { 0 } , \eta )$ as $\Delta _ { t } ( d , \eta )$ to explicitly show the dependence of $d , t ,$ and η. Since ω (from $( E D 1 )$ can be any positive number, we choose $\begin{array} { r } { \omega = \frac { \delta ( \mathbf { r } _ { 0 } ) } { 6 v _ { 0 } z _ { \mathrm { H } } ( \eta ) } } \end{array}$ . Therefore, $\begin{array} { r } { \Delta ( \mathbf { r } _ { 0 } , \eta ) = 2 \delta ( \mathbf { r } _ { 0 } ) \mathbf { r } _ { 0 } ^ { 2 } \le L \frac { \mathbf { r } _ { 0 } ^ { 3 } } { \mathbb { T } _ { 2 } ^ { 1 / 2 } } } \end{array}$ . Moreover, $\mathbf { r } _ { 0 } ^ { 2 } = C ( \eta + d )$ for some known

constant C [26, Section 5.2]. Note that $\left\| a _ { s } \right\| = 1$ because g is monotonic and the optimizer of $a ^ { \top } \theta$ on $a \in \mathbb { R } ^ { d } : \| a \| \leq 1$ is nothing but $\theta / | | \theta | |$ , which lies on a d-dimensional sphere $( S ^ { d - 1 } )$ . Observe that for some $\begin{array} { r } { \gamma ^ { * } \in \mathbb R ^ { d } \backslash \{ 0 \} , \ \mathbb T ^ { - 1 / 2 } : = \operatorname* { m a x } _ { s \in [ t ] } \operatorname* { s u p } _ { \gamma \in \mathbb R ^ { d } } \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 1 / 2 } | a _ { s } ^ { \top } \gamma | } { \| \mathbb D _ { 0 } \gamma \| } \ = \ \operatorname* { m a x } _ { s \in [ t ] } \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 1 / 2 } | a _ { s } ^ { \top } \gamma ^ { * } | } { \| \mathbb D _ { 0 } \gamma ^ { * } \| } \ \leq } \end{array}$ $\begin{array} { r l r } { \frac { 1 } { \sqrt { t } } \frac { \operatorname* { m a x } _ { s \in [ t ] } A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 1 / 2 } \vert a _ { s } ^ { \top } \gamma ^ { * } \vert } { \operatorname* { m i n } _ { s \in [ t ] } A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 1 / 2 } \vert a _ { s } ^ { \top } \gamma ^ { * } \vert } \le } & { \frac { 1 } { \sqrt { t } } \sqrt { \frac { C _ { g } } { m } } \frac { \operatorname* { m a x } _ { s \in [ t ] } \vert a _ { s } ^ { \top } \gamma ^ { * } \vert } { \operatorname* { m i n } _ { s \in [ t ] : \vert a _ { s } ^ { \top } \gamma ^ { * } \vert \neq 0 } \vert a _ { s } ^ { \top } \gamma ^ { * } \vert } \le } & { \frac { 1 } { \sqrt { t } } \sqrt { \frac { C _ { g } } { m } } \frac { \Vert \gamma ^ { * } \Vert } { \operatorname* { m i n } _ { s \in [ t ] : \vert a _ { s } ^ { \top } \gamma ^ { * } \vert \neq 0 } \vert a _ { s } ^ { \top } \gamma ^ { * } \vert } . } \end{array}$ Since, $\gamma ^ { * } \neq 0$ and $a _ { s } \ \in \ S ^ { d - 1 }$ , we can compute a t-independent lower bound on $\begin{array} { r l } { \operatorname* { m i n } _ { s \in [ t ] : | a _ { s } ^ { \top } \gamma ^ { * } | \neq 0 } | a _ { s } ^ { \top } \gamma ^ { * } | } & { \geq } \end{array}$ mi $\mathsf { l } _ { a \in S ^ { d - 1 } : | a ^ { \top } \gamma ^ { * } | \neq 0 } | a _ { s } ^ { \top } \gamma ^ { * } |$ . Therefore, $\mathbb { T } ^ { - 1 / 2 } = O ( t ^ { - 1 / 2 } )$ . By definition of $\mathbb { D } _ { 0 } ^ { 2 } ,$ observe that $\| \mathbb { D } _ { 0 } \gamma ^ { * } \| ^ { 2 } =$ $\gamma ^ { \top } \mathbb { D } _ { 0 } ^ { 2 } \gamma$   
$\begin{array} { r l } & { \stackrel { \cdot } { = } \sum _ { s = 1 } ^ { t } A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) | \gamma ^ { \top } a _ { s } | | a _ { s } ^ { \top } \gamma | \geq A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) | a _ { s } ^ { \top } \gamma | ^ { 2 } } \end{array}$ . Choosing $t _ { 1 }$ large enough and fixing $\eta = O ( d )$ , we have $\Delta _ { t } ( d , \eta ) \leq 1$

## E Verifying assumptions for the sub-Gaussian family

First, we show a general result for the sub-Gaussian family of distribution that implies Assumption 2.

Lemma 8. Fix $\alpha \in ( 0 , 1 )$ . For any two sub-Gaussian measures µ and ν with sub-Gaussian parameter $\sigma _ { \mu }$ and $\sigma _ { \nu }$ respectively, such that ν is absolutely continuous wrt $\mu ,$ the α−Renyi divergence can be bounded´ below by the absolute difference between the respective means. In particular, for any random variable X having measure µ and $\nu ,$ we have $\begin{array} { r } { | \mathbb { E } _ { \nu } [ X ] - \mathbb { E } _ { \mu } [ X ] | \leq \sqrt { ( \sigma _ { \mu } ^ { 2 } \alpha + \sigma _ { \nu } ^ { 2 } ( 1 - \alpha ) ) } \sqrt { \frac { 2 } { \alpha } { D } _ { \alpha } ( \nu \| \mu ) } . } \end{array}$

Proof. Proof of Lemma 8: For any $\alpha \in ( 0 , 1 )$ , recall the definition of α-Renyi divergence´ $D _ { \alpha } ( \nu \| \mu ) =$ log $\begin{array} { r } { \int g ^ { \alpha } d \mu = \frac { 1 } { \alpha - 1 } { \log \int \left( \frac { d \nu } { d \mu } \right) } ^ { \alpha } d \mu } \end{array}$ , where $\begin{array} { r } { g \equiv \frac { d \nu } { d \mu } } \end{array}$ . Now observe that

$$
\begin{array} { r l } { ( \alpha - 1 ) D _ { \alpha } \big ( \nu \| \mu \big ) = \log \mathbb { E } _ { \nu } \big [ g ^ { \alpha - 1 } e ^ { - ( \alpha - 1 ) X } e ^ { ( \alpha - 1 ) X } \big ] = \log \displaystyle \int ( g e ^ { - X } ) ^ { \alpha - 1 } e ^ { ( \alpha - 1 ) X } d \nu } & { } \\ { \leq \log \left( \displaystyle \int e ^ { ( \alpha - 1 ) X } d \nu \right) ^ { \alpha } \left( \displaystyle \int ( g e ^ { - X } ) ^ { - 1 } e ^ { ( \alpha - 1 ) X } d \nu \right) ^ { 1 - \alpha } } & { } \\ { = \log \left( \mathbb { E } _ { \nu } \big [ e ^ { ( \alpha - 1 ) X } \big ] ^ { \alpha } \mathbb { E } _ { \nu } \big [ g ^ { - 1 } e ^ { \alpha X } \big ] ^ { 1 - \alpha } \right) } & { } \\ { = \log \left( \mathbb { E } _ { \nu } \big [ e ^ { ( \alpha - 1 ) X } \big ] ^ { \alpha } \mathbb { E } _ { \mu } \big [ e ^ { \alpha X } \big ] ^ { 1 - \alpha } \right) } & { } \\ { = \alpha \log \mathbb { E } _ { \nu } \big [ e ^ { ( \alpha - 1 ) X } \big ] + ( 1 - \alpha ) \log \mathbb { E } _ { \mu } \big [ e ^ { \alpha X } \big ] , } & { } \end{array}\tag{41}
$$

where the first inequality follows from the Holder’s inequality wrt measure¨ $e ^ { ( \alpha - 1 ) X } d \nu$

Now fix $X = s X$ for any $s \in \mathbb { R }$ (without loss of generality). Since, $\alpha \in ( 0 , 1 )$ , it follows from the inequality above that

$$
\begin{array} { l l } { \displaystyle D _ { \alpha } ( \nu \| \mu ) \geq \left[ \frac { \alpha } { \alpha - 1 } \log \mathbb { E } _ { \nu } [ e ^ { ( \alpha - 1 ) s X } ] - \log \mathbb { E } _ { \mu } [ e ^ { \alpha s X } ] \right] } \\ { \displaystyle \qquad \geq \left[ \frac { \alpha } { \alpha - 1 } [ s ( \alpha - 1 ) \mathbb { E } _ { \nu } [ X ] + \sigma _ { \nu } ^ { 2 } s ^ { 2 } ( \alpha - 1 ) ^ { 2 } / 2 ] - [ s \alpha \mathbb { E } _ { \mu } [ X ] + \sigma _ { \mu } ^ { 2 } s ^ { 2 } \alpha ^ { 2 } / 2 ] \right] } \\ { \displaystyle \qquad = \left[ s \alpha [ \mathbb { E } _ { \nu } [ X ] - \mathbb { E } _ { \mu } [ X ] ] - \frac { s ^ { 2 } \alpha } { 2 } [ \sigma _ { \nu } ^ { 2 } ( 1 - \alpha ) + \sigma _ { \mu } ^ { 2 } \alpha ] \right] , } \end{array}\tag{42}
$$

where the second inequality uses the fact that $\mu$ and ν are sub-Gaussian measures. Recall if $X \sim \mu ,$ is a sub-Gaussian random variable, then $\mathbb { E } _ { \mu } [ e ^ { s X } ] \stackrel { \cdot } { \le } e ^ { s \mathbb { E } _ { \mu } [ X ] + \sigma _ { \mu } ^ { 2 } s ^ { 2 } / 2 }$ for all $s \in \mathbb { R }$ . Now it follows from above that $\begin{array} { r } { | \mathbb { E } _ { \nu } [ X ] - \mathbb { E } _ { \mu } [ X ] | \leq \operatorname* { i n f } _ { | s | } \frac { D _ { \alpha } ( \nu \| \mu ) } { | s | \alpha } + \frac { | s | } { 2 } ( \sigma _ { \mu } ^ { 2 } \alpha + \sigma _ { \nu } ^ { 2 } ( 1 - \alpha ) ) } \end{array}$ □ □

Next, we provide a technical result that bounds 2-Renyi divergence between two random variables that are´ modeled with additive noise.

Lemma 9. For a given $a \in \mathbb { A }$ and any $\theta \in \Theta ,$ , let $X = g ( a ^ { \top } \theta ) + \eta $ , where $g ( \cdot )$ is a known twice differentiable mapping and η is a random variable with density $p ( \cdot )$ . Then for any $\epsilon > 0 ,$ , there exist a $\theta ^ { * } ( \epsilon )$ in $B ( \theta _ { 0 } , \epsilon )$ , a convex ball centred at $\theta _ { 0 }$ with radius $\epsilon ,$ such that for any $\theta \in B ( \theta _ { 0 } , \epsilon ) , D _ { 2 } ( \theta , \theta _ { 0 } ) =$ $( \theta - \theta _ { 0 } ) ^ { \top } a ^ { \top } \mathcal { Z } ( \theta ^ { * } ( \epsilon ) ) a ( \theta - \theta _ { 0 } )$ , where $\mathcal { Z } ( \theta ) = \mathbb { E } _ { \theta } \left[ ( \nabla _ { u } \log p ( x - g ( u ) ) ) ( \nabla _ { u } \log p ( x - g ( u ) ) ) ^ { \top } \Big | _ { u = a ^ { \top } \theta } \right]$ and $\mathcal { T } ( \theta _ { 0 } ) a a ^ { \top }$ is the Fisher information about $\theta _ { 0 }$ that X contains.

Proof. Proof: Recall from the definition of the 2−Renyi divergence that, for a given ´ a $\in \mathbb { A }$

$$
\begin{array} { l } { \displaystyle { D _ { 2 } ( \theta , \theta _ { 0 } ) = \log \int p _ { \theta } ( x | a ) ^ { 2 } p _ { \theta _ { 0 } } ( x | a ) ^ { - 1 } d x } } \\ { \displaystyle { \ = \log \mathbb { E } _ { \theta _ { 0 } } \left[ \frac { p _ { \theta } ( x | a ) ^ { 2 } } { p _ { \theta _ { 0 } } ( x | a ) ^ { 2 } } \right] = \log \mathbb { E } _ { \theta _ { 0 } } \left[ \frac { p ( x - g ( a ^ { \top } \theta ) ) ^ { 2 } } { p ( x - g ( a ^ { \top } \theta _ { 0 } ) ) ^ { 2 } } \right] . } } \end{array}\tag{43}
$$

For brevity, we denote $\dot { f }$ and $\ddot { f }$ as the first and second derivative of $f$ for $f = \{ g , p \}$ . It is straightforward to compute the gradient of $D _ { 2 }$ with respect to θ as

$$
\nabla _ { \theta } D _ { 2 } = - \left( \mathbb { E } _ { \theta _ { 0 } } \left[ \frac { p ( x - g ( a ^ { \top } \theta ) ) ^ { 2 } } { p ( x - g ( a ^ { \top } \theta _ { 0 } ) ) ^ { 2 } } \right] \right) ^ { - 1 } \mathbb { E } _ { \theta _ { 0 } } \left[ \frac { 2 p ( x - g ( a ^ { \top } \theta ) ) \dot { p } ( x - g ( a ^ { \top } \theta ) ) \dot { g } ( a ^ { \top } \theta ) a ^ { \top } } { p ( x - g ( a ^ { \top } \theta _ { 0 } ) ) ^ { 2 } } \right] .\tag{44}
$$

Similarly, the Hessian of $D _ { 2 }$ is computed as,

$$
\begin{array} { r l } & { \nabla _ { \theta } ^ { 2 } D _ { 2 } = - \left( \mathbb { E } _ { \theta _ { 0 } } \left[ \frac { p \left( x - g ( a ^ { \top } \theta ) \right) ^ { 2 } } { p \left( x - g ( a ^ { \top } \theta _ { 0 } ) \right) ^ { 2 } } \right] \right) ^ { - 2 } \left( \mathbb { E } _ { \theta _ { 0 } } \left[ \frac { 2 p ( x - g ( a ^ { \top } \theta ) ) \dot { p } ( x - g ( a ^ { \top } \theta ) ) \dot { g } ( a ^ { \top } \theta ) } { p ( x - g ( a ^ { \top } \theta _ { 0 } ) ) ^ { 2 } } \right] \right) ^ { 2 } a a ^ { \top } } \\ & { \quad \quad \quad + \left( \mathbb { E } _ { \theta _ { 0 } } \left[ \frac { p \left( x - g ( a ^ { \top } \theta ) \right) ^ { 2 } } { p \left( x - g ( a ^ { \top } \theta _ { 0 } ) \right) ^ { 2 } } \right] \right) ^ { - 1 } } \\ & { \quad \quad \quad \quad 2 p ( x - g ( a ^ { \top } \theta ) ) \dot { p } ( x - g ( a ^ { \top } \theta ) ) \dot { g } ( a ^ { \top } \theta ) ^ { 2 } + 2 \left( \dot { p } ( x - g ( a ^ { \top } \theta ) ) \dot { g } ( a ^ { \top } \theta ) \right) ^ { 2 } } \\ & { \mathbb { E } _ { \theta _ { 0 } } \left[ \frac { - 2 p ( x - g ( a ^ { \top } \theta ) ) \dot { p } ( x - g ( a ^ { \top } \theta ) ) \dot { g } ( x - g ( a ^ { \top } \theta ) ) \dot { g } ( a ^ { \top } \theta ) } { p ( x - g ( a ^ { \top } \theta _ { 0 } ) ) ^ { 2 } } \right] a a ^ { \top } . } \end{array}\tag{45}
$$

When we evaluate the above Hessian at $\theta = \theta _ { 0 }$ using the fact that $\nabla _ { \boldsymbol { \theta } } \mathbb { E } _ { \boldsymbol { \theta } _ { 0 } } [ \log { p ( x - g ( a ^ { \top } \boldsymbol { \theta } ) ) } ] \Big \rvert _ { \boldsymbol { \theta } = \boldsymbol { \theta } _ { 0 } } = 0$ and $\begin{array} { r } { \mathbb { E } _ { \theta _ { 0 } } \left[ \frac { 2 \nabla _ { \theta } ^ { 2 } p ( x - g ( a ^ { \top } \theta ) ) } { p ( x - g ( a ^ { \top } \theta _ { 0 } ) ) } \right] = \nabla _ { \theta } ^ { 2 } \int p _ { \theta } ( x | a ) d x = 0 } \end{array}$ , then

$$
\nabla _ { \theta } ^ { 2 } D _ { 2 } \bigg | _ { \theta = \theta _ { 0 } } = \mathbb { E } _ { \theta _ { 0 } } \bigg [ 2 ( \nabla _ { u } \log p ( x - g ( u ) ) ) ( \nabla _ { u } \log p ( x - g ( u ) ) ) ^ { \top } \Big | _ { u = a ^ { \top } \theta _ { 0 } } \bigg ] a a ^ { \top } = 2 \mathcal { Z } ( \theta _ { 0 } ) a a ^ { \top } .\tag{46}
$$

Now using second-order Taylor’s theorem, it follows that for any $\epsilon > 0$ , there exists a $\theta ^ { * } ( \epsilon )$ in $B ( \theta _ { 0 } , \epsilon )$ such that for any $\theta \in B ( \theta _ { 0 } , \epsilon ) , D _ { 2 } ( \theta , \theta _ { 0 } ) = 2 ( \theta - \theta _ { 0 } ) ^ { \top } a ^ { \top } \mathcal { Z } ( \theta ^ { * } ( \epsilon ) ) a ( \theta - \theta _ { 0 } )$ □ □

Our next result shows that the sub-Gaussian family satisfies Assumption 4.

Lemma 10. Under Assumption 1, the sub-Gaussian family of distributions as defined in Definition 4, satisfies Assumption 4 for $\begin{array} { r } { \epsilon _ { t } ^ { 2 } = \frac { 4 d \tilde { C } \log ( t ) } { D \alpha C _ { \phi } t } } \end{array}$ and sufficiently large $: ( t \geq \log ( t ) / C _ { \phi } )$ , where $\tilde { C } \geq \mathcal { I } ( \theta )$ for any $\theta \in B ( \theta _ { 0 } , 1 )$ and $\begin{array} { r } { C _ { \phi } = \operatorname* { m i n } _ { i \in [ d ] , x \in B ( \theta _ { 0 } ^ { i } , 1 ) } \phi _ { i } ( x ) , B ( \theta _ { 0 } ^ { i } , u ) \subset \mathbb { R } } \end{array}$ is a ball ofradius u centered at $\theta _ { 0 } ^ { i } \in \Theta$ and $\phi _ { i } ( \cdot )$ is the density ofΠ<sup>i</sup>.

Proof. Proof of Lemma 10: Using Lemma 9, there exist a $\theta ^ { * } ( 1 ) \in B ( \theta _ { 0 } , 1 )$ (defined later), a convex ball centred at $\theta _ { 0 }$ with radius 1, such that for any $\theta \in B ( \theta _ { 0 } , 1 )$ ,

$$
\begin{array} { r l } & { \mathcal { D } _ { 2 } ^ { ( t ) } ( \theta , \theta _ { 0 } ) = \log \displaystyle \int p _ { \theta } ^ { ( t ) } ( r ^ { ( t ) } | a ^ { ( t ) } ) ^ { 2 } p _ { 0 } ^ { ( t ) } ( r ^ { ( t ) } | a ^ { ( t ) } ) ^ { - 1 } d \mu ^ { ( t ) } } \\ & { \qquad = \displaystyle \sum _ { s = 1 } ^ { t } \log \int p _ { \theta } ( r _ { s } | \mathcal { F } _ { t - 1 } , a _ { s } ) ^ { 2 } p _ { 0 } ( r _ { s } | \mathcal { F } _ { s - 1 } , a _ { s } ) ^ { - 1 } d \mu } \\ & { \qquad \le \tilde { C } ( \theta - \theta _ { 0 } ) ^ { \top } \left( \displaystyle \sum _ { s = 1 } ^ { t } a _ { s } a _ { s } ^ { \top } \right) ( \theta - \theta _ { 0 } ) \le t \tilde { C } \| \theta - \theta _ { 0 } \| ^ { 2 } . } \end{array}\tag{47}
$$

where in the first inequality $\tilde { C }$ is the bound on $\mathcal { T } ( \boldsymbol { \theta } )$ for any $\theta \in B ( \theta _ { 0 } , 1 )$ and the last inequality follows due to CS and Assumption 1. Now observe for $B ( \theta _ { 0 } , 1 ) = \{ \theta \in \Theta : \| \theta - \theta _ { 0 } ) \| \leq 1 \}$ that

$$
\begin{array} { l l l } { \displaystyle \Pi \left( D _ { 2 } ^ { ( t ) } ( \pmb \theta , \theta _ { 0 } ) \leq \frac { D \alpha } { 4 } t \epsilon _ { t } ^ { 2 } \right) \geq \Pi \left( B ( \theta _ { 0 } , 1 ) , D _ { 2 } ^ { ( t ) } ( \theta , \theta _ { 0 } ) \leq \frac { D \alpha } { 4 } t \epsilon _ { t } ^ { 2 } \right) } \\ { \displaystyle \qquad \geq \Pi \left( B ( \theta _ { 0 } , 1 ) , \| \theta - \theta _ { 0 } \| ^ { 2 } \leq \frac { D \alpha } { 4 \tilde { C } } \epsilon _ { t } ^ { 2 } \right) } \\ { \displaystyle \qquad = \Pi \left( ( \theta - \theta _ { 0 } ) ^ { \top } ( \theta - \theta _ { 0 } ) \leq \frac { D \alpha } { 4 \tilde { C } } \epsilon _ { t } ^ { 2 } \right) } \\ { \displaystyle \qquad \geq \displaystyle \prod _ { i = 1 } ^ { d } \Pi ^ { i } \left( ( \theta ^ { i } - \theta _ { 0 } ^ { i } ) ^ { 2 } \leq \frac { D \alpha } { 4 d \tilde { C } } \epsilon _ { T } ^ { 2 } \right) , } \end{array}\tag{48}
$$

where the second inequality follows from equation 47 and the first equality is due to the assumption that $\begin{array} { r } { \frac { D \alpha } { 4 \tilde { C } d } \epsilon _ { t } ^ { 2 } < 1 } \end{array}$ (for sufficiently large t). Now the result follows for $\begin{array} { r } { \epsilon _ { t } ^ { 2 } = \frac { 4 d \tilde { C } \log ( t ) } { D \alpha t C _ { \phi } } } \end{array}$

To show that the sub-Gaussian family satisfies Assumption 6, we first write down various expressions used in their definition for this family. The conditional log-likelihood of generating $X _ { t } | a ^ { ( t ) }$ as $\mathbb { L } ( \theta ) =$ log $\begin{array} { r } { \prod _ { s = 1 } ^ { t } \left[ p _ { \eta } ( r _ { s } - a _ { s } ^ { \top } \theta ) \right] } \end{array}$ , where $p _ { \eta }$ is the density of the sub-Gaussian error with sub-Gaussian parameter 1. Also, denote log $p _ { \eta } : = h _ { \eta }$ . The stochastic part of the conditional log-likelihood is denoted as $\zeta ( \theta ) : = \mathbb { L } ( \theta ) -$ $\mathbb { E } [ \mathbb { L } ( \theta ) | a ^ { ( t ) } ]$ and $\begin{array} { r } { \dot { \nabla } \zeta ( \theta ) \overset { , } { = } \nabla \mathbb { L } ( \theta ) - \mathbb { E } [ \nabla \mathbb { L } ( \theta ) | a ^ { ( t ) } ] = \sum _ { s = 1 } ^ { t } - { h _ { \eta } ^ { \prime } } ( r _ { s } - a _ { s } ^ { \top } \theta ) a _ { s } ^ { \top } + \mathbb { E } [ { h _ { \eta } ^ { \prime } } ( r _ { s } - a _ { s } ^ { \top } \theta ) a _ { s } ^ { \top } | a ^ { ( t ) } ] } \end{array}$ and therefore $\begin{array} { r } { \mathbb { E } [ \nabla \zeta ( \theta _ { 0 } ) | a ^ { ( t ) } ] = - \sum _ { s = 1 } ^ { t } h _ { \eta } ^ { \prime } ( r _ { s } - a _ { s } ^ { \top } \theta ) a _ { s } ^ { \top } \mathrm { ( s i n c e ~ } \mathbb { E } [ \nabla \mathbb { L } ( \theta _ { 0 } ) | a ^ { ( t ) } ] = 0 ) } \end{array}$ . We assume that $h _ { \eta }$ is twice continuously differentiable and let $\begin{array} { r } { \mathbf { \dot { h } } ^ { 2 } : = - \int h _ { \eta } ^ { \prime \prime } ( z ) p _ { \eta } ( z ) d z < \infty . \mathrm { A l s o } } \end{array}$

$$
\mathbb { D } _ { 0 } ^ { 2 } = \mathbb { D } _ { 0 } ^ { 2 } ( \theta _ { 0 } ) = - \nabla ^ { 2 } \mathbb { E } [ \mathbb { L } ( \theta _ { 0 } ) | a ^ { ( t ) } ] = - \sum _ { s = 1 } ^ { t } \mathbb { E } [ h _ { \eta } ^ { \prime \prime } ( r _ { s } - a _ { s } ^ { \top } \theta _ { 0 } ) | a ^ { ( t ) } ] a _ { s } a _ { s } ^ { \top } = \mathbf { h } ^ { 2 } \sum _ { s = 1 } ^ { t } a _ { s } a _ { s } ^ { \top } .\tag{49}
$$

Similarly, $\begin{array} { r } { \mathbb { D } _ { 0 } ^ { 2 } ( \theta ) = - \sum _ { s = 1 } ^ { t } \mathbb { E } [ h _ { \eta } ^ { \eta } ( r _ { s } - a _ { s } ^ { \top } \theta ) | a ^ { ( t ) } ] a _ { s } a _ { s } ^ { \top } = - \sum _ { s = 1 } ^ { t } \mathbb { E } [ h _ { \eta } ^ { \eta } ( \eta + a _ { s } ^ { \top } ( \theta _ { 0 } - \theta ) ) | a ^ { ( t ) } ] a _ { s } a _ { s } ^ { \top } } \end{array}$ . Henceforth, use $\mathbb { E } _ { a }$ in place of the conditional expectation $\mathbb { E } [ \cdot | a ^ { ( t ) } ]$

Next, we assume two other conditions on the error density $p _ { \eta }$

## Assumption 8.

1. There exists some constant v<sub>0</sub> and $\mathbf { g } _ { 1 } ~ > ~ 0 ,$ , such that a random variable $\eta \sim p _ { \eta }$ , it holds that log E ex $\mathrm { p } ( \mu h _ { \eta } ^ { \prime } ( \eta ) / \mathbf { h } ) \le v _ { 0 } ^ { 2 } \mu ^ { 2 } / 2 , \quad | \mu | < \mathbf { g } _ { 1 }$

2. There exists some constant $v _ { 0 }$ and for every $\mathbf { r } > 0 ;$ , there exists $\mathbf { g } _ { 1 } ( \mathbf { r } ) > 0 ,$ , such that for all δ with $| \delta | <  { \mathbb { T } } _ { 2 } ^ { - 1 / 2 }  { \mathbf { r } } / \vartheta _ { s }$ it holds that log $\begin{array} { r } { \mathbb { E } \left[ \exp \left( \frac { \mu } { \vartheta _ { s } ^ { 2 } } \left\{ h _ { \eta } ^ { \prime \prime } ( \eta _ { s } + \delta ) - \mathbb { E } [ h _ { \eta } ^ { \prime \prime } ( \eta _ { s } + \delta ) ] \right\} \right) \right] \le v _ { 0 } ^ { 2 } \mu ^ { 2 } / 2 , \quad | \mu | < \delta } \end{array}$ $\mathbf { g } _ { 1 } ( \mathbf { r } )$ , where $\vartheta _ { s }$ is known value (given $a ^ { ( t ) } )$ and $\begin{array} { r } { \mathbb T _ { 2 } ^ { - 1 / 2 } : = \operatorname* { m a x } _ { s } \operatorname* { s u p } _ { \gamma \in \mathbb R ^ { d } } \frac { \vartheta _ { s } | a _ { s } ^ { \top } \gamma | } { \| \mathbb D _ { 0 } \gamma \| } } \end{array}$

The above assumption essentially requires that the error distribution has an exponentially decaying tail. Since we assume that the error distribution is sub-Gaussian due to Assumption 4, the above condition is automatically satisfied.

Lemma 11. The sub-Gaussianfamily ofdistributions (Definition 4) satisfies Assumption 6. The condition $( E D _ { 0 } )$ is satisfied with $\mathbf { g } : = \mathbf { g } _ { 1 } \mathbb { T } ^ { 1 / 2 }$ , where $\begin{array} { r } { \mathbb { T } ^ { - 1 / 2 } : = \operatorname* { m a x } _ { s \in [ t ] } \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } } \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { \widehat { 1 } / 2 } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } } \end{array}$ and condition $\left( E D _ { 1 } \right)$ can be satisfied for any $\omega > 0$ and $\mathbf { g } > 0$ . Condition $\mathcal { L } _ { 0 }$ follows for $\ddot { \delta } ( \mathbf { r } ) = L \mathbb { T } _ { 2 } ^ { - 1 / 2 } \mathbf { r }$ , where T $\begin{array} { r } { \mathfrak { s } _ { - 1 / 2 } : = \operatorname* { m a x } _ { s \in [ t ] } \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } } \frac { A ^ { \prime \prime } ( a _ { s } ^ { \top } \theta _ { 0 } ) ^ { 1 / 2 } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } \mathfrak { 0 } \gamma \| } } \end{array}$

Proof. Proof: We derive all the conditions in seriatim. The proofs are adapted from [23].

1. Proof for $\left( E D _ { 0 } \right)$ : Using the definition of $\nabla \zeta ( \theta _ { 0 } )$ and $\mathbb { D } _ { 0 }$ , observe that

$$
\mathbb { E } _ { a } \exp \left\{ \mathbf { m } \frac { \langle \nabla \zeta ( \theta _ { 0 } ) , \gamma \rangle } { \| \mathbb { D } _ { 0 } \gamma \| } \right\} = \mathbb { E } _ { a } \exp \left\{ - \mathbf { m } \frac { \sum _ { s = 1 } ^ { t } h _ { \eta } ^ { \prime } ( r _ { s } - a _ { s } ^ { \top } \theta ) a _ { s } ^ { \top } \gamma } { \| \mathbb { D } _ { 0 } \gamma \| } \right\}\tag{50}
$$

$$
= \mathbb { E } _ { a } \exp \left\{ \frac { - \mathbf { m } \mathbf { h } \sum _ { s = 1 } ^ { t } { a _ { s } ^ { \top } \gamma } } { \| \mathbb { D } _ { 0 } \gamma \| } \frac { h _ { \eta } ^ { \prime } ( \eta ) } { \mathbf { h } } \right\} .\tag{51}
$$

Define $\begin{array} { r } { \mathbb { T } ^ { - 1 / 2 } : = \operatorname* { m a x } _ { s } \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } } \frac { \mathbf { h } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } \geq \frac { \mathbf { h } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } } \end{array}$ and $\mathbf { g } = \mathbf { g } _ { 1 } \mathbb { T } ^ { 1 / 2 }$ . Consequently, for $\begin{array} { r } { \mu = \frac { - \mathbf { m } \mathbf { h } a _ { s } ^ { \top } \gamma } { \| \mathbb { D } _ { 0 } \gamma \| } } \end{array}$ $\begin{array} { r } { | \mu | \leq | { \bf m } | \frac { { \bf h } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } < { \bf g } ^ { \top 1 / 2 } = { \bf g } _ { 1 } } \end{array}$ . Therefore, using Assumption 8, the result follows as

$$
\mathbb { E } _ { a } \exp \left\{ \frac { - \mathbf { m } \mathbf { h } \sum _ { s = 1 } ^ { t } a _ { s } ^ { \top } \gamma } { \| \mathbb { D } _ { 0 } \gamma \| } \frac { h _ { \eta } ^ { \prime } ( \eta ) } { \mathbf { h } } . \right\} \leq \exp \left( \frac { v _ { 0 } ^ { 2 } \mathbf { m } ^ { 2 } } { 2 } \frac { \mathbf { h } ^ { 2 } \sum _ { s = 1 } ^ { t } | a _ { s } ^ { \top } \gamma | ^ { 2 } } { \| \mathbb { D } _ { 0 } \gamma \| ^ { 2 } } \right) = \exp \left( \frac { v _ { 0 } ^ { 2 } \mathbf { m } ^ { 2 } } { 2 } \right) .\tag{52}
$$

2. Proof for $( E D _ { 1 } ) \colon$ : Using the definition of $\zeta ( \theta )$ , observe that

$$
\begin{array} { r l } & { \mathbb { E } _ { a } \exp \left\{ \displaystyle \frac { \mathbf { m } } { \omega } \frac { \gamma _ { 1 } ^ { \top } \nabla ^ { 2 } \zeta ( \theta ) \gamma _ { 2 } } { \| \mathbb { D } _ { 0 } \gamma _ { 1 } \| \| \mathbb { D } _ { 0 } \gamma _ { 2 } \| } \right\} } \\ & { \quad = \mathbb { E } _ { a } \exp \left\{ \displaystyle \frac { \mathbf { m } } { \omega } \frac { \sum _ { s = 1 } ^ { t } \big ( h _ { \eta } ^ { \prime \prime } ( r _ { s } - a _ { s } ^ { \top } \theta ) - \mathbb { E } _ { a } [ h _ { \eta } ^ { \prime \prime } ( r _ { s } - a _ { s } ^ { \top } \theta ) ] \big ) \gamma _ { 1 } ^ { \top } a _ { s } a _ { s } ^ { \top } \gamma _ { 2 } } { \| \mathbb { D } _ { 0 } \gamma _ { 1 } \| \| \mathbb { D } _ { 0 } \gamma _ { 2 } \| } \right\} } \\ & { \quad = \mathbb { E } _ { a } \exp \left\{ \displaystyle \frac { \mathbf { m } } { \omega } \frac { \sum _ { s = 1 } ^ { t } \big ( h _ { \eta } ^ { \prime \prime } ( \eta _ { s } - a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) ) - \mathbb { E } _ { a } [ h _ { \eta } ^ { \prime \prime } ( \eta _ { s } - a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) ) ] \big ) \gamma _ { 1 } ^ { \top } a _ { s } a _ { s } ^ { \top } \gamma _ { 2 } } { \| \mathbb { D } _ { 0 } \gamma _ { 1 } \| \| \mathbb { D } _ { 0 } \gamma _ { 2 } \| } \right\} . } \end{array}\tag{53}
$$

Using the definition of $\mathbb { T } _ { 2 }$ for $\theta \in \mathsf { \Gamma } \Theta _ { 0 } ( \mathbf { r } )$ , observe that $\begin{array} { r l r } { \mathbb { T } _ { 2 } ^ { - 1 / 2 } } & { { } \ge } & { \frac { \vartheta _ { s } \vert a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) \vert } { \vert \vert \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \vert \vert } } \end{array}$ . Therefore, $\begin{array} { r } { | a _ { s } ^ { \top } ( \theta \mathrm { ~ - ~ } \theta _ { 0 } ) | ~ \le ~ \mathbb { T } _ { 2 } ^ { - 1 / 2 } \frac { \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \| } { \vartheta _ { s } } ~ \le ~ \mathbb { T } _ { 2 } ^ { - 1 / 2 } \frac { \mathbf { r } } { \vartheta _ { s } } } \end{array}$ . For $\begin{array} { r l } { \mu } & { = \begin{array} { l } { \frac { \mathbf { m } \vartheta _ { s } ^ { 2 } \gamma _ { 1 } ^ { \top } a _ { s } a _ { s } ^ { \top } \gamma _ { 2 } } { \omega \| \mathbb { D } \boldsymbol { 0 } \gamma _ { 1 } \| \| \mathbb { D } \boldsymbol { 0 } \gamma _ { 2 } \| } } \end{array} } \end{array}$ , observe that $| \mu | \leq$

$\begin{array} { r } { \frac { | m | } { \omega } \frac { \vartheta _ { s } | a _ { s } ^ { \top } \gamma _ { 1 } | } { \| \mathbb { D } _ { 0 } \gamma _ { 1 } \| } \frac { \vartheta _ { s } | a _ { s } ^ { \top } \gamma _ { 2 } | } { \| \mathbb { D } _ { 0 } \gamma _ { 2 } \| } \leq \frac { \mathbf { g } ( r ) } { \omega } \mathbb { T } ^ { - 1 } : = \mathbf { g } _ { 1 } ( \mathbf { r } ) } \end{array}$ . It follows from Assumption 8(2), that

$$
\mathbb { E } _ { a } \exp \left\{ \frac { \mathbf { m } } { \omega } \frac { \gamma _ { 1 } ^ { \top } \nabla ^ { 2 } \zeta ( \theta ) \gamma _ { 2 } } { \| \mathbb { D } _ { 0 } \gamma _ { 1 } \| \| \mathbb { D } _ { 0 } \gamma _ { 2 } \| } \right\} \leq \exp \left( \sum _ { s = 1 } ^ { t } v _ { 0 } ^ { 2 } \frac { \mathbf { m } ^ { 2 } \vartheta _ { s } ^ { 4 } | \gamma _ { 1 } ^ { \top } a _ { s } | ^ { 2 } | a _ { s } ^ { \top } \gamma _ { 2 } | ^ { 2 } } { 2 \omega ^ { 2 } \| \mathbb { D } _ { 0 } \gamma _ { 1 } \| ^ { 2 } \| \mathbb { D } _ { 0 } \gamma _ { 2 } \| ^ { 2 } } \right) \leq \exp \left( \frac { v _ { 0 } ^ { 2 } \mathbf { m } ^ { 2 } } { 2 \omega ^ { 2 } } \frac { t } { \mathbb { T } _ { 2 } ^ { 2 } } \right) .\tag{54}
$$

By fixing $\begin{array} { r } { \omega = \frac { \sqrt { t } } { \mathbb { T } _ { 2 } } } \end{array}$ , the result follows.

3. Proof for $( \mathcal { L } _ { 0 } )$ : For $I _ { d } = \mathbb { D } _ { 0 } ^ { - 1 } \mathbb { D } _ { 0 } ^ { 2 } \mathbb { D } _ { 0 } ^ { - 1 }$ , we have by the definition of the operator norm

$$
\begin{array} { r l } & { \| \mathrm { D } _ { 0 } ^ { - 1 } ( \| \widetilde { \mathbb { P } } _ { 0 } ^ { 2 } ( \theta ) - \mathbb { P } _ { 0 } ^ { 2 } ) \| \mathrm { D } _ { 0 } ^ { - 1 } \| } \\ & { = \underset { \gamma \in \mathbb { R } ^ { d + } \times [ 1 ] = 1 } { \operatorname* { s u p } } \Big | \mathrm { V } ^ { \top } \mathbb { T } _ { 0 } ^ { - 1 } ( \mathbb { D } _ { 0 } ^ { 2 } ( \theta ) - \mathbb { D } _ { 0 } ^ { 2 } ) \mathbb { D } _ { 0 } ^ { - 1 } \gamma | } \\ & { = \underset { \gamma \in \mathbb { R } ^ { d + } \times [ 1 ] \times [ 1 ] } { \operatorname* { s u p } } \Big | \displaystyle \sum _ { s = 1 } ^ { t } \Big ( - \mathbb { E } [ h _ { \eta } ^ { \eta } ( \eta + a _ { s } ^ { \top } ( \theta _ { 0 } - \theta ) ) ] \boldsymbol { a } ^ { ( b ) } ] + \mathbb { E } [ h _ { \eta } ^ { \eta } ( \eta ) ] \boldsymbol { a } ^ { ( c ) } ] \Big ) \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } a _ { s } a _ { s } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \gamma } \\ & { \qquad \le \underset { \gamma \in \mathbb { R } ^ { d + } \times [ 1 ] \times [ 1 ] } { \operatorname* { s u p } } \displaystyle \sum _ { s = 1 } ^ { t } \mathbb { E } \Big [ \Big | h _ { \eta } ^ { \eta } ( \eta ) - h _ { \eta } ^ { \eta } ( \eta + a _ { s } ^ { \top } ( \theta _ { 0 } - \theta ) ) \Big | \Big | \boldsymbol { a } ^ { ( c ) } \Big ] \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } a _ { s } a _ { s } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \gamma } \\ &  \qquad \le \underset { \gamma \in \mathbb { R } ^ { d + } \times [ 1 ] } { \operatorname* { s u p } } \displaystyle \sum _ { s = 1 } ^ { t } \mathbb \end{array}\tag{55}
$$

Define $\begin{array} { r } { \mathbb { T } _ { 1 } ^ { - 1 / 2 } : = \operatorname* { m a x } _ { s } \operatorname* { s u p } _ { \gamma \in \mathbb { R } ^ { d } } \frac { { \mathbf { h } } | a _ { s } ^ { \top } \gamma | } { \| \mathbb { D } _ { 0 } \gamma \| } \geq \frac { { \mathbf { h } } | a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) | } { \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \| } } \end{array}$ and observe that

$$
\begin{array} { r l } & { \| \mathbb { D } _ { 0 } ^ { - 1 } ( \mathbb { D } _ { 0 } ^ { 2 } ( \theta ) - \mathbb { D } _ { 0 } ^ { 2 } ) \mathbb { D } _ { 0 } ^ { - 1 } \| \le \underset { \gamma \in \mathbb { R } ^ { d _ { \star } } \times \mathbb { I } ^ { * } } { \operatorname* { s u p } } \sum _ { s = 1 } ^ { t } L \left| a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) \right| \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } a _ { s } a _ { s } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \gamma } \\ & { \qquad \le \frac { L \mathbb { T } _ { 1 } ^ { - 1 / 2 } } { \mathbf { h } } \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \| \underset { \gamma \in \mathbb { R } ^ { d _ { \star } } \times \mathbb { I } \times \mathbb { I } = 1 } { \operatorname* { s u p } } \gamma ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \sum _ { s = 1 } ^ { t } \mathbf { h } ^ { 2 } a _ { s } a _ { s } ^ { \top } \mathbb { D } _ { 0 } ^ { - 1 } \gamma } \\ & { \qquad = \frac { L } { \mathbb { T } _ { 1 } ^ { 1 / 2 } \mathbf { h } } \mathbf { r } . } \end{array}\tag{56}
$$

Consequently, $\delta ( \mathbf { r } ) = L \mathbf { h } ^ { - 1 } \mathbb { T } _ { 2 } ^ { - 1 / 2 } \mathbf { r }$

4. Proof for (Lr): Note that

$$
\begin{array} { r l } & { \mathbb { E } _ { a } \big [ \mathbb { L } ( \theta , \theta _ { 0 } ) \big ] = \mathbb { E } _ { a } \big [ \mathbb { L } ( \theta ) - \mathbb { L } ( \theta _ { 0 } ) \big ] } \\ & { \quad = \displaystyle \sum _ { s = 1 } ^ { t } \mathbb { E } _ { a } \left[ \log \left( \frac { p _ { 0 } ( r _ { s } | a _ { s } ) } { p _ { \theta _ { 0 } } ( r _ { s } | T _ { s - 1 } , a _ { s } ) } \right) \right] } \\ & { \quad = \displaystyle \sum _ { s = 1 } ^ { t } \mathbb { E } _ { a } \left[ h _ { \eta } ( r _ { s } - a _ { s } ^ { \top } \theta ) - h _ { \eta } ( r _ { s } - a _ { s } ^ { \top } \theta _ { 0 } ) \right] } \\ & { \quad = \displaystyle \sum _ { s = 1 } ^ { t } \mathbb { E } _ { a } \left[ - h _ { \eta } ^ { \prime } ( r _ { s } - a _ { s } ^ { \top } \theta _ { 0 } ) a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) + h _ { \eta } ^ { \prime \prime } ( r _ { s } - a _ { s } ^ { \top } \theta ^ { * } ) ( \theta - \theta _ { 0 } ) ^ { \top } a _ { s } a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) \right] } \\ & { \quad = \displaystyle \sum _ { s = 1 } ^ { t } \mathbb { E } _ { a } \big [ h _ { \eta } ^ { \prime \prime } ( r _ { s } - a _ { s } ^ { \top } \theta ^ { * } ) \big ] ( \theta - \theta _ { 0 } ) a _ { s } a _ { s } ^ { \top } ( \theta - \theta _ { 0 } ) = - \| \mathbb { D } _ { 0 } ( \theta ^ { * } ) ( \theta - \theta _ { 0 } ) \| ^ { 2 } , } \end{array}\tag{57}
$$

where the third equality uses second-order Taylor’s theorem for a $\theta ^ { * }$ that lies between $\theta$ and $\theta _ { 0 }$ and the penultimate inequality uses the fact that $\begin{array} { r } { \sum _ { s = 1 } ^ { t } \mathbb { E } _ { a } \left[ - h _ { \eta } ^ { \prime } ( r _ { s } - a _ { s } ^ { \top } \theta _ { 0 } ) a _ { s } ^ { \top } \right] = \nabla \mathbb { E } _ { a } [ \mathbb { L } ( \theta _ { 0 } ) ] = 0 } \end{array}$

Now observe that $\begin{array} { r } { \frac { - \mathbb { E } _ { a } [ \mathbb { L } ( \theta , \theta _ { 0 } ) ] } { { \bf r } ^ { 2 } } = \frac { \| \mathbb { D } _ { 0 } ( \theta ^ { * } ) ( \theta - \theta _ { 0 } ) \| ^ { 2 } } { \| \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \| ^ { 2 } } } \end{array}$ . Note that, by definition $\theta ^ { * }$ is a mapping from r and so is $\frac { \lVert \mathbb { D } _ { 0 } ( \theta ^ { * } ) ( \theta - \theta _ { 0 } ) \rVert ^ { 2 } } { \lVert \mathbb { D } _ { 0 } ( \theta - \theta _ { 0 } ) \rVert ^ { 2 } }$ . Therefore, we can always find a mapping $\mathbf { b } ( \mathbf { r } )$ such that rb $\mathbf { \Sigma } ( \mathbf { r } )  \infty \mathrm { a s } \mathbf { r }  \infty$ and $\begin{array} { r } { \frac { - \mathbb { E } _ { a } [ \mathbb { L } ( \theta , \theta _ { 0 } ) ] } { { \bf r } ^ { 2 } } > { \bf r } ^ { 2 } { \bf b } ( { \bf r } ) } \end{array}$ □

$\Delta _ { t } ( d , \eta ) < 1$ can be established using similar steps as used for exponential family models for ${ \bf r } _ { 0 } ^ { 2 } = C ( d +$ η) [23, Theorem 6] for sufficiently large $t _ { 1 } \geq 1$ and $\eta = O ( d )$